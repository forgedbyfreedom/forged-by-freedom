name: Auto Build and Commit Master Transcripts

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install requests beautifulsoup4 openai

      - name: Run master transcript builder
        run: |
          echo "Running master transcript builder..."
          python3 build_master_transcripts.py

      - name: Commit and push changes
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "Auto-updated master transcripts [skip ci]" || echo "No changes to commit"
          git pull --rebase origin main || true
          git push origin main || echo "No new changes to push"
name: Auto Build and Commit Master Transcripts

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: .

    steps:
      - name: 🧾 Checkout repository
        uses: actions/checkout@v4

      - name: 🐍 Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: 📦 Install dependencies
        run: |
          pip install --upgrade pip
          pip install requests beautifulsoup4 openai

      - name: 🔧 Run master transcript builder
        run: |
          echo "🔧 Running master transcript builder..."
          python3 build_master_transcripts.py

      - name: 💾 Commit and push changes
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "🤖 Auto-updated master transcripts [skip ci]" || echo "No changes to commit"
          git pull --rebase origin main || true
          git push origin main || echo "No new changes to push"
name: Auto Build and Commit Master Transcripts

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: . # Ensure root of repo

    steps:
      - name: 🧾 Checkout repository
        uses: actions/checkout@v4

      - name: 🐍 Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: 📦 Install dependencies
        run: |
          pip install --upgrade pip
          pip install requests beautifulsoup4 openai

      - name: 🔧 Run master transcript builder
        run: |
          echo "🔧 Running master transcript builder..."
          python3 build_master_transcripts.py

      - name: 💾 Commit and push changes
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "🤖 Auto-updated master transcripts [skip ci]" || echo "No changes to commit"
          git pull --rebase origin main || true
          git push origin main || echo "No new changes to push"
name: Auto Build and Commit Master Transcripts

on:
  schedule:
    - cron: "0 */6 * * *"  # every 6 hours
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: 🧭 Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: 🐍 Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: 📦 Install dependencies
        run: |
          pip install -r requirements.txt || echo "No requirements file, continuing"
          pip install requests beautifulsoup4 openai || true

      - name: 🔧 Run master transcript builder
        working-directory: .
        run: |
          echo "🔧 Running master transcript builder..."
          python build_master_transcripts.py

      - name: 🧠 Commit and push updates
        run: |
          git config user.name "GitHub Action"
          git config user.email "action@github.com"
          git add . || true
          git commit -m "Auto-update master transcripts $(date -u +'%Y-%m-%d %H:%M:%S')" || echo "No changes to commit"
          git pull --rebase origin main || true
          git push origin main || echo "No changes to push"
name: Auto Update Master Transcripts

on:
  workflow_dispatch:
  schedule:
    - cron: '0 6 * * *'  # runs every day at 6 AM UTC

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies safely
        run: |
          if [ -f requirements.txt ]; then
            echo "📦 Installing from requirements.txt..."
            pip install -r requirements.txt
          else
            echo "⚙️ No requirements.txt found — installing defaults..."
            pip install "requests>=2.31.0" "beautifulsoup4>=4.12.0" "openai>=1.2.0"
          fi

      - name: Run transcript builder
        run: |
          echo "🔧 Rebuilding master transcripts..."
          python build_master_transcripts.py

      - name: Upload master transcripts to OpenAI
        if: success()
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
        run: |
          echo "📤 Uploading master transcripts to OpenAI..."
          python <<'PYCODE'
import os, glob, openai

openai.api_key = os.environ["OPENAI_API_KEY"]

for path in glob.glob("**/master_transcript*.txt", recursive=True):
    with open(path, "rb") as f:
        try:
            resp = openai.files.create(file=f, purpose="assistants")
            print(f"✅ Uploaded {path} → id={resp.id}")
        except Exception as e:
            print(f"❌ Failed to upload {path}: {e}")
PYCODE

      - name: Commit and safely push changes
        run: |
          set -e
          git config user.name "GitHub Action"
          git config user.email "actions@github.com"

          echo "🔄 Fetching latest remote state..."
          git fetch origin main

          echo "🧩 Rebasing on top of latest main..."
          git rebase origin/main || {
            echo "⚠️ Rebase failed, merging instead..."
            git merge --strategy-option ours origin/main -m "Merge remote changes safely"
          }

          echo "📝 Adding and committing transcript updates..."
          git add -A
          git commit -m "Auto-update master transcripts $(date +'%Y-%m-%d %H:%M:%S')" || echo "No changes to commit."

          echo "🚀 Pushing changes to remote..."
          git push origin HEAD:main || {
            echo "⚠️ Normal push failed, retrying with force-with-lease..."
            git push --force-with-lease origin HEAD:main
          }

          echo "✅ Finished sync successfully."
      - name: Commit and safely push changes
        run: |
          set -e
          git config user.name "GitHub Action"
          git config user.email "actions@github.com"

          echo "🔄 Fetching latest remote state..."
          git fetch origin main

          echo "🧩 Rebasing on top of latest main..."
          git rebase origin/main || {
            echo "⚠️ Rebase failed, trying merge instead..."
            git merge --strategy-option ours origin/main -m "Merge remote changes safely"
          }

          echo "📝 Adding and committing transcript updates..."
          git add -A
          git commit -m "Auto-update master transcripts $(date +'%Y-%m-%d %H:%M:%S')" || echo "No changes to commit."

          echo "🚀 Pushing changes to remote..."
          git push origin HEAD:main || {
            echo "⚠️ Normal push failed, retrying with force-with-lease..."
            git push --force-with-lease origin HEAD:main
          }

          echo "✅ All done!"
      - name: Commit and safely push changes
        run: |
          set -e
          git config user.name "GitHub Action"
          git config user.email "actions@github.com"

          echo "🔄 Syncing with latest remote before committing..."
          git fetch origin main
          git checkout main
          git pull --rebase origin main || echo "⚠️ Pull with rebase failed, continuing."

          echo "📝 Adding and committing any transcript updates..."
          git add -A
          git commit -m "Auto-update master transcripts $(date +'%Y-%m-%d %H:%M:%S')" || echo "No changes to commit."

          echo "🚀 Pushing updates to remote..."
          git push origin main || (echo "⚠️ Push failed, retrying with force-with-lease..." && git push --force-with-lease origin main)

          echo "✅ Finished Git sync successfully."


import { runAIEngine } from './aiEngine';

export async function askCoach(payload) {
  const question = payload?.question?.trim();
  if (!question) {
    throw new Error("NO_QUESTION_PROVIDED");
  }

  return await runAIEngine(question);
}

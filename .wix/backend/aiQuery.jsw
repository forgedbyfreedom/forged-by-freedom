// backend/aiQuery.jsw

export async function queryPinecone(query) {
  console.log("🧠 Backend function called");
  console.log("Query:", query);

  // TEMP: hard response to verify Wix wiring
  return {
    answer: `Backend received your question: "${query}"`
  };
}

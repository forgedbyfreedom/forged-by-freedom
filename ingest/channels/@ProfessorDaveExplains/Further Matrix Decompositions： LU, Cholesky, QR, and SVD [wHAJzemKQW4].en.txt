Previously, we saw how the eigenvalues\h and eigenvectors of a matrix can be used\h\h to diagonalize the matrix, which means that\h we express the matrix as a product of simpler\h\h matrices with a given structure. This is called\h the eigendecomposition, and it’s only one type\h\h of a matrix decomposition. There are four more\h matrix decompositions that we should be aware of,\h\h so let’s take a look at them one at a time. The LU decomposition expresses a given matrix\h\h A as a product of a lower triangular matrix L and\h an upper triangular matrix U. These are matrices\h\h where their only entries lie below or above the\h leading diagonal respectively, and every other\h\h entry is zero, such as shown here. L will always\h be a square matrix with the same number of rows\h\h as A, and U will always be the same shape as A.\h Let’s see how to compute the LU decomposition,\h\h sometimes also known as the LU factorization,\h of this 3 by 4 matrix A. The first step is to\h\h use the Gaussian elimination to get all zeros\h below the diagonal of this matrix, which we\h\h do using row operations. Here, we can subtract\h twice row 1 from row 2, then add row 1 to row 3,\h\h and this will generate zeros in the first column\h below the diagonal. We can then subtract twice row\h\h 2 from row 3 to get another zero in the second\h column below the diagonal. And this will be\h\h our upper triangular matrix U. To find L, we just\h write the negative version of whatever coefficient\h\h we used in the row operations to produce each\h zero below the diagonal. The diagonal entries\h\h of L will always be ones, and the entries\h above the diagonal will always be zero, and\h\h so we can easily write down our LU decomposition. Let’s talk about what LU decomposition is good\h\h for. When faced with an equation of the form A x\h equals b, where x is an unknown vector and b is\h\h a known vector of constants, the obvious solution\h is to use x equals A inverse times b. But taking\h\h a matrix inverse can be very computationally\h intensive, and so in practice it is not done\h\h this way when A is large. Instead, we can\h first factorize A into L U and then write\h\h L U x equals b. We can then write U x equals\h c, where L c equals b. We can now treat this\h\h vector c as its own unknown vector, and because\h the matrix L is triangular, the system L c equals\h\h b is very easy to solve by just equating the rows\h and using back-substitution. No inverse matrices\h\h are required to find c. Once we’ve found c, we can\h apply the exact same procedure to find x from the\h\h system U x equals c, since U is also triangular. Next, let’s mention the Cholesky decomposition.\h\h This is a simplification of the LU decomposition\h that only applies when the matrix A is a Hermitian\h\h matrix, and if A is real then this means A must\h be symmetric. In this case, when we do an LU\h\h decomposition of a symmetric matrix, we find that\h L and U are just each other’s conjugate transpose,\h\h and so we only need to find one of them.\h Conventionally this is written A equals L L star\h\h in general, or A equals L L transpose for real\h matrices. The Cholesky decomposition is often used\h\h as a sort of ‘square root’ of a matrix, since the\h matrix L multiplied by itself transposed gives A.\h Next up is the QR decomposition. For a given\h matrix A, we can write A equals Q times R, where Q\h\h is a rotation matrix, meaning its columns are all\h orthonormal vectors, and R is an upper-triangular\h\h matrix. We can use the Gram-Schmidt process\h to find suitable column vectors for Q,\h\h as these will be a minimal set of orthonormal\h basis vectors that span the column space of A. The\h\h number of such vectors we can find is equal to the\h rank of the matrix A, and this will be the number\h\h of rows in our matrix Q. Check out my tutorials\h on matrix subspaces and the Gram-Schmidt process\h\h if these ideas sound unfamiliar. Once we’ve found\h Q, we can immediately compute R by taking the dot\h\h product between the columns of A and Q as shown\h here. For the i’th column of R, we take the i’th\h\h column of A and dot it with each column of Q\h to form the row entries down to the diagonal.\h Like the LU decomposition, the QR decomposition\h can be used to solve systems of equations of the\h\h form A x equals b, but the QR decomposition has\h another advantage, as QR can be used to solve\h\h overdetermined systems, meaning systems with\h more equations than unknown variables, in an\h\h algorithm called least squares. This occurs, for\h example, when we want to find a line of best fit\h\h to a dataset with lots of data points. There is no\h one line that goes through every single point, but\h\h what we can do is find the line that minimizes the\h sum of squared distances from each point. To solve\h\h an overdetermined system A x equals b, we write\h A equals Q R so that R x star equals c, where c\h\h is Q transpose times b. Note that because Q is\h orthonormal, its inverse is just its transpose,\h\h which again simplifies the computations. We can\h evaluate c and then solve the system R x star\h\h equals c for x star using back-substitution,\h as R is triangular. Here, x star represents\h\h the optimal solution for x that minimizes the\h squared Euclidean distance between A x and b.\h The last decomposition we will look at is\h called the singular value decomposition or\h\h SVD for short. The SVD can be thought of as a\h generalized version of diagonalization using\h\h eigenvectors and eigenvalues. Diagonalization\h only works when the matrix A is square and has\h\h a full set of independent eigenvectors for every\h eigenvalue. This is not always the case, and we\h\h might have a matrix A which is square, but has\h repeated eigenvalues, and it might not be possible\h\h to find enough corresponding eigenvectors to\h construct the eigenvector matrix. In these cases,\h\h we must turn to the singular value decomposition,\h which states that any matrix A can be written as\h\h Q 1 times capital Sigma times Q 2 transpose,\h where Q 1 and Q 2 are both rotation matrices,\h\h meaning their columns are orthonormal vectors, and\h capital Sigma is a diagonal matrix. The first step\h\h is to compute the matrices A times A transpose,\h and A transpose times A, and these will both be\h\h square matrices. First, we compute the eigenvalues\h of either one of these products, as their\h\h eigenvalues will be the same. We then need to take\h the square roots of these eigenvalues, which gives\h\h us what are known as the singular values of our\h matrix A, and list them on the leading diagonal\h\h in descending order to form the matrix capital\h Sigma. Capital Sigma must have the same shape\h\h as A, so fill in all the other entries with zeros.\h Then, Q 1 will have the normalized eigenvectors of\h\h A times A transpose as its columns, while Q 2 will\h have the normalized eigenvectors of A transpose\h\h times A as its columns, ensuring that the order\h of the columns corresponds to the singular values\h\h in capital Sigma. The singular value decomposition\h has some unexpected applications across statistics\h\h and computer science, because it neatly\h encodes matrix A’s spectral information,\h\h which is a fancy term for anything we can infer\h from the eigenvectors and eigenvalues. In fact,\h\h we can directly read off the spans of the\h four fundamental subspaces of A from its\h\h singular value decomposition. The column space\h and the row space of A is spanned by the first\h\h r columns of Q 1 and Q 2 respectively, where r\h is the rank of A, which itself is the number of\h\h non-zero singular values. The left nullspace and\h the nullspace of A will be the remaining columns\h\h of Q 1 and Q 2 respectively, and all of these will\h be orthonormal bases. Let’s check comprehension.
In the last tutorial, we saw how as a differential\h equation becomes more complicated, the harder it\h\h is to find a solution. But since differential\h equations are so essential to applied math,\h\h surely we can do better than trying a load\h of substitutions and hoping for the best?\h\h Indeed we can, because we have something\h that those dusty old mathematicians of\h\h the eighteenth century didn’t have: we\h have computers. In this tutorial, we’ll\h\h learn a way to approximate the solution to any\h differential equation, no matter how complicated.\h Let’s suppose we have any general\h first-order initial value problem,\h\h which can be written in the form dy/dx equals f\h of x y, with some initial y of zero, which we’ll\h\h call y sub zero. Knowing nothing at all about\h the function f, is there anything we can do?\h\h Recall that dy/dx really represents the slope of\h the tangent to the curve when we plot y against\h\h x at a given point. Since we are starting with a\h given initial condition, we know that our solution\h\h must pass through the point (zero, y sub zero),\h and we can calculate that the gradient of the\h\h tangent line to the solution at this point\h must be f of zero y sub zero, since this is\h\h equal to dy/dx in our differential equation.\h If we make the assumption that our solution\h\h is a well-behaved continuous function, then this\h tangent line will be a decent approximation to the\h\h solution for small steps away from this point.\h We can therefore take a tiny step to the right,\h\h call it h, and using the gradient equals rise\h over run formula, the resulting change in y\h\h on the tangent line will be f of zero y sub\h zero times h. The new y coordinate is then\h\h y sub zero plus h times f of zero y sub zero,\h and we can use this as our new starting point\h\h and repeat the process, drawing a new tangent\h line, extrapolating the change in y and adding\h\h to our current approximation. In general, we can\h write down what is known as Euler’s method: x\h\h sub n plus 1 equals x sub n plus our step size h,\h and y sub n plus 1 equals y sub n plus h times f\h\h of x sub n, y sub n. This allows us to compute\h a whole sequence of y values, starting from our\h\h initial condition, and progressively approximating\h further and further into our solution, as long\h\h as our step size h is small enough to not stray\h too far from whatever the true solution may be.\h What if we weren’t given an initial condition?\h In that case, we wouldn’t have a fixed starting\h\h point, but we can still make some progress. We\h can represent the set of all possible starting\h\h tangent lines using a tangent field, sometimes\h known as a direction field. In this example,\h\h we have the differential equation dy/dx equals\h x squared minus x minus 2, so our function f of\h\h x y is x squared minus x minus 2. If we were\h given an initial condition for some point (x,\h\h y), we could plug it into f of x y to draw our\h initial tangent line, but since we don’t have one,\h\h we need to sub in points all across\h the x y plane. For each point, we can\h\h draw the tiny tangent lines with the appropriate\h gradient, which will populate the tangent field,\h\h shown as the collection of green line segments on\h this graph. We can consider this a vector field,\h\h and the solutions to our DE are the field lines\h of this tangent vector field, shown by the\h\h colored curves on the graph. Each possible curve\h represents the solution with a different initial\h\h condition, so this is a way to visualise the\h family of curves making up the general solution.\h\h A way to help speed up the sketching of tangent\h fields is to first find the curves where dy/dx is\h\h a constant, which are the contour lines of f, or\h also called the isoclines of the tangent field.\h\h In our case, since our function f of x y does\h not depend on y at all, the isoclines are all\h\h vertical lines. For example, notice that along\h the line x = 2, all of the tangent lines have\h\h the same gradient. We can therefore see that all\h of the solution curves will simply be vertical\h\h translations of each other, which correspond\h to choosing different initial conditions.\h One issue with Euler’s method is that you need\h an extremely tiny step size h in order to get\h\h accurate results, which makes computations\h slow and susceptible to rounding errors. This\h\h motivated the development of better numerical\h methods, so let’s take a look at some of those\h\h now. An algorithm known as Heun’s method, which\h is also simply called the improved Euler method,\h\h uses a two-step, predictor-corrector approach\h to compensate for the change in gradient as\h\h we step from one point to the next. In\h Heun’s method, the first step is the same,\h\h but then instead of using the old gradient, we\h replace it with the average of the gradients at\h\h the old point and the new point, giving a much\h closer approximation. An even more sophisticated\h\h algorithm is the Runge-Kutta fourth order method,\h or RK4, which uses a point positioned between the\h\h start and end point to sample another value of\h the gradient, and calculate the best estimate\h\h for where the solution is going. RK4\h uses the formulae shown here, and it’s\h\h actually the standard workhorse of most modern\h computational solvers for differential equations.\h So those are some good methods to solve any\h first-order differential equation, but what\h\h about second-order and even higher? We can use a\h trick we learned in the last tutorial for reducing\h\h the order of difficult differential equations.\h If we have a 2nd order DE in the general form\h\h x double prime equals some function of the\h independent variable t, the dependent variable\h\h x and its first derivative x prime, then we can\h create a new variable, call it v, defined as v\h\h equals dx/dt, and this will lead to the formation\h of a system of two differential equations in two\h\h dependent variables, x and v. It might seem like\h we’ve made the problem harder, but note that these\h\h are both first-order, and so we can now apply the\h numerical methods studied here to each equation,\h\h stepping through both variables at each point in\h time, approximating the solution as we go. For\h\h example, to apply Euler’s method to this system,\h we calculate the estimates for both v and x at\h\h each time step. For more complicated numerical\h methods like Heun’s method, we need to be a\h\h little more careful, since the calculation steps\h have interdependencies, so we need to evaluate\h\h each of the predictor and corrector sub-steps\h fully for each variable. Our calculations must\h\h therefore alternate between finding estimates\h for x and v, starting with x hat and v hat,\h\h and then the final estimates for x and v. Nowadays, scientific computing capabilities\h\h are at the fingertips of anyone who owns a\h laptop. With a little knowledge of coding,\h\h especially in a high-level programming\h language such as Python or Matlab,\h\h we can write programs to implement these numerical\h algorithms automatically, solving even the most\h\h difficult of differential equations in the blink\h of an eye. There is a lot more that could be said\h\h about numerical methods, but that will be all\h for this tutorial, so let’s check comprehension.
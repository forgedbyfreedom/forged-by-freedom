we will now review a problem on error correction and detection in order to better understand how the selected encoding of a message can help both detect and correct transmission errors in this problem messages consist of nine data bits and seven parity bits each dij represents a data bit which belongs to row I and column J the pIJ bits are used to make the parity of each row and column B odd the pxx box which is in the bottom right corner is used to make the parity of the entire message odd in other words it makes a total number of ones be odd the first question we want to ask ourselves is what is the minimum Hamming distance between valid code words using this encoding in order to figure that out we need to think about how many bits of the encoding are changed if I want to change one of the data bits the easiest way to see this is through an example suppose that I want to change D zero one from a zero to a one the first thing changing is my data bit itself then p0x would get flipped because I just added another one to my row which means that I need to flips a parity bit in order for the total number of ones in my row to remain odd the next change is that px1 needs to get flipped in order to maintain an odd parity in my column so far three entries have changed which implies that the parity of the entire message flipped in order to keep the parity of the entire message odd pxx needs to be flipped as well so that there are a total of four entries being flipped which will cause the odd parity of the entire message to remain unchanged this means that our minimum Hamming distance for this encoding equals four because anytime we flip a data bit a total of four entries need to change to maintain our encoding we know that to detect an e bit error the hamming distance has to be greater than e or in other words the hamming distance has to be greater than or equal to e plus one so to detect a one bit error one needs a Hamming distance greater than or equal to two we also know that two correct and a bit error the Hamming distance has to be greater than two e or the Hamming distance has to be greater than or equal to 2 e plus one so it's a correct a one bit error one needs to have a Hamming distance greater than or equal to three for this encoding since our hamming distance equals four that means we should be able to both detect and correct one bit errors taking a look at this message if we check the parity of each row column and full message we see that in all cases our parity is odd since odd parity indicates a valid message there are no errors in this message looking at this message we see that all the row parodies are odd but the parity of column one is even this means that there is an error in column one however since there were no errors in the parity of the rows this means that the bit in error is the parity bit itself if we flip px one from a zero to a one we now have a valid message again looking at the parody of each row and column in this message we see that row zero has an even parity and column zero has an even parity this means that the database colum 0 is wrong if we flip this bit we now see that all of our row and column parodies are correct and our total message parity is correct as well so we have restored our message by identifying the bit and error in this example all of the row parodies and column parodies are odd however the parity of the entire message is even this means that the bit and error in this message is the pxx bit itself flipping that bottom-right bit results in a valid message once again now let's go back to our valid message and see if we can correct two-bit errors suppose we flip D 1 1 to 0 and D 2 to 2 1 to create a 2 bit error now when we look at the parity of each row we find that both Row 1 and 2 have a parity error similarly columns 1 and 2 have a parity error the total message parity remains correct given this information we know that an error occurred in the transmission of this message but we cannot identify exactly which bits are an error because there is more than one possible way to alter two bits and arrive at a valid message this demonstrates that we can detect a two bit error but we can't correct a two bit error going back to our original claims about having distances this is in line with our expectations if e equals 2 meaning that we want to detect a 2 bit error then the Hamming distance must be greater than or equal to e plus 1 which equals 3 to correct the 2 bit error the Hamming distance must be greater than or equal to 2 e plus 1 which equals 5 since our Hamming distance in this problem is 4 we can only detect two bit errors but not correct them
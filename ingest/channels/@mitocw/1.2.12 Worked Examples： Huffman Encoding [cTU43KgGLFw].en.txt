this problem presents Huffman encoding which produces variable length encoding x' based on the probabilities of the occurrence of each different choice more likely choices will end up with shorter encodings than less likely choices the goal of this problem is to produce a Huffman code to encode student choices of majors there are a total of four majors and each has a probability associated with it to produce a Huffman encoding one begins with the two choices with lowest probability in this example major 6/7 has a probability of 0.06 and major six one has a probability of 0.09 since these are the two lowest probabilities these two choices are selected as a starting point for building are encoding tree the root node that combines the two choices has a probability equal to the sum of the leaf nodes or 0.15 in this example we then label one side of this tree with a zero and the other with a 1 the next step is to find the next two smallest probabilities out of the remaining set of probabilities where majors 6 1 & 6 7 have been replaced by node a which has probability 0.15 in this case our lowest probabilities are 0.15 which is the probability of node a and 0.4 1 which is the probability of major 6 3 so we create a new node B that merges nodes a and 6 3 this new node has probability zero point 5 6 again we label the two branches one with a 0 and the other with a 1 we now repeat this process one last time with the only two remaining choices which are now note B and major 6 2 this means that we should make a new node C that merges node B and major 6 to note that the probability of this node is 1.0 because we have reached the top of our tree our final step is to label these last two branches now that all the branches are labeled we can traverse the tree from the root node to each leaf node in order to identify the encoding that has been assigned to the major associated with that leaf node we find that four major six one the encoding is one zero one four major six two we end up with a one bit encoding of zero next we traverse the tree to identify the last two encoding x' and find that four major six three the encoding one one has been assigned and four major six seven the encoding one zero zero has been assigned these encoding x' make sense because we expect the major with the highest probability in this case major six to two end up with a shortest encoding the next highest probability major is six three so it ends up with a second shortest encoding and so on we just saw that the encoding is resulting from this Huffman encoding tree our one zero one four major six one zero four major six two one one four major six three and one zero zero four major six seven note that the Huffman encoding tree for this problem could have also been drawn like this these two trees are identical in structure and result in the same encoding for the four majors furthermore a Huffman tree can result in more than one valid encoding the only constraint and labeling the edges is that from each node there is both a zero branch and a one branch but there's no constraint about which side has to be labeled zero in which side one so for example we could have chosen to label the left side of the B node with a 1 and the right side with a zero instead of the way we originally labeled them note however that this would result in a different but also valid Huffman encoding in this case the encoding for major 6 1 is 1 1 1 major 6 2 remains 0 major 6 3 becomes ones and major six 7 becomes 1 1 0 as long as one maintains consistency across a selected tree a valid Huffman encoding is produced we now add one more column to our table which gives P times log base 2 of 1 over P for each of the majors using this information we can calculate the entropy which is the average amount of information contained in each message this is calculated by taking the sum of P times log base 2 of 1 over P for all choices of majors for this problem the entropy is 1.6 we can now also calculate the average bits per major of the encoding z' that we have identified this is calculated by multiplying the number of bits and each encoding times the probability of that major recall that our encoding for major 6 1 was 1 1 1 for majors 6 2 it was 0 from major 6 3 it was 1 0 and finally for major six 7 it was 1 1 0 this means that the average bits per major is 3 times zero point zero nine plus one times zero point four four plus 2 times zero point four 1 plus three times zero point zero 6 which equals point two 7 plus point four 4 plus point eight 2 plus point one 8 which equals one point seven one note that this is slightly larger than the entropy which is one point six this occurs because while Huffman encoding is an efficient encoding which gets us most of the way there there are still some inefficiencies present in Huffman encoding because only encoding one major at a time rather than also considering the probabilities associated with seeing a particular sequence of majors in a message that conveys the major selected for a large number of students
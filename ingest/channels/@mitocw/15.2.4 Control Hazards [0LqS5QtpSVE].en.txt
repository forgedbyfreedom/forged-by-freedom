now let's turn our attention to control hazards illustrated by the code fragment shown here which instruction should be executed after the B&E if the value in r3 is nonzero and it C should be executed if the value in r3 is 0 the next instruction should be sub if the current instruction is an explicit transfer of control in other words jumps or branches the choice of the next instruction depends on the execution of the current instruction what are the implications of this dependency on our execution pipeline how does the unpiloted determine the next instruction for branches BEQ or B&E the value to be loaded into the program counter depends on 1 the opcode in other words whether the instruction is a BEQ or be any to the current value of the program counter since that's used in the offset calculation and 3 the values stored in the register specified by the RA field of the instruction since that's the value tested by the branch for jump instructions the next value of the program counter depends once again on the opcode field and the value of the RA register for all other instructions the next PC value depends only on the opcode of the instruction and the value PC plus 4 exceptions also change the program counter we'll deal with them later in the lecture the control hazard is triggered by jump and branches since their execution depends on the value in the RA register in other words they need to read from the register file which happens in the RF stage our bypass mechanisms ensure that we'll use the correct value for the RA register even if it's not yet written into the register file what we're concerned about here is that the address of the instruction following the jumper branch will be loaded into the program counter at the end of the cycle when the jump or branch is in the RF stage but what should the I have stage be doing while all this is going on in the RF stage the answer is that in the case of jumps taken branches we don't know what the i/o state should be doing until those instructions are able to access the value of the RA register in the RF stage one solution is to stall the if' stage until the RF stage can compute the necessary result this was the first of our general strategies for dealing with hazards how would this work if the opcode and the RF stage is jump the EQ or be any stall the if' stage for one cycle in the example code shown here assume that the value in r3 is non zero when the BNE is executed in other words that the instruction following the B&E should be the ad c-- at the top of the loop the pipeline diagram shows the effect we're trying to achieve a no op is inserted into the pipeline and cycles four and eight then an execution resumes in the next cycle after the RF stage determines what instruction comes next note by the way that we're relying on our bypass logic to deliver the correct value for r3 from the mem stage since the adds the instruction that wrote into r3 is still in the pipeline in other words we have a data hazard to deal with - looking at say the right back stage in the pipeline diagram we see that it takes four cycles to execute one iteration of our three instruction loop so the effective CPI is 4/3 an increase of 33 percent using stall to deal with control hazards has had an impact on the instruction throughput of our execution pipeline we've already seen the logic needed to introduce no ops into the pipeline in this case we add a MUX to the instruction path in the if' stage controlled by the IR source if' signal we use the superscript on the control signals to indicate which pipeline state holds the logic if they control if the opcode in the RF stage has jumped BEQ or B&E we set IR source if' to one which causes a no op to replace the instruction that was being read from main memory and of course we'll be setting the PC roll signals to select the correct next PC value so the i/o stage will fetch the desired follow-on instruction in the next cycle if we replace an instruction with a no op we say we annulled the instruction the branch instructions in the beta is a make their branch decision in the RF stage since they only need the value in register RA but suppose the ISA had a branch where the branch decision was made in the ALU stage when the branch decision is made in the ALU stage we need to introduce to no ops into the pipeline replacing the now unwanted instructions in the RF and I of stages this would increase the effective CPI even further but the trade-off is that the more complex branches may reduce the number of instructions in the program if we annul instructions in all the earlier pipeline stages this is called flushing the pipeline since flushing the pipeline has a big impact on the effective CPI we do it when it's the only way to ensure the correct behavior of the execution pipeline we can be smarter about when we choose to flush the pipeline when executing branches the branch is not-taken it turns out that the pipeline has been doing the right thing by fetching the instruction following the branch starting execution of an instruction even when we're unsure whether we really want it executed is called speculation speculation is okay if we're able to annul the instruction before it has an effect on the CPU State for example by writing into the register file or main memory since these state changes called side-effects happen in the later pipeline stages an instruction can progress through the if' RF and ALU stages before we have to make a final decision about whether it should be annulled how does speculation help with control hazards guessing that the next value of the program counter is PC plus 4 is correct for all but jumps and taken branches here's our example again but this time let's assume that the BND is not taken in other words that the value in three is zero the sub instruction enters the pipeline at the start of cycle four at the end of cycle four we know whether or not doin all the sub if the branch is not-taken we want to execute the sub instruction so we just let it continue down the pipeline in other words instead of always annulling the instruction following the branch we only annul it if the branch was taken if the branch is the pipeline is speculated correctly and no instructions need to be annulled however if the BND is taken the sub is annulled at the end of cycle four and a no op is executed in cycle five so we only introduce a bubble in the pipeline when there's a taken branch fewer bubbles will decrease the impact of annulment on the effective CPI we'll be using the same data path circuitry as before we'll just be a bit more clever about when the value of the IRS or SIF control signal is set to one instead of setting it to one for all branches we only set it to one when the branch is taken our naive strategy of always speculating that the next instruction comes from PC plus four is wrong for jumps and taken branches looking at simulated execution traces will see that this error in speculation leads to about ten percent higher effective CPI can we do better this is an important question for CPUs with deep pipelines for example Intel's Nehalem processors from 2009 resolves the more complex x86 branch instructions quite late in the pipeline since Nehalem is capable of executing multiple instruction C cycle flushing the pipeline and Nehalem actually involves the execution of many instructions resulting in a considerable hit on the CPI like many modern processor implementations Nehalem has a much more sophisticated speculation mechanism rather than always guessing the next instruction is at PC plus 4 it only does that for non branch instructions for branches it predicts the behavior of each individual branch based on what the branch did last time it was executed and some knowledge of how the branch is being used for example backwards branches at the end of loops which are taken for all but the final iteration of the loop can be identified by their negative branch offset values Nehalem can even determine if there's a correlation between branch instructions using the results of an another earlier branch to speculate on the branch decision of the current branch with these sophisticated strategies Nehalem speculation is correct 95 to 99 percent of the time greatly reducing the impact of branches on the effective CPI there's also the lazy option of changing the is a to deal with control hazards for example we could change the is a to specify that the instruction following a jumper branch is always executed in other words the transfer of control happens after the next instruction this change ensures that the guess of PC plus 4 as the address of the next instruction is always correct in the example shown here assuming we change the is a we can reorganize the execution order of the loop to place the mall instruction after the be any instruction in the so called branch delay slot since the instruction in the branch delay slot is always executed the mall instruction will be executed during each iteration of the loop the resulting execution is shown in this pipeline diagram assuming we can find an appropriate instruction to place in the delay slot the branch will have zero impact on the effect of CPI our branch delay slots a good idea seems like they reduce the negative impact that branches might have on instruction throughput the downside is that only half of the time can we find instructions to move to the branch delay slot the other half of the time we have to fill it with an explicit no op instruction increasing the size of the code and if we make the branch decision later in the pipeline there are more two branch delay slots which would be even harder to fill in practice it turns out that branch prediction works better than delay slots in reducing the impact of branches so once again we see it's problematic to alter the is a to improve the throughput of pipeline execution ISAs outlive implementations so it's best not to change the execution semantics to deal with performance issues created by a particular implementation
the following content is provided under a Creative Commons license your support will help MIT open courseware continue to offer highquality educational resources for free to make a donation or view additional materials from hundreds of MIT courses visit MIT opencare at ocw.mit.edu all right today we start a new section of data structures this is going to be two lectures long so this week of succinct data structures where the goal is to get really small space and uh first thing to do is to Define what small means um most succinct data structures are also static although there are few that are Dynamic we'll be focusing here on static uh data structures and so in general the name of the game is taking a data structure that you're familiar with we're going to talk about essentially two today one is binary tries which has the killer application of doing binary suffix trees in particular so it could be compressed try whatever but we'll assume here the alphabet is binary a lot of this has been generalized to larger alphabets I'll tell you a little bit about that but to keep it simple in lecture I'm going to stick to Binary alphabet and another data structure we're going to look at is a uh bit Vector so n Bits in a row and you want want to do interesting operations uh like find the I one bit in constant time these are things that are easy to do in linear space uh where linear space means order and words like if you want to implement a try you have a pointer at every node two pointers at every node that's easy uh bit vectors you could store an array of all where all the ones are and so uh it's easy to do linear space but linear space is not optimal and there are three senses of small space that we'll be going for one is the best version is implicit an implicit data structure means you use the very optimum number of bits plus a constant but here we're we're focusing on bits not words and if you translate a linear word data structure order NW is order n * W bits and to store something like an nit Vector you should really only use order n Bits not order n w bits okay uh now what would be ideal is if you use n Bits plus a constant the plus a constant is essentially because sometimes the optimum number is not an integer and it's hard to store non- integer number of bits so you add a constant yeah I mean I wouldn't mind too much if you added order log n Bits or something but typically the goal here is constant sometimes you can really get zero but uh the next best thing would be a succinct data structure uh where the goal is to get opt plus little o of opt bits so the key here is to get a constant factor of one in front and then the the worst thing in this regime is still pretty good it's order opt bits so for example if you're storing an nbit Vector if you use order n Bits that's still better than order and W bits usually uh compact is a savings of at least a factor of w over what you'd normally call a linear space data structure so this is the big savings Factor W but there's a constant here sometimes you like to get rid of that constant make it one uh and it's not so bad if you have say another I don't know square root of n or something little Lo and uh bits of extra space um but of course the ideal would be to have no extra space and that's implicit now it's a little confusing I usually call this area succinct data structures to mean all three but the middle one is called succinct um that's typically the goal is to go for succinct because implicit is very hard and compact is kind of like a warm-up towards cinct so this is the usual goal in the middle uh and we're going to do this for binary tries and rank and select so let me tell you a little bit about what's known oh uh sorry one quick example you've seen implicit data structures you may have even heard this term in the context of uh heaps binary heaps are an example of a dynamic implicit data structure they achieve this bound um they have no extra space in the appropriate model um and another one is would be a sorted array sort array supports binary search you can't really update it so it's a static search structure it achieves the optimal number of bits you're just storing the data and usually implicit data structures just store the data though sometimes they reorder the data in interesting ways like sorting a sorted array is one way to reorder your data items so here's a short survey this is definitely not exhaustive but it uh covers a bunch of the main results so one place where this this area really got started well there are a few places actually uh my academic father uh my one of my PhD advisers Ian Monroe is sort of the one of the fathers of this field and he started looking at succinct data structures uh at the very early days and one of the problems he worked on uh was Dynamic search trees so if you want to do static search trees you can just store the items in a sorted array easy login search no extra space what if you want to do inserts and deletes also in login time per operation just like a regular search tree but you want to do it implicitly now this is tricky and uh there's an old result that let you get log squared per update and query which essentially encoded you can't for pointers at all here but the idea was to encode the pointers by permuting uh enough items if you have take say Logan items then the permutations among them is roughly log in log log in bits and so you could encode bits by just permuting pairs of items and so you could read a pointer in like log and operations you end up with log squar uh and then Ian Monroe got down to little bit less than log squ and then the then there's a series of Improv M ments over the last several years the final result is log n worst case insert delete and predecessor and this is by Fran chesini and Grossi and furthermore it's cach oblivious you can get log based B of n uh cashious so this has been pretty much completely solved implicitly you can do all the good things we know how to do with search trees now this is not trying to solve the predecessor problem using vom deos and such tricks that's I believe open but for a basic login performance it's solved okay before this got solved another important problem is essentially the the equivalent of hashing so you want to succinct dictionary you want to be able to do uh now this is going to be static so there's no insert and delete it's just is this item in the dictionary so I have a universal size U I have n items in the dictionary so first question is what is the optimal number of bits and this is actually usually very easy to compute you just take what are the possible the set of possible structures you're trying to represent which is n items out of a universe of size U how many different ways are there to have n items in a univers of size U come on it's easy combinatorics somebody log you sorry that's how many bits you'll need yeah log you choose n is the number of bits you need the number of different possibilities just you choose chose n you take log base 2 of that that's how many bits you'll need to represent this now this is not necessarily an integer uh cuz it's got a log that's why we would have uh plus order one if we doing implicit it's not known how to do implicit it's known how to do succinct so this is going to be plus little o of that thing and uh actually I have the explicit bound eraser I don't know how exciting this is but you get to know how little low it is log log N squared over log n so this is slightly smaller so this is roughly something like uh U log n depends if if n is small this is like U log n if n is Big this is like uh sorry nlog U I should say you can always encode a dictionary using nlog U bits just for every item you specify the log U bits but when n is Big close to U then you can just use a bit vector and use U bits um and uh so this is little o of n so it's always smaller than whatever you're encoding over here it's only slightly smaller than n log log n squ over log n but it's little low of one and that's I believe the best known uh so this is uh Broadneck and Monroe and then improved by poac and uh the point is you get constant time uh membership query in general the name of the game is you want to do the queries you're used to doing in something like a dictionary same amount of time but with less space okay next one is uh maybe I'll go over here binary try which is what we're going to work on today this is there's various results on this but uh sort one of the main ones is by Monroe and ramen again now this is a little harder of a question how many binary tries on N nodes are there uh the answer I will tell you is the nth catalon number which is a quantity we've seen before to n choose n over n + 1 and as we mentioned last time this is roughly 4 to the N this kind of interesting we saw this we saw the catalon number in a different context which is we were doing in Direction and using lookup tables at the bottom on all rooted trees on N nodes number of rooted trees on end nodes was also Catalan number this is a different concept binary tries if you've ever taken a combinator class you see Catalan numbers all over the place there's a zillion different things all all of them the number of them is Catalan number and we will actually use that equivalence between binary tries and rooted trees at the end of today's lecture so we'll see why they're the same number okay so we take log of that uh that's 2 n Bits so you need uh 2 n Bits to represent a binary try and indeed you can achieve 2 N plus little o of n Bits so that's a ccin data structure and our goal will be to be able to do constant time traversal of the tree so left child right child parent and for fun another operation we might want to do is compute the size of the current sub tree again think of a suffix tree you're at you start at the root and you want to be able to go along the left child every time you have a zero bit in your uh query string you want to go to the right child every time you have a one bit uh parent we don't really need but why not and then sub tree size would tell us how many matches are there below us you could count either the number of nodes below you or the number of leaves below you it's roughly the same for a compact try and so this lets you do uh substring searches at we'll actually talk more about that next lecture how to actually do a full suffix tree but this is a component of a full of a binary suffix tree that has the same performance but uses optimal amount of space and this is a big motivator originally for doing uh doing compact or succinct data structures uh at University of waterl they were doing this new OED project where Oxford English Dictionary was trying to go online or digital and this concept of having a CD ROM that could have an entire dictionary on it was crazy uh and CD ROMs were really slow so you don't want to just scan the entire CD to do a search you want to be able to do a search for arbitrary substrings that's what suffix trees let you do but you really can't afford much space and so this was that was the motivation for developing these these data structures back in the day uh things are a lot easier now space is cheaper but still there's always going to be some giant thing that you need to store uh that if you want to store a data structure you really can't afford much space cool so that's uh static uh there is a dynamic version this a more recent result from just a few years ago you can do constant time insert and delete of a leaf and you can do uh subdivision of an edge uh these are operations we saw for dynamic LC a so same operations as Dynamic LCA you can do these in constant time and still maintain uh this disin binary tree Tri representation where you can do all these traversal operations so we won't cover that we will cover the static version today um then that's for binary alphabet let me tell you what's known about larger alphabet this a problem I worked on a while ago Although our result is no longer the best uh so here it's a little complicated but the number of tries number of Kary tries so K is now the size of the alphabet is k n + 1 choose n over KNN + one and so it's succinct meaning you we can achieve log of that plus little o of that uh bits and the queries we can achieve are constant time child with label I and parent and sub tree size so we can do all the things we're able to do before the the analog of left child and right child now is I have a character in my pattern p and I want to know which child has that label so it's not the same as finding the E child of a node it's uh the Ed the edges are labeled by their by their letter in the alphabet and you can achieve that um we had an earlier result that achieved like log log or something and then finally was brought down to constant by uh faran and Monroe so couple more why don't I just mention what they are it's getting a little tedious a lot of other things you might want to store uh you can store succinct permutations although there's some open problems here so you want to store a permutation using log n factorial bits plus a little low of n uh if you want to a chief the synct the best known uh oh and the interesting query is you want to be able to do the Ki power of your permutation so see where an item goes after K steps best query known for that is log n over log log n if you want succinct if you only want compact then it's known how to do constant time so an interesting open question is can you achieve succinct constant time queries you if you relax either then it's known how to do it uh there's a generalization of this to functions where one of those results is known the other one isn't uh you can try to do aan groups there finite aan groups there aren't too many different ones and you can represent an entire aelan group on N items using log bits which is pretty crazy order lend bits and you can represent an item in that group in lend bits and do multiplication inverse and equality testing uh there's other results on graphs which I won't get into those are a little harder to state uh and then another interesting case is integers uh so you want to store an integer and you want to be able to increment and decrement the integer and you want to do as few bit operations as possible worst case um for example if you have lots of ones in your bit string you do an increment you don't want to uh pay linear cost linear number of bit updates to do it uh and so you can achieve implicit so just a constant number of extra bits of space uh if I have an N bit integer then I can do it an increment or a decrement in order log and bit reads and constant bit wres and this is ramen and rro from a couple years ago uh so this is pretty good of course ideaal would be to do a constant number of well constant number of bit reads or writes would be optimal I guess um I personally I'd be interested in getting a constant number of word reads and writes uh so but that's an open problem I believe so these there's only order and bit reads if they were all consecutive that would be constant number of word reads but they're kind of spread out be nice to get constant number of word operations so that's a quick survey of what's known let's do some actual data structures now so we're going to be focusing on this uh succinct binary tries and we're going to do two versions of it one of them is level order and the other we'll use a balance parenthesis representation so let's start with the level order one this is very easy so I'm going to just Loop over the nodes in my try in level order so level by level and write one bit to say whether there's a left child and one bit to say whether there's a right child let's do a little example so here is a binary try and I'm going to write bit string for it so first I'm going to look at the top level I have the node a has a left child of B right child of C so I write one one and this corresponds to B this correspon to c okay uh then next level is B it has no no left child and it has a right child which is D I'm just writing down the label so I don't get lost uh then we have node c um which has a left child and a right child E and F then we have node D which has no left child has a right child then we have uh which is G node e has no children node F has no children node G has no children okay so there is a 2end bit string this is obviously 2 n Bits for nend nodes so this is one way to prove there's at most four to the N tries and uh well we'll talk about how useful it is I want to give you another representation of the same thing which is uh we take if we take these nodes and add on wherever there's an absent Leaf I'm going to add what we call an external node as you sometimes see in data structures books one way to represent a null pointer say oh there's a node there that has no children this unifies things a little bit because now every node either has two children or no children another way to think about the same thing and it turns out if you look at this bit string and add a one in front so I put this one in parenthesis then uh what this is in coding is just for every node in level order are you a real node or are you an external node are you an internal node or an external node right a here is internal B is internal C is internal this is an external node uh then this is internal internal internal then external G external external external external external external right the zeros correspond to external nodes because those are absent children so same thing so I'll write equivalently 1 equals an internal node and equals an external node uh of course to do this we need one more bit uh and we're I'm going to take this view primarily because it's a little easier to work with doesn't really make much of a difference just shft everything over by one and I'd like to to write down the indices here so we have 1 2 3 4 5 6 7 into this array because now our challenge is all right great we've represented a binary try but we want to be able to do constant time left child right child parent we're not going to be able to do SUB tree size level order is really not good for subtree size but left child right child and parent we can do in constant time and the reason we can do it in constant time is because there's a nice Lemma kind of analogous to uh binary heaps so uh the claim is if we look at the I internal node so for example uh C is the third internal node so internal node number three then we look at positions 2 * 3 and 2 * 3 + 1 so 6 and seven in this array and we get the two children E and F so that works six and seven are E and F uh or this one would be the fourth internal node D is a fourth internal node um and so at positions 8 and N should be this external node and g eight is an external node n is G so it works this the Lemma you can prove it pretty easily by induction on I uh so the idea is well if you look at let's say the I internal node and the IUS first internal node this one has has two left children don't know whether they're internal or external between them they're on the same level and we we're in level order so anything in between here is an external node so they have no children which means if you look at the children of I they're going to appear right after the children of IUS one um because we're level order so we have these two guys the next children the next nodes at this level are going to be these two guys so this one appeared at uh 2 i - 2 and 2 i - 1 this one will appear at 2 I and 2 I + 1 this is if I and IUS one are on the same level but it also works if they're in different levels so IUS one is the last node on its level again it's going to have two children here which will be the last nodes on this level and then right after that will come the children of I again at position 2 I and 2 I + one okay so that's essentially the proof that this works out there lots of ways to see why this is true but I think I'll leave it at that okay uh so this is good news it says if we have the I internal node we can find the left and the right children but these are in different name spaces right on the one hand we're counting by internal nodes on the other hand we're counting by position in the array which is counting position by internal and external nodes this counts both zeros and ones this only counts ones so we need a mechanism for translating between those two worlds between translating uh between indices that only count ones and indices that only that count zeros and ones and this is the idea of rank and select so in general if I have a a string of n Bits I want to be able to compute the rank of a bit which is the number of ones at or before position I so I'm given a position like six and I want to know how many ones are there up to six which would be five in the full full array here or I'm given a query like eight number of ones is six up to position eight and then the inverse of rank is Select which gives you the position of the jth one bit so this lets you translate between these two worlds of counting just the ones is Rank and uh or going to the J one bit that's select so this lets you compute the left child as just being at position twice the rank because the rank tells you uh this value I which is which internal node are you that's your rank you multiply by two and that was the position of the left child right child is going to be that + one parent is going to use select so if we want the parent of I this is going to be select of I over two with a floor so that's just the inverse of left and right child if I divide by two and take the floor I get rid of that plus one I get the rank and then I do select sub one and that gives that's the inverse of rank okay so that lets me Implement if I have Rank and select in constant time now I can do left child right child parent in constant time the remaining challenge is how do I do Rank and select and that's what we're going to do next any questions about that before we go on all right so now we do real data structures this is going to be some integer data structures some fun stuff it's going to use some techniques we know but in in a different setting because now our goal is to really minimize space I'm going to use IND Direction and look up tables in a new kind of way these are going to be word Ram data structures I want to do both Rank and select in constant time and L and the amount of space I have is little o of N I want to synct I'm going to store the bit Vector so then in addition to the bit Vector all I'm allowed for Rank and select is little low of end space that's the cool part so rank is one of the first succinct data structures is by Jacobson 1989 so uh first observation is what can we do with the lookup table suppose I wanted to store all the answers but I can't afford uh much space well uh if I had let's do a sort of a worksheet if I had space X if or if I looked at all bit strings of length x and then I wanted to store them that's going to cost or store uh a lookup table for each of them it's going to cost well there's two to the X different bit strings of that length then for each of them I have to store all possible answers to rank and select queries so there's Order X different queries you could query every bit every bit and then for each of them I have to write down an answer so this is going to be log X bits because the answer is a value between 0 and x - one so it takes log X bits to write it down so this is how much space it's going to be to store the answer for all bit strings of length x so what should I set X2 I'd like this to be a little low of n so anything that's a little bit less than log n is going to be okay and in particular half log n is good enough choice if we use half log n Bits this is going to be root n uh log n log log n which is little o of n quite small as the sync data structures go we're going to use more space than root n the point is if we could get down to bit strings of logarithmic size we'd be done but we have a bit string of linear size so how do we reduce it something like indirect ction so here's our the funny thing here is we're going to do IND Direction twice kind of recursively but stopping after two levels the first level of IND direction is uh going to reduce things down to size log squared so we're going to take our n bit string divide into chunks of size log squ n so there's n over log squ n chunks they look something like this log s n and the idea is right now we're trying to just do rank okay so rank number of ones at or before a given position so what I'm going to do is at each of these vertical bars I'm going to store the cumulative rank so far okay why log squ basically because this is what I can afford to store that cumulative rank is uh login bits could be I mean this rank is going to get very big by the end it'll have most of the one bits so it could be potentially linear so I'm going to need log n Bits to write that down uh but how many of these vertical bars are there well only n over log s n of them so I have n over log 2 N Things I need to write down each of them is log n Bits so some cancellation this cancels with that we have n over log n Bits overall which is slightly little of N and that's what we're that's the bound we're going to achieve okay of course now we have to solve the problem within a chunk but we've at least reduced to something aside log squar unfortunately we need something as size half log n before we can use lookup table so there's a bit of a gap here so we're going to use IND Direction a second time this time we can go all the way to half log n so I use red vertical bars to denote the sub chunks each of these is half log n overall size of a chunk here is log squ n so every one of these chunks gets further divided now how could this help why didn't I just subdivide into chunks of size half log n before I mean why I couldn't do it is clear if I did n over log n of them each of them stores log n Bits I'd have an over I'd have linear number of bits I can't afford linear number of bits that would only be compact not succinct how does it help me to First reduce to this and then reduce to this well what I want to do at each of these uh red vertical bars is store the cumulative rank but not the overall cumulative rank I only need the cumulative rank within the overall chunk not relative to the entire array okay why does that help me need less bits these ranks can't get too big because the overall size of a chunk is just log squared log of log squ is log log n so I only need log log n Bits to write down those cumulative ranks and so total size here is going to be n over log n Times log log n Bits because there's n over log n of these red vertical bars each one I only need to write log log n Bits And this is slightly little of n it's actually a little bit bigger than this but still little o so we can still afford this and now we're we're done because these sub chunks are of size half log n so I can use this lookup table and solve my problem so uh I mean step four is just putting everything together to compute the rank of a query first thing you do is figure out which chunk you fall into which you can do by division integer division you these things are stored in an array so you just compute what is that cumulative rank so you take the rank of that chunk you add on the rank of the sub chunk within the chunk and then you add on the rank of the element in the sub chunk so rank of the chunk is stored in the array known as two the rank of the sub chunk within the chunk is stored in the ARR in the array known as three and then to compute the rank of the element in the sub chunk you use the lookup table which is essentially telling you for every possible sub chunk what the answers are so 3 * a constant is a constant and we get rank constant time and N log log n over log n space um if you're concerned that n * log log n over log n is not very sublinear you can do a little bit better using fancier tricks namely you can achieve n over log to the k n uh this is a result of Petru from uh 2008 I'm not going to go into how it's done but if you're interested it's a little bit less it would be nice to do better but my guess is there should be a lower bounds that with constant so this is for any constant K it would be nice to do better like a square root of n or something but my guess is there's a matching lower bound I don't think that's known okay so that was rank our next challenge is to do select the inverse and select is a little bit harder I would say uh don't have a great intuition why but it is and we're going to be able to use the same kind of technique so again we can use a lookup table and uh so I'm to do that first so just like before if we have bit strings of length at most half log n then we're only going to need something like root n space it's root n again time log n log log n space because just like rank there at most n possible queries actually fewer because there may be fewer one bits but at most there are n one bits to query an answer is now an index which is within a thing of size half log n so I just have to write down an index of that size so it's log log bit log log n Bits to write it down cool so that's the same now the challenge is about getting down to uh half log n Bits we're going to use the same technique of two levels of indirection but they work differently uh there's an extra thing we need to deal with uh in select there will be two cases depending on whether your array has lots of ones or not so many ones and those two cases can vary throughout the string so uh what we do first of all is uh maybe I'll go over here I'll stick here whatever so we're back to an N bit string so we're looking at we want the analog of this structure of the structure of chunks now we can't just say oh take the bit string divide it into chunks of equal size because then given a query you know we want to do select of J uh we need to know uh where J belong which of these chunks J belongs to so instead of making them equal size we're going to make them have an equal number of one bits so then we can just take J divide by the size of these chunks which is log n log log n you could probably do log squared as well but log n log log n will uh is a slightly better choice um and so we just divide every log n log log n uh one bit put a vertical bar that way given J we divide by this thing take floor that tells us which Chunk we belong to okay so it's different decomposing by one space instead of zero one space and so for those guys we just store an array if your query happens to have a a zero mod this then you have your answer otherwise you still need to query within the Chunk in some sense the array has gotten divided something like this so the number of one bits in here is is always the same log n log log n ones okay so you can now teleport to the appropriate chunk the issue is how do I solve a chunk but now chunks have different sizes which is kind of annoying that's why we need this extra step which is within a group of log n log log n one bits I'm calling them groups now instead of chunks so each of these groups has different size let's suppose it has size R so let's say it's R bits long okay R is going to be different for each chunk but we'll you do this for every chunk every group uh then there's two cases if R is big uh we're done how big if it's at least the square of the number of one bits that means it's very sparse only square root of the bits are ones the rest are all zeros then I can afford to just store all the answers I'm just going to store a lookup table of all the answers if it's very sparse because then then I claim I only need this many bits in order to store all of these answers so if I I do this for all groups that have a large number of bits I store this lookup array how many how many if I sum up the sizes of all these arrays how much do I pay well um the lookup array has this kind of size they're log n log log n one bits and each of them I need to store an index for them now this could cost log n Bits because potentially one of these uh groups is very large it could be almost linear size so I need log n Bits to write down a position in there there's log n log log n one bits to write down a position four so this is the size of one of these arrays now how many of these do could I possibly need to store well I know that this group has log n log log N squared bits in it so the maximum number of such groups is n over that n over log n log log n^ s and now we get to do some cancellation so this two cancels with this log n log log n and then this log n cancels with this log n and so we get n over log log n Bits which is slightly little o of okay again it is possible to get n over log to the k space uh but we won't do that here we'll be happy enough with n over log log in okay but we're not done unfortunately so we've now we've reduced into groups and I've only given you one case this is when R is large the other case is R is small meaning the number of bits in the group is at most log and log log n s that's a good case for us CU that's pretty similar to rank here we got chunks size log squ here it's slightly larger than log squ but only by a poly log log Factor uh and that would correspond to this step two in rank if you do step two and step three here then we get step two of rank we've reduced to poog size chunks by getting rid of this case and so we have to do it again because we have poly log size groups but we need to get down to groups of size log half log so we need to do another layer of indirection so we get to do steps two and three again this is what I'll call step four repeat steps two and three on sorry I didn't say I need an else Clause else uh I'm going to call this bit Vector A reduced bit vector so I've reduced to order log n log log n squ Bits And so step four is on all reduced strings all reduced bit strings I want to do steps two and three again uh let me do it quickly uh my goal is to further reduce to poly log log n right I took n bit strings and I got down to log poly log bits I do it again I should get down to poly log log bits and indeed I can and this is plenty small Lo poly log log it's way smaller than half log so we don't even need that much of the lookup table uh fine so I'll call this 2 Prime I want to make this explicit because they are slightly different because now everything's relative to this or to the reduced string which is polylog this gets hard to pronounce but every log log n square one bit we're going to write down the relative index within the reduced string of size log n so writing down the relative index only cost log log n Bits because we're in something of size log again so writing down that index is short we write it down for all of these so we end up paying n over log log n sared that's the maximum number of these indices that we need to store each of them we pay log log n so here I'm I'm summing over all the reduced bit strings this is an overall size it's at most n over log Lan square that we need to store could be fewer if there aren't many reduced bit strings but worst case everything ends up being reduced so we have this many times that many times that many bits and we get n over log log n Bits okay this is roughly following the pattern of step two over here step two over here didn't just have the log term it also had a auxiliary log log term so if you felt like it you could make this log log n Times log log log n but it'll actually give you worse Space Bound so this is slightly better okay then we apply step three prime which is we look at each of the groups that we've identified and either it's big either it's big and it has lots of zero bits or it's not big and in either case we're going to be happy so if a group of log log n^ squar 1 bits has R bits we look at each of them individually and if uh R is at least the square of that so log log n to the 4th power so we're losing constants in the exponents but it's not a big deal uh then store uh relative I mean store all the answers but now as relative indices okay let's go over here so how much do these relative indices cost again it's at most order log log n Bits to write them down uh we don't know that a group is any smaller than log n but it's at most the original size of log n It's Only log log n Bits to write each of them down and now we get to say oh well we had to write down log log n^ s 1 bits but this could only happen n over log log n 4th many times so the space is n over log log n to the 4th that's the maximum number of these uh I guess you call them sparse bit vectors sparse groups there could be because each of them is at least this big total number of them is at most n divided by that for each of them we have to write down log log and squared different indices for our array and each of those indices cost log log n Bits to write down so this is three log l n to the third power this is log log n to the 4th power so again this is n over log log n you can tell I've tuned all of these numbers to come out to n over log log n Bits okay um that was the if case there's the else case which is that you have reduced to poly log log size namely then in the in the dense case you have RS at most log log n to the 4th so at this point the so else you are further reduced when you're further reduced you have at most uh log log n to the 4th bits and at most log log N squared of them are one bits but we don't really care about that once we're down to a bit Vector of poly log log size we can use our lookup table and we're done so that's select if you want to do a select on an on an index first you figure figure out which group it's in by dividing by log n log log n taking the floor uh you teleport to the appropriate group using this array then within that group there's a bit saying whether it was sparse or dense um if it was sparse so lots of zeros in it then you have a lookup table that gives you all your answers for the remainder of your uh query if it's dense then you go over here you know that this thing will be stored and so you figure out which subgroup you belong to by dividing by log log n squar taking the floor uh there's an array this thing that teleports you to that group sorry to that subgroup and then you apply uh then there's a bit there saying whether it was sparse or dense if it was sparse there's a lookup table giving you the answer if it was dense there's an index into the number one lookup table that tells you what this bit string is because there there's only log log n to the fourth bits so just by in fact that is the index just what those bits are lets you look up into this table and solve your query in constant time in all cases constant time but here is a little bit more branching depending on the situation as I said select is a little more complicated than rank but in the end constant time little o of n space n over log log n which can again be improveed proved by PQ and N over log to the K and for any constant K question can you just quickly remind us how the two and three changed on the second okay how did two and three change you don't actually really need to change them the big change is that you're storing only relative indices not indices so before uh we're storing an array of indices of every log login one bit these were you know Global pointers but now after 2 and three we've reduced to something as size log n or poly logn we need to exploit that here so we're only storing log log n Bits if we didn't do that this would be order n Bits and it' be too big uh that's really the only thing you need to change the other thing I changed was this value if you follow that plan it would be uh there's also a square so we did have to add the square here you could also add a log log log n term here but it won't matter basic Bally you do something that works uh the square was necessary to cancel out this guy for example you didn't do the square well you so instead of this you could have done log log n time log log log n without the square then here you would have gotten n over log log log n so you could have followed the same pattern you just get a slightly worse Space Bound I tuned it here here we needed here we could not have afforded to go to log squared if I recall correctly though you can check that uh maybe it's a good P at question um there's lots of choices that work here but this is the one I find the cleanest that gets a decent bound not the best bound but reasonable other questions I think that's all that I changed the other the sparcity definition was still a squared thing so it was squared over here and it was squared over here it's just the thing we were squaring was a little different okay one more thing I want to talk about so at this point we just finished this level order representation of binary tries because we already saw left child right child and parent reduced to rank and select we just SOL Rank and select in little L of n Bits so at least statically um we can do left child right child parent in a binary TR now in constant time per operation uh 2 N plus little L of n Bits of space the two end bits are to store those two end bits that we wrote down before so that's that's uh succinct binary tries done uh one mention there are some Dynamic versions in particular there are Dynamic versions of rank and select but the best versions that are known to do Dynamic Rank and select uh achieve something like log over log log uh time per operation if you're interested Dynamic so this is kind of annoying if you want to go to Dynamic either you pay more time or you don't use Rank and select but I'm not going to worry too much about Dynamic stick to rank and select but there's one more thing on this list which is uh a different way to do succinct binary tries and this different way is going to be more powerful more useful for things like suffix trees which is what we're going to do next class so I want to tell you a little bit about uh this um the level order representation is kind of like a warm-up it motivates Rank and select uh but it does not let us do subtree size sub Tre size would be nice to do because you care about how many matches you have after you do a search down a suffix tree level order just ain't going to cut it for that uh so we're going to use different representation we're still going to use Rank and select a lot uh and and generalized forms of Rank and select but it's going to be a little bit more handy essentially I want to do more like a depth for search of the try less like a uh less level order so more depth first okay here's our friend the binary try same one as before we had our binary representation of it I'm not going to draw that here uh first thing I want to do is say hey look uh this is the same thing as a rooted ordered tree I I already mentioned that there's the same number of them there's Catalan of these and there's Catalan of these so a rooted order tree has a node and has some number of children and then more nodes the children are ordered but they don't have labels on them so it's a tree not a try okay so I claim these two things are equivalent and there's a nice combinatorial bje between them which you may have seen before it's kind of a a classic but here we're going to use it for Handy stuff uh basically so binary Tri is distinguish between left and right rooted ordered trees do not they just have order so to clean that up I'm going to look at the right spine of the tri distinguish that because right and left make a difference here and then recurse so this is the right spine of down here this is the right spine of this sub tree now every node lives in some right spine and then I'm just going to rotate 45° counterclockwise so I have AEG that's my first right spine I'm going to think of them as children of a new root node and then they have children below that which correspond to the right spines that hang below so a for example has this uh right spine BCD so b c d here e has a right spine of f hanging off of it g has no right spine hanging off of it so you need to prove that this is a real bje every uh binary TR can be so converted into rooted order treat and it's Unique so uh if it's different over here it'll be different over here and you can convert backwards as well if you just delete this super root and turn all the children into a right spine and recurse they're really the same thing this is why there's Catalan of each of them okay um now what I'd really like to get to is balance parenthesis and while it's a little unclear how to represent a binary try with balanced parenthesis these things it's really clear how to represent a binary uh represent with balanced parenthesis here I just do an Oiler tour which was ad depth for search visiting these things and every time I start a note I'll write an open pen every time I finish a note I write a closed P similar to representation we talked about before so this would be I'm going to need more space it's going to be an open pen for Star I don't I write that make that one really big we start here then we open the a chunk then we do B which has no children then we do c which has no no children then we do D which has no children and that finishes a okay then we start e uh then we do F then we finish F we finish e then we do G and then we're done with star okay so that's very easy transformation again there are Catalan many of these balanced parns you think oh there's two to the end of them uh cuz each each per could be open or closed but they have to be balanced so it's a little bit more constrained than that uh and so it ends up being Catalan of n over two if there's n Pern uh F because that there's two pns here for every node over here this is going to be our bit string open pns are zeros closed pns are ones this has roughly two n Bits 2 n + 2 I guess for the star relative to this n uh so basically nodes here correspond to nodes here which correspond to an open Parn Clos per n pair over here now we're not we can't afford to store these labels those are just guidelines think about what you need um so let's think about there are three things we really want here left child right child and parent this is the rep this is the thing that we care about but this is what we're going to store so I want to translate from here to here to here this is an exercise in Translation so what does a left Child mean here left child over here corresponds to uh well I guess it goes in general the left child goes to this branch which is like all of these children pointers from a but really what if you follow the left child you get to B not any of the other things on the right spine you always get to the top of the right spine top of the right spine is the leftmost node in the spine here in other words it is the first child of a node first child of a node if there is one is going to be the left child over here right child is like following the spine that's like going this way so right child is what I would call next sibling the next sibling to the right if there is one that's going to correspond to the right child because we're just following a right spine okay parent is a little trickier parent is the reverse of these so either uh you take your previous sibling but if you're here and there is no previous sibling then you take your actual parent because parent should walk up here this is like going left previous sibling previous sibling parent of this guy though is the actual parent over here so this is going to be previous sibling if there is one or if there isn't one you go to the parent okay so that's the easy translation now we need to convert these pictures into balance parenthesis pictures which is also going to be easy in itself but the jump all the way from binary tries to balance paren would be pretty confusing so that's why we have this intermediate step so we want uh first child here if I have a p like look I'm looking at a so a corresponds to this p and this P I'm going to represent the node let's say by the first p uh then the first child is just the very next character right you put the first child right after that open p so this is really the next character if we want to find the first child um this is if it's an open p it could be the very next like if you're doing B the very next character is a closed pen that means there are no children okay but that's how you can tell whether there's a child if there's an open pen right after your open pen that's your next child that's your first child I should say now what about next sibling so let's say again I'm at a and I want to know the next sibling next sibling is e so that's like I go to the closed paren for a and then I go to the next character so this would be uh go to the Clos peren for where you are right now and then go to the next character uh this is again if it's an open pen if it's a closed pen then you have no next sibling so again you can tell whether this operation fails what we need is an operation given a bit string representing balanc parentheses and given a query position of a left Pern I need to know what is the matching right pin and I'll just wave my hands and claim that can be done with the same techniques as Rank and select it's not easy it's quite a bit harder but you do enough of these recursions eventually you you can solve it okay last operation is parent over here which corresponds to previous sibling or parent over here here which corresponds to uh there two cases we want to move backwards so here we're always ending with next character so first thing we do is go to the previous character and there are two cases um if it's a Clos pen so let's say we're here at e we go to the previous character if it's a closed P then a is our previous sibling and so so we want to do the previous sibling situation we again find the match we hit percent in VI and what's the corresponding thing in emac I forget uh you go to the matching Clos pen uh sorry open pen and then there you go you've got your previous sibling so if it's a close pen then you go to the corresponding open pen if the ne If the previous character is an open pen then you're done that's your parent so like here if you're at a you go to the previous character and it's an open pen then you just found the parent of a there was no previous sibling so in either case you end up with an open PR corresponding to either your previous sibling or your parent so that's left child right child parent if you have this uh matching parent operation you can do all of these in constant time and uh little o end space beyond the two end bits to write down that bit string that's not so exciting because we just reinvented the same results we had before of doing left right and parent in constant time but what we buy out of this representation is we can now do SUB tree size so um this is a little bit trickier uh let me go to another board but whereas with level representation it was impossible now it is possible and we're going to use subtree size I think next class when we do suffix compact suffix trees so subtree size is what we want in the binary try in the rooted order tree it's a little tricky cuz sub trees no longer correspond to sub trees for example the sub tree of C consists of this sub tree and this sub tree so it's really C over here it's C and all of its right siblings so this is size of the node plus size of right siblings however many you have okay so in the rooted order it's actually kind of messy turns out in the balanced parenthesis uh it's pretty clean cuz all your right siblings correspond to paren groups that just follow each other and you want to know so these are a bunch of siblings here varying size and we're given say this sibling we want to know for this sibling up to all all the ones to the right so there's a an enclosing parenthesis here for our parent in this representation uh we want to know the length of these so it's just um we want to take the here we are at this left per n we want to compute the distance to the enclosing Clos peren so that's here that's our enclosing close P so here's a new operation given a pen pair I want to compute the enclosing peren pair these guys uh that can also be done in constant time with Rank and select like techniques and then you just measure this distance and you divide by two that will give you the number of nodes in here it's half the number of Pern that will give you sub tree size and we have a couple extra seconds so another bonus is suppose you want to know the number of leaves in a sub tree if I recall correctly that's something like instead of doing this distance to the enclosing uh peren you do something like rank of just rank of that those are the number of leaves of uh the peren this is getting notationally confusing minus the rank of uh uh here okay so I just want to compute how many open par closed par ends are there from here to here and so I just take the rank here subtract by the rank here that gives me the number of leaves in that range so this is a generalization of rank before we did rank of just a single bit this is rank of a two bit pattern but two bits is not much harder than one bit you can very easily adapt the rank structure we saw to do any two bit pattern instead of just the one bit uh so that gives you the number of leaves in the sub tree which corresponds to the number of matches so you can do lots of fun things like this this representation is super powerful and we'll use it next time
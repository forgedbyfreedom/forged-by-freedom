now it's time to evaluate our models on the testing set so the first model we're gonna want to look at is that smart baseline model that basically just took a look at the polling results from the Rasmussen poll and use those to determine who is predicted to win the election so it's very easy to compute the outcome for this simple baseline on the testing set we're gonna want to table the testing set outcome variable Republican and we're going to compare that against the actual outcome of the smart baseline which as you recall would be the sign of the testing sets rasmussen variables and we can see that for these results there are 18 times where the smart baseline predicted that the Democrat would win it's correct 21 were predicted the Republican would win and was correct two times when it was inconclusive and four times where it predicted Republican but the Democratic actually won so that's four mistakes and two inconclusive results on the testing set so this is going to be what we're going to compare our logistic regression based model against so we need to obtain final testing set prediction from our model so we selected mod two which was the two variable model so we'll say test prediction is equal to the predict of that model that we selected now since we're actually making testing set predictions will pass in new data is equal to test and again since we want probabilities to be returned we're going to pass type equals response and the moment of truth we're finally going to table the test set Republican value against the test prediction being greater than or equal to zero point five at least A-50% probability of the republic you money and we see that for this particular case in all but one of the 45 observation in the testing set were correct now we could have tried changing this threshold from 0.5 to other values and kind of compute it out an ROC curve but that doesn't quite make as much sense in this setting where we're just trying to accurately predict the outcome of each state and we don't care more about one sort of error when we predicted a Republican it was actually Democrat and the other when we predict a Democrat and it was actually Republican so in this particular case we feel ok just using the cutoff of 0.5 to evaluate our model so let's take a look now at the mistake we made and see if we can kind of understand what's going on so to actually pull out the mistake we made we can just take a subset of the testing set and limited to when we predicted true but actually the Democrat one which is the the case when that one failed so this would be when test prediction is greater than or equal to 0.5 and it was not a Republican so Republican was equal to zero so here is that subset which just has one observation since we made just one mistake so this was for the year 2012 the testing set year this was the state of Florida and kind of looking through these predictor variables we see why we made the mistake right the Rasmussen poll gave the Republican a two percentage point lead survey u-s-a called a tie discount said there were six more polls that predicted republican than democrat and two-thirds of the polls predicted the Republican was going to win but actually in this case the Republican didn't win Barack Obama won the the state of Florida in 2012 over Mitt Romney so the models here are not magic and given this sort of data it's it's pretty unsurprising that our model actually didn't get Florida correct in this case and made the mistake however overall it seems to be outperforming the smart baseline that we selected and so we think that maybe this would be a nice model to use in the election prediction
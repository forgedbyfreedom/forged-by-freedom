on our to-do list from the previous section is figuring out how to build and and or gates with many inputs these will be needed when creating circuit implementations using a summer products equation as our template let's assume our gate library has only two input gates and figure out how to build wider gates using the two input gates as building blocks we'll work on creating three and four input gates but the approach we use can be generalized to create and and or gates of any desired width the approach shown here relies on the associative property of the and operator this means we can perform an N way and by doing pairwise ends in any convenient order the or and XOR operations are also associative so the same approach will work for designing wide or and XOR circuits from the corresponding two input gate simply substitute two input or gates or two input XOR gates for the two input and gate shown below and you're good to go let's start by designing a circuit that computes the and of three inputs a blast and cruise in the circuit shown here we first compute a and B and then and that result with C using the same strategy we can build a four input and gate from three two input end gates essentially we're building a chain of an gates which implement an n way and using n minus 1 two input and gates we can also associate the four inputs a different way computing a and B in parallel with C and D then combining those two results using a third and gate using this approach we're building a tree of and gates which approach is best chains or trees first we have to decide what we mean by best when designing circuits were interested in cost which depends on the number of components and performance which we characterized by the propagation delay of the circuit both strategies require the same number of components since the total number of pairwise ands is the same in both cases so it's a tie when considering costs now consider propagation delay the chain circuit in the middle has a TPD of three gate delays and we can see that the TPD for an N input chain will be n minus one gate delays the propagation delay of change grows linearly with the number of inputs the tree circuit on the bottom has a TPD of two gates smaller than the chain the propagation delay of trees grows logarithmically with a number of inputs specifically the propagation delay of tree circuits built using two input gates grows as log base two of n when n is large tree circuits can have dramatically better propagation delay than chain circuits the propagation delay is an upper bound on the worst case delay from inputs to outputs and is a good measure of performance assuming that all inputs arrive at the same time but in large circuits a B C and D might arrive at different times depending on the TPD of the circuit generating each one suppose input D arrives considerably after the other inputs if we use the tree circuit to compute the and of all four inputs the additional delay in computing Z is two gate delays after the arrival of D however if we use the chain circuit the additional delay in computing Z might be as little as one gate delay the moral of this story it's hard to know which implementation of a sub circuit like the for input and showing here will yield the smallest overall tpd unless we know the tpd of the circuits that compute the values for the input signals in designing CMOS circuits the individual gates are naturally inverting so instead of using and in or gates for the best performance we want to use the NAND and nor gates show in here NAND and nor gates can be implemented as a single CMOS gate involving one pull-up circuit in one pulldown circuit and an or gate require two CMOS gates in the implementation for example a NAND gate followed by an inverter we'll talk about how to build some our product circuitry using nand's and NORs in the next section note that NAND and nor opera Asians are not associative NAND of a blast and cruise is not equal to the nand of see with the nand of a and B so we can't build a NAND gate with many inputs by building a tree of two input Nance we'll talk about this in the next section too we've mentioned the exclusive or operation sometimes called XOR several times this logic function is very useful when building circuitry for arithmetic or parity calculations as you'll see in lab 2 implementing a two input XOR gate will take many more n FETs and p FS than required for a two input NAND or nor we know we can come up with a summer products expression for any truth table and hence build a circuit implementation using inverters and gates and or gates it turns out we can build circuits with the same functionality using only two input NAND gates we say the two input NAND is a universal gate here we show how to implement the summer products building blocks using just two input NAND gates in a minute we'll show a more direct implementation for summer products using only Nan's but these little schematics are a proof of concept showing that NAND only equivalent circuits exist to input nor gates are also universal as shown by these little schematics inverting logic takes a little getting used to but it's the key to designing low-cost high performance circuits in CMOS
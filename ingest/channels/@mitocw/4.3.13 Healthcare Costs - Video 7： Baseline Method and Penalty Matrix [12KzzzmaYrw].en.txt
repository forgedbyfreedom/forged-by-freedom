let's now see how the baseline method used by D to Hawkeye would perform on this data set the baseline method would predict that the cost bucket for a patient in 2009 would be the same as it was in 2008 so let's create a classification matrix to compute the accuracy for the baseline method on the test set so we'll use the table function where the actual outcomes are claims test bucket 2009 and our predictions are claims test bucket 2008 the accuracy is the sum of the diagonal the observations that were classified correctly divided by the total number of observations in our test set so we want to add up a hundred and ten thousand one hundred and thirty eight plus ten thousand seven hundred and twenty one plus two thousand seven hundred and seventy four plus one thousand five hundred and thirty nine plus a hundred and four and we want to divide by the total number of observations in this table or the number of rows in claims tests so the accuracy of the baseline method is point sixty eight now how about the penalty air to compute this we need to first create a penalty matrix in our keep in mind that we'll put the actual outcomes on the left and the predicted outcomes on the top so I'll call it penalty matrix which will be equal to a matrix object in our and then we need to give the numbers that should fill up the matrix 0 1 2 3 for that'll be the first row and then to 0 1 2 3 that'll be the second row for 2 0 1 2 for the third row 6 4 2 0 1 for the 4th row and finally 8 6 4 to 0 for the fifth row and then after the parentheses type a comma and then by row equals true and then add n row equals 5 close the parentheses and hit enter so what do we just create type penalty matrix and hit enter so with the previous command we filled up our matrix row by row the actual outcomes are on the left and the predicted outcomes are on the top so as we saw in the slides the worst outcomes are when we predict a low cost bucket but the actual outcome is a high cost bucket we still give ourselves a penalty when we predict a high cost bucket and it's actually a low cost bucket but it's not as bad so now to compute the penalty error of the baseline method we can multiply our classification matrix by the penalty matrix so go ahead and hit the up arrow to get back to where you created the classification matrix with the table function and we're going to surround the entire table function by AZ dot matrix to convert it to a matrix so that we can multiply it by our penalty matrix so I'm at the end close the parentheses and then multiplied by penalty matrix and hit enter so what this does is it takes each number in the classification matrix and multiplies it by the corresponding number in the penalty matrix so now to compute the penalty air we just need to sum it up and divide by the number of observations in our test set so scroll up once and then we'll just surround our entire previous command by the sum function and we'll divide by the number of rows in claims test and hit enter so the penalty air for the baseline method is 0.74 in the next video our goal will be to create a cart model that has an accuracy higher than 68% and a penalty error lower than 0.7 for
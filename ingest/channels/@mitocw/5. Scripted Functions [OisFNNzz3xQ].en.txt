this is do Matlab lecture five scripts and functions the previous lecture we saw the function F to C that converts Fahrenheit to Celsius let's take another look at that function here it is it's a very simple function I've changed it a little bit but it accepts Fair as input sell as output and this is how we write it I could have of course written this as a script and it would have been a much worse idea let's try it as a script I'll comment this out I'll comment this out and now if I save it and change directory to scripts I can say fair equals 80 and Now call my f2c and now if I look at cell it equals the answer the problem with this there are several problems with this one is that I need to understand the inner working my inner notation of the script in order to use it I need to know that fair is an input I need to know that the cell is the output so I need to know that before I call the script I have to assign ad to Fair after I call the script I get the answer inside cell I need to know all of that and of course most functions are going to be much more complicated than this one liner another problem is that obviously since it requires me to write into fair and write into cell it will overwrite fair and cell and particularly it will overwrite cell so if I have an important number in cell and now I call f2c this important number is overwritten there's nothing I can do about it there's no except for changing this script there's no way I can make it not overwrite cell now most functions have all kinds of intermediary variables so intermediary one and there's some assignment that goes into it there and another one and so on which are needed for the calculation these are of course fake ones but imagine that you had these and now you would call these sorry you would call F Toc again you would be littered by these variables that you don't know where they came from you don't care about them because there's some partial result inside the script and also they might have overwritten one of your variables so this could be really a disaster and this this is where scoping is important when you put this inside a function even if you keep all the littering the same and now you call this function let's clear my variables first let's look who's here no one's here and now I'm going to call F to C with ad I get the answer and the only variable that exists is this automatic variable called ANS ANS is an automatic variable that is generated when a value is returned but it's not assigned so for example if I do 1 + 2 there is also an ANS and ANS equals 3 this is useful if for example notice that here I forgot to actually assign this into a variable so I can now save it by saying xals ANS and now my answer is saved into X again using functions does not litter my workspace with partial results you see in1 in two are are contained inside this function and we do not see them outside and they make it unnecessary to know the inner workings of the function I don't need to know what the inner name of the input variable is and what the inner name of the output variable is I just need to know that f2c accepts an input variable and returns a value I don't need to know the name of those variables scope also allows us to write what's called recursive functions classic example of recursive function is the Fibonacci is the function that creates the Fibonacci sequence so let's try this we already had one Fibonacci so let's do Fibonacci 2 and then accept an N the base case of course we need to return the value and if it's not less than three then we need to return the sum of the two lower Fibonacci numbers this is a horrible way of writing the Fibonacci creating the Fibonacci sequence because it involves an exponentially large amount of function calls but it works and the reason is work works is because the fibo in here the variables inside it are also disconnected from the feibo here and the feo here all these functions are are called one inside the other but they don't overwrite their the variables so the V inside the inner feo is different from the V inside the outer feo and so on so let me save this okay so this works now notice that if I try it with 25 it takes a bit of a time and 35 is almost too long to run I'm not going to even wait for this to run this is calling 2 to the^ 35 function calls which is a little bit too many for me and I'm going to cancel it contrl + C cancel then you can see here let me show you the trace of all the function calls when it was stopped so it was stopped here but this was called by from this line and this was called from this line and this was called from here from here this is all the same line calling itself over and over again each time lowering n by one until eventually n is two and then it can return the value is that it keeps forgetting the value of the Fibonacci sequence when it needs it again to fix that let's just keep a variable that will actually hold the Fibonacci sequence so we initialized it with 1 one which is the starting point and now we will call a helper file helper function that is and we will will return the value that is needed the helper function will guarantee that FN is defined let's see how we do this here is the helper function it doesn't have any return value but it has an input and all it does it checks if the number of elements in F is less than n and if it is then it calculates the required one so first it calls helper with nus one and then it says that FN = FN -1 + FN minus 2 and then it's done so the helper function guarantees that the Fibonacci sequence is updated up to n this variable F and then we just return the one we need so by this way we only this assignment only happens once for every nend which makes it linear at end now we don't need all of this stuff the only thing we do need is this Final End we want let me show you here we want this function here sorry we need another end we want this function here to be embedded inside this function because what that does is it makes this variable F visible both for this function and for this function so so this F and this F are the same variable and the the hover text says so scope of variable F spans multiple functions okay so let's see how that works we'll save it and now we call it Fe 2 with three and the answer is correct four five and 35 No problem 45 no problem 55 no problem this is a simple example of what's called dynamic programming and this is a way of reducing an exponential recursive implementation into a linear one now you can have more than one input V variable and you can have more than one output variable but I suggest that you investigate that on your own
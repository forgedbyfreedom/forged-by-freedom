using a deregister as the memory component in our sequential logic system works great at each rising edge of the clock the register loads the new state which then appears at the registers output as the current state for the rest of the clock period the combinational logic uses the current state and the value of the inputs to calculate the next state and the values for the outputs a sequence of rising clock edges and inputs will produce a sequence of states which leads to a sequence of outputs in the next chapter we'll introduce a new abstraction finite state machines that will make it easy to design sequential logic systems let's use the timing analysis techniques we've learned on the sequential logic system shown here the timing specifications for the register and combinational logic are as shown here are the questions we need to answer the contamination delay of the combinational logic isn't specified what does it have to be in order for this system to work correctly well we know that the sum of the register and logic contamination delays has to be greater than or equal to the whole time of the register using the timing parameters we do know along with a little arithmetic tells us that the contamination delay of the logic has to be at least one nanosecond what is the minimum value for the clock period T clock the second timing inequality from the previous section tells us that T clock has to be greater than the sum of the register and logic propagation delays plus the set-up time with the register using the known values for these parameters gives us a minimum clock period of 10 nanoseconds what are the timing constraints for the input signal relative to the rising edge of the clock for this we'll need a diagram the next state signal is the input to the register so it has to meet the setup and hold x is shown here next we show the input signal how the timing of each transitions affect the timing of the next state signal now it's pretty easy to figure out when input has to be stable before the rising clock edge in other words time for input the set-up time for input is the sum of the propagation delay of the logic plus the set-up time for the register which we calculated seven nanoseconds in other words if the input signal is stable at least seven nanoseconds before the rising clock edge the next state will be stable at least two nanoseconds before the rising clock edge and hence meet the register specified set-up time similarly the whole time of input has to be the whole time of the register minus the contamination delay of the logic which we calculate as one nanosecond in other words if input is stable at least one nanosecond after the rising clock edge the next state will be stable for another one now second in other words a total of two nanoseconds after the rising clock edge this meets the specified hole time of the register this completes our introduction to sequential logic pretty much every digital system out there is a sequential logic system and hence is obeying the timing constraints imposed by the dynamic discipline so next time you see an ad for a 1.7 Kiko Hertz processor chip you'll know where the 1.7 came from
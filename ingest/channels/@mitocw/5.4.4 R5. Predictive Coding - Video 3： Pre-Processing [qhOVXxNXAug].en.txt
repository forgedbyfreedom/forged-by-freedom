okay now it's time to construct and pre-process the corpus so we'll start by loading the TM package with library of TM and now that we have done that we'll construct a variable called corpus using the corpus and vector source functions and passing in all of the emails in our data set which is emails dollar sign email okay so now that we've constructed the corpus we can output the first email in the corpus we'll start out by calling this to our wrap function to get it on multiple lines and then we can select the first element in the corpus using the double square bracket notation and selecting element 1 and we can see that this is exactly the same email that we saw originally so email about the working paper alright so now we're ready to pre-process the corpus using the TM map function so first we'll convert the corpus to lowercase using TM map and the two lower function still have corpus is equal to TM map of corpus and pass it along to lower alright and then we'll do the exact same thing except removing punctuation so I have corpus is equal to TM map of the corpus with the remove punctuation ok we'll remove the stop words with remove words function and we'll pass along to stop words of the English language as the words we want to remove and lastly we're going to send the document so if corpus is you put a TM map of corpus with the stem document function ok and now that we've gone through those for pre-processing steps we can take a second look at the first email in the corpus so again call STR wrap on corpus selecting the first email and now it looks quite a bit different let me come up to the top here it's a lot harder to read now that we removed all the stop words and punctuation and word stems but now the emails and this corpus are ready for our machine learning algorithms
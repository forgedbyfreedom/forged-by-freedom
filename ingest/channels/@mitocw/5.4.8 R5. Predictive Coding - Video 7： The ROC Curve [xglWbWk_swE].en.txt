now let's look at the ROC curves so we can understand the performance of our model at different cut-offs will first need to load the ROC our package with library of ROC our next we'll build our ROC our prediction object so we'll call this object P re D our C our prediction our RC r that's equal to the prediction function the first argument is our predicted probabilities of a document being responsive so that's pre dot prob and the second argument is the true outcomes whether or not the documents are responsive which is test dollar sign responsive all right so now we want to plot the ROC curve so we'll use the performance function to extract the true positive rate and false positive rate so create something called perf ROC our sequel to the performance will pass our prediction object and we want to extract the true positive rate and the false positive rate and then we'll plot our performance object and we can use colorize equals true so that we can see the colors for the different cutoff thresholds all right now of course the best cutoff to select is entirely dependent on the costs assigned by the decision-maker to false positives and true positives however again we do favor cut-offs that give us a high sensitivity we want to identify a large number of the responsive documents so something that might look promising might be a point right around here in this point part of the curve where we have a true positive rate of around 70% meaning that we're getting about 70% of all the responsive documents and a false positive rate of about 20% meaning that we're making mistakes and accidentally identifying as responsive 20% of the non-responsive documents now since typically the vast majority of documents are non-responsive operating at this cut would result perhaps in a large decrease in the amount of manual effort needed in the e-discovery process and we can see from the blue color of the plot at this particular location that we're looking at a threshold around maybe 0.15 or so significantly lower than 50% which is definitely what we would expect since we favor false positives to false negatives so lastly we can use the RCR package to compute our AUC value so again call the performance function with our prediction object this time extracting the AUC value and just grabbing the Y value slot of it we can see that we have an AUC and the test set of seventy nine point four percent which means that our motto can differentiate between a randomly selected responsive and non-responsive document about 80 percent of the time
in the previous video we identified clusters or tissue substances and a healthy brain image it would be really helpful if we can use these clusters to automatically detect tumors in MRI images of sick patients the tumor that CSV file corresponds to an MRI brain image of a patient with oligodendroglioma a tumor that commonly occurs in the front lobe of the brain since brain biopsy is the only definite diagnosis of this tumor ever I guidance is key in determining its location and geometry now make sure that the tumor does CSV file is in your current directory let's go to the console and clear it and then read the data and save it to a data frame that we're going to call tumor who use the wreath of CSV function takes as an input the tumor they assess and make sure to turn off the header using header equals false and let's quickly create our tumor matrix using the S dot matrix function over the tumor data frame and the tumor vector using the esta vector function over the tumor matrix now we will not run the k-means algorithm again on the tumor vector instead we will apply the k-means clustering results that we found using the healthy brain image on the tumor vector in other words we treat the healthy vector as a training set and the tumor vector as a testing set to do this we first need to install a new package that is called flex blast so let us type installed our packages and then in between quotations flex cost and then the first thing our well ask us to unset the region that is closest to our geographical location and after that press ok and the installation shouldn't take more than two seconds to complete ok now that the package is installed let us load it by typing library and parentheses flex plus the flex plus package contains the object class KCC a which stands for carry centroids cluster analysis we need to convert the information from the clustering algorithm to an object of the class KCC a and this conversion is needed before we can use the predict function on the test set tumor vector so calling our new variable K and C dot G CCA and then using the S dot KCC a function which takes as a first input the original K and C variable that stored all the information from the k-means clustering function and the second input is the data that we clustered and in this case it's the training set which is the healthy vector and I'll be aware that this data conversion will take some time to run now that our finally finished creating the object k + c dot k CCA we can cluster the pixels of the tumor vector using the predict function let us call the cluster vector tumor clusters and that would be equal to predict and the first input is this new object can see that K CCA and the second input is the test data so let's type new data equals tumor vector and now the tumor clusters is a vector that assigns a value 1 through 5 to each of the intensity values in the tumor vector as predicted by the k-means algorithm to output the segmented image we first need to convert the tumor clusters to a matrix so let's use the dimension function and then the input is simply tumor clusters and then using the C function with the first input as the number of rows in the cuber matrix and the second input as the number of columns in the tumor matrix and now we can visualize the clusters by using the image function with the input tumor clusters matrix and make sure to set the axe system Falls and let's use again these fancy rainbow colors here so color is equal rainbow with the input K again K is equal to 5 all right let's navigate to the graphics window now to see if we can detect the tumor only yes we do it is this abnormal substance here that is highlighted in blue that was not present in the healthy MRI image so we were successfully able to identify more or less the geometry of the malignant structure we see that we did a good job capturing the major tissue substances of the brain the gray matter is highlighted in purple and the white matter in yellow for the sick patient the substance highlighted in blue is the only go down to human tumor notice that we do not see substantial blue regions and the healthy brain image apart from the region around the eyes actually looking at the eyes regions we notice that the tool images were not taken precisely at the same section of the brain this might explain some differences and shapes between the two images let's see how the images look like originally we see that the tumor region has a lighter color intensity which is very similar to the region around the eyes and the healthy brain image this might explain highlighting this region in blue of course we cannot claim that we did a wonderful job obtaining the exact geometries of all the tissue substances but we are definitely on the right track in fact to do so we need to use more advanced algorithms and fine-tune our clustering technique MRI image segmentation is an ongoing field of research while k-means clustering is a good starting point more advanced techniques have been proposed in the literature such as the modified fuzzy k-means clustering method also if you are interested or has packages that are specialized for analyzing reticle images now if we had MRI axial images taken at different sections of the brain we could segment each image and capture the geometries of substances at different levels then by interpolating between the segmented images we can estimate the missing sizes and we can then obtain a 3d reconstruction of the anatomy of the brain from 2d MRI cross-sections in fact 3d reconstruction is particularly important in the medical field for diagnosis surgical planning and biological research purposes I hope that this recitation gave you a flavor of the Stassen ating field of image segmentation in our next video we will review all the UNIX tools we have covered so far in this class and discuss their uses pros and cons
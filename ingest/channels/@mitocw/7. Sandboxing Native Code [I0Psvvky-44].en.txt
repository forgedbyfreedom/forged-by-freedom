the following content is provided under a Creative Commons license your support will help MIT open courseware continue to offer highquality educational resources for free to make a donation or to view additional materials from hundreds of MIT courses visit MIT opencourseware at ocw.mit.edu all right let's get started so um today we're going to talk about uh the system called native client which um is actually a real world system used uh by Google uh as one example they actually use it in the Chrome browser to allow web applications to run arbitrary native code it's actually a pretty cool system and uh it's also an illustration of another isolation or sort of sandboxing or privilege separation technique that's called software fault isolation that doesn't rely on operating systems to sandbox a process virtual machines but instead has a very different approach to looking at the particular instructions in a binary to figure out whether it's going to be safe to run or not so before we I guess start looking at the technical details of this system um let's figure out why do these guys actually want to run native code right so the particular context they are interested in applying their solution to is a web browser where you could already run JavaScript code and maybe Flash and maybe a couple couple of other things why are these guys so excited about running raw x86 it seems like a step backwards like really fast computation yeah so one huge advantage of native code even though it might be unsafe from some perspectives uh it's really high performance whatever you can do in JavaScript presumably you can just write the same thing in assembly and it'll go at least as fast probably much faster any other reasons yeah code yeah so another big thing is maybe not everything is written in JavaScript so if you have an existing application I guess Legacy in Industry terminology uh if you have some existing code that you really want to run in the web uh then this seems like a great solution because you can just take an existing Library like some complicated I don't know Graphics processing engine that's both performance sensitive and like lots of complicated stuff you don't want to reimplement then this seems like a good solution anything else if you're just like programming a new web app should you use native client if you don't care about Legacy or performance so much any other reasons I guess another yeah you don't have to use JavaScript yeah that's an awesome reason right like you know it might if you don't like JavaScript then you don't have to use it right you can actually use I don't know well see if you're so inclined you could run python code you could write hascal whatever you think is useful uh you can actually support other languages all of a sudden so this is a you know reasonably compelling list of sort of motivation uh for them to run native code in the browser and um turns out to be reasonably tricky to get right so we'll look at the technical details I guess of how it works in a second but just to show you guys what this paper is talking about I just want to show a very simple tutorial demo almost that I got out of their native client you know website it's fairly simple as it turns out to just take a C++ or a c program and run it in the browser so just to show you what this looks like here's the basically a demo I mostly sort of took from one of their examples so you can look at you know a web page like this index HTML file and inside of it you have a bunch of JavaScript code and the reason this JavaScript code exists is to sort of interact with the native client piece so the way you can sort of think of this running in the browser is that you have the browser you have well we'll talk about much more about web security later but roughly you have some sort of a page uh web page that contains JavaScript code and this runs with the pages Privileges and this can do you know various things to the web page itself maybe talk to the network in some circumstances but what Native client allows you to do is to have this native client module running sort of to the side in the browser as well and the JavaScript code can actually interact with the native client module and get responses back and what you see here in this web page is the little bit of JavaScript code that's necessary a native client to interact with a particular nackle module that we're going to be running and you can send messages to this module the way you do it is you take this module object in JavaScript and you call Post message in it and you can actually Supply a message to send to the native client module and when the native client module responds it'll run this handle message function in JavaScript and in this particular case it just pops up an alert dialogue box in my browser so this is actually air simple interface from the web page side from the JavaScript side and the only thing you Additionally you have to do is you actually have to declare this native client module this way so you just say embed a module with a particular ID and the sort of most interesting part is the source equals hello. some nmf part attribute and this one just says well here's the roughly executable file that you need to load and start running in the native side of things and this native code actually looks like any other C++ code you might write roughly so here's the program the interesting part is roughly this handle message function so this is a C++ class and whenever the JavaScript code sends some message to the native code it'll actually run this function and it'll check if the message that's being sent is hello and if so construct a reply string of some sort and send it back it's like fairly simple stuff but just to be concrete uh let's try to run it and see what happens so we can actually build it and uh run a little web server that uh is going to serve up this page and Native client module so here I can go to this URL and here we go right it's actually loaded in the module the module seems to have gotten our Hello message from JavaScript replied back with the string back to JavaScript and the JavaScript code popped up a dialog box containing that response so it actually does kind of work um we can try to see if we can crash native client but hopefully not but uh we can you know take this code and we have this buffer we could you know write a bunch of A's to it I don't know quite a lot um and see what happens so hopefully this shouldn't crash my browser because native client is trying to provide isolation um um but let's see what happens so we can rebuild it rerun the web server and here if you run it nothing happens we don't get the message back anymore so clearly message didn't get sent back from the Native client module because I don't see any popup we can look at the JavaScript console here and we can see that the native client module tells us knle module crashed so somehow it caught this buff overflow uh scribbling over some memory or maybe it jump to some bad address containing all A's uh but in any case the native client module is actually able to contain this without this arbitary sort of memory corruption in the module affecting the rest of the browser so this is roughly just a sort of quick demo of what the system is and um what how how you use it as as a end user or web developer so let's look now some more so that's all in terms of demos I have to show you uh so let's look more now in at uh how Native client is going to work or perhaps even why we need this particular design as opposed to some of the Alternatives so so if your goal I guess is to sandbox native code there's a number of Alternatives you could make right people actually had these problems before of performance existing Legacy code and other languages before native client came around and people just solved them in different ways that maybe weren't as satisfying in from a security standpoint orbe a usability standpoint as native client but it is doable so let's see so what so what what could you do if you really want to run native code in a browser so one option that people did was to trust the developer and maybe a variant of this approach is to ask the user whether they want to run some piece of code in their browser or not so does everyone understand roughly what the plan is right like instead of having that whole native client compilation strategy I could have just built a simple C program served it up on the browser and maybe the browser asked do you want to run this site or not if I click yes then it accidentally scri over the browser's memory and crashes the browser so it's possible Right it certainly solves all these goals but what's wrong with it well I guess there's the insecurity part uh which is unfortunate uh one way to potentially get around this and some systems did like uh Microsoft had the system called active X that basically implemented this plan you could serve binaries to IE the browser on your machine and as long as it came with a certificate from a particular developer signed by let's say Microsoft or someone else then it would actually run this code so what do you guys think about this plan is this useful yeah well doesn't tell you anything about does just a signature on the code the code should be contining malicious that's right yeah so right you really have to put quite a bit of trust into whoever it is that's signing this that they will only sign binaries that will not do something bad um but it's kind of vague what this bad thing is and presumably they're just writing C code and you know signing it blindly without doing a huge amount of work um in which case you might well be susceptible to some problems down the line what if we ask the user yeah that like we read that paper in 6033 where user sometimes will not even read the message oh yeah yeah yeah right like the user's goal is to run this thing and uh you know even if the user wants to be cautious it's actually not clear how should the user decide like suppose I really want to understand should I allow this program to run all it tells me is well maybe it came from google.com or microsoft.com and its file name is food. exe I don't know like what's in there right even if I disassemble the code it might be very hard to tell whether it's going to do something bad or not so it's actually really hard to decide so one way to think of native client is it's a mechanism by which users can actually get some confidence whether they should say yes or not to running this thing so in practice I guess like Paul Jan was giving this guest lecture last week and he suggested that you should enable this click to play extension and chrome so every extension including native client you should first click on this thing before it starts running so in some way let's ask the user but then the user actually has some sense that well if I click it I'm still hopefully safe in the sense that native client will protect me so the two aren't necessarily exclusive but you really want some technical guarantee from native client that ensures that even if you say yes there's something meaningful that is enforcing your security soes this make sense okay so the other I guess approach that uh people have taken is uh to use Os or Hardware sandboxing or isolation so this is uh things we looked at in the last couple of lectures so uh maybe you would use Unix isolation mechanisms uh maybe if you had something more sophisticated if you're running on FreeBSD maybe You' use capsicum it seems very well suited to sandboxing some piece of code because you could give it very few two capabilities and then seems great and Linux has a similar mechanism called SEC comp that we briefly touched on in last lecture that could allow you to do similar things so it seems like there's already a mechanism for running code and isolation on your machine why are these guys opposed to using this existing solution and they're like Reinventing the wheel for some reason so what's going on yeah oh yeah maybe they want to minimize the PCD yeah so in some sense maybe they don't want to trust uh the operating system so maybe they're here they're actually worried about OS bugs it might be that the FreeBSD kernel or the Linux kernel has quite a lot of C code written that they don't want to audit for correctness or maybe can't even audit for correctness even if they wanted to and in one of these capsicum or seccom based isolation plans you probably do trust quite a bit of code in the kernel to be correct for the sandbox to actually hold and enforce isolation yeah as you get a lot more uh ways to use browsers and stuff like you have to deal with having some sort of thing and doing with it on like IOS and Android and all these other things access yeah so so actually another interesting consideration is that not only might the os's have bugs but actually the os's are incompatible with each other in some ways uh meaning that each OS has its own mechanis like right here well there's Unix there's capsicum there's setc comp and this is just the Unix variants there's Mac OS seat belt there's windows something else and and this just keeps going on and on so as a result every platform you will have to use a different isolation mechanism and the thing that actually bothers them is not so much they'll have to write different code for mac and windows and Linux but more that this impacts how you write the thing inside of the sandbox because a native client you actually write a piece of code and it runs the same way or it's the same piece of code that runs on Apple or Windows or Linux systems and if you use these isolation mechanisms they actually impose different restrictions on the program being sandboxed so you'll have to run right one program that's going to run inside of a Linux sandbox another program inside of a Windows sandbox and so on so this is actually not acceptable to them they don't want to deal with these kinds of problems so are there other considerations yeah presumably performance as well because if you if you use a uh capsum you need to Fork up a separate process for whatever is running inside the sandbox whereas here they can actually run it in the same in that's true yeah so potentially the approach they take the software fault isolation plan is actually highly performant and could outperform these uh sandboxes at the OS level it turns out that a native client actually use both their sandbox and the OS sandbox just for extra precaution for safety so they don't actually win on performance in their implementation but they could you're right yeah can there be some like control aspect to it they can control what happens in the browser but if they send C off to the client's machine into their OS sort of don't necessarily know what might be happening so I guess one maybe one way to think of that is that yeah the OS might have bugs so my OS might not do as good of a job at sandboxing it or maybe there's you know the interfaces are all different so you don't know what the O is going to expose yeah uh so doesn't like prevent the cach doing some bad things like there are a lot of cases of bad things that the code can just do like maybe you want to statically analyze it just as one to sit in a loop and then not allow that as a valid program so you could right so their approach is quite powerful in the sense that you could try to look for various kinds of problems in the code like maybe infinite Loops Etc it's hard to decide kind of like the halting problem whether it's going to have infinite Loops or not uh but in principle you might be able to catch some problems um I think one actually interesting example that I almost didn't realize sort of existed before reading this paper is these guys are worried about Hardware bugs as well that not only are they worried that the operating system might have vulnerabilities that the malicious code will exploit but also that the processor itself has some instructions that will hang it or it'll reboot your machine and in principle your Hardware shouldn't have such bugs because the operating system relies on the hardware to trap into the kernel if there's anything bad executing in user mode so that the operating system can take care of it but experimentally it turns out that processors are so complicated they do have bugs and these guys actually say well we actually found some evidence that this happens if you have a some complicated instruction the CPU wasn't expecting the CPU will actually halt instead of trapping to the kernel so that seems bad but I guess it's not as disastrous if I'm sort of only running reasonable things on my laptop but it is bad if you visit some web page and your computer hangs so they want basically a stronger level of protection for these native client modules than what you would get out of sort of os level isolation even from a hardware B bug standpoint so that's actually pretty cool they're like really paranoid about uh security uh including even Hardware uh problems all right so any questions about all these alternativ how that works or why these guys are not excited about them make sense all right so I guess let's uh try to look at now how Native client does actually uh decide to s box processes so let me pull up these boards so native client takes this different approach that's uh in general called software fault isolation and the plan is to actually not rely on the operating system or Hardware to check things at runtime but instead to somehow look at the instructions ahead of time and decide that they're always going to be safe to execute so actually you actually look at the binary and you check all the possible instructions to see whether there're going to be safe instructions or unsafe instructions and once you've decided that it's all going to be safe you can just jump in and start executing because you know it's all composed of safe things so it cannot go wrong so we'll talk about exactly what this means but roughly what they're going to do is they're going to look at pretty much every instruction in the binary code that is served up to the browser and they're going to decide that particular instructions are going to be safe or unsafe so what do they do for safe instructions well they're they're just going to allow them uh what's an example of a safe instruction what are they thinking of that doesn't need any extra checks or protection Etc yeah so like any kind of ALU operation basically yeah MTH presumably moves between registers uh and so on so this doesn't actually affect the security of the system as far as their concern because all they really care about is things like memory safety where what code you execute and so on um so as long as you're just Computing on some stuff and registers they don't really care it's not going to affect the rest of the browser so what about unsafe operations I guess these are things that they might care much more about so this is anything that maybe does a memory access or maybe some privileged instruction maybe invoking a system call on this machine um maybe trying to jump out of the sandbox who knows what so for these kinds of instructions they're going to do one of two things if the instruction actually is necessary for the application to do its job like accessing memory seems important then they're going to somehow make sure that they can execute this unsafe instruction safely or if you're jumping around for example in the program's address space then what they're going to do is somehow instrument the instruction and what instrumenting the instruction means is that you used to have one unsafe instruction that sometimes does good things that the application might actually want to do legitimately and sometimes that instruction could do bad things so what instrumenting it means is that you add some extra instructions before it that will check or enforce that only good things are going to happen with that instruction so for example if you're accessing a memory location what you could do turns out they don't do it for performance reasons but one way to instrument A Memory access would be to put some checks like an if statement before the instruction saying well is this address you're accessing in range of what's allowed for this module and if so do they access otherwise exit so that would instrument an instruction and that would turn an unsafe instruction into one that's always safe to execute because there is always that check in front of it so does that make sense this is their sort of big idea for running this enforcing safety without support from the operating system at some level and I guess for other unsafe instructions they don't instrument all of them some of them they actually prohibit if they believe that this is an instruction that's not really necessary during normal operation and if an application tries to run it there we should just kill the application or not allow it to run in the first place makes sense so this is their sort of big plan for software fault isolation and once you've checked the application's binary and It All Passes and everything seems to be properly instrumented then you can just run the program and off it goes and by definition almost it'll not do bad things if we do all these checks and instrumentations right and I guess there's one more part to the software fault isolation story which is that once you've made sure that everything is safe then it can't actually do anything terribly interesting other than compute stuff in its own little memory so it can't access the network can't access the disc can't access your browser display keyboard almost anything so in pretty much every software fault isolation story there's actually also some trusted service runtime and this trusted runtime is going to not be subject to these checks on its instructions so the trusted runtime could actually do all these potentially unsafe things but the trusted runtime is written by Google so hopefully they get it right and it's going to implement all the functions that Google's native client team believes are okay to allow for these modules so these are going to be things like maybe allocating memory maybe creating a thread maybe communicating with the browser as we saw so some sort of a message passing system Etc and the way it's going to be exposed to this isolated module is through certain presumably um special jumps or operations that the uh module is going to perform to transition control into this trusted runtime but hopefully in a very predictable way so question I'm just curious um so do you have to design this application um knowing that it's going to be sent to the NC module or is it like change the code so that it'll make necessary call to the service so I think if you're taking an if you're building an application you do have to be sort of aware that it's going to be running inside of native client um so some function calls like Malo or pthread create I think they just transparently replace with calls to their trusted runtime but if you do anything like opening a file by path name or anything else that sort of you would expect to do on a Unix machine that You' probably have to replace with something else and you'd probably want to structure your thing to interact at least a little bit with your JavaScript or web page in some way and that you would have to do by explicitly doing some message passing or rpcs into the JavaScript part so that you would have to probably change uh so there's some so it's not like you can run an arbitrary Unix program in there and you'll just sort of get a shell and you can run commands all of a sudden probably if you Tred hard maybe you could create such an environment uh but by default you probably have to make it operate inside of this web page and interact with it in some way make sense all right okay so so that's the overall plan for software of isolation so I guess let's look at actually what safety means in their case for Native client so we talked Loosely about this notion of safe instructions unsafe instructions what do they actually care about here so as far as I can tell for a native client safety uh basically means two things one is that there's no disallowed instructions that can execute and uh these disallowed instructions are things like maybe system calls or triggering an interupt which is another mechanism on x86 to jump into the kernel and invoke a system call uh and probably other privileged instructions that would allow you to escape out of the sandbox so we'll look a little bit more later at what instructions could actually let you jump out of a Sandbox and in addition to this no disallowed instructions rule they also um want to make make sure that all code and data accesses are in bounds for the module so what this means is that they actually dedicate a particular part of the program's address space specifically it goes from zero up to 256 Megs in the process and everything that this untrusted module does has to refer to locations within this chunk of memory in the program all right so just a double check so why do they want to disallow these instructions so what if they fail to disallow the instructions yeah module can make a CIS right so that's yeah fairly straightforward exactly yeah so you could just directly reboot the machine or open your home directory and enumerate all the files and do all these things so that seems like a good thing to do why do they care about this plan like isolating the code on data to only access these low addresses so what goes wrong if they fail to do that does anything yeah then they can start accessing interrupting memory that doesn't belong that module like on your computer we don't care if they ruin their own program and it crashes that's fine as long as it's not going to crash the computer that's running yeah so in some sense uh that's true but and in their case they actually run the thing in a separate process so in in theory this would only crash that extra process um I think that I guess what they're really worried about is that this is a necessary condition to ensure there's no disallowed instructions in some way because there's other stuff like this trusted runtime in your process so if what you really care about is not corrupting the rest of your computer then if the untrusted module could arbitrarily jump into this trusted service runtime and do anything that the trusted service runtime can do then they could sort of violate this property so in some ways this is really like a supporting mechanism for enforcing that um in in principle this could also be used for lighter weight isolation if you could run this native client module inside of the browser process itself and not start an extra process but it turns out for performance reasons they really have to tie down the module to this particular range of memory or it has to start at zero anyway uh so this means that you can only have one really native client untrusted module per process so you probably are going to start a separate process anyway make sense other questions yeah is it like is there actually a reason why it has to start at zero yeah so it turns out it's more efficient in terms of performance to enforce jump targets if you know that the legitimate address is a contiguous set of addresses starting at zero because you can then do it with a single and mask where all the High bits are one and only a couple of low bits are zeros well if you're willing well I thought the an mask was to ensure alignment right so the N mask ensures alignment you're right uh so so yeah so why do they start at zero um I think it probably helps them to avoid well I guess they rely on these on the segmentation Hardware so in principle maybe they could use the segmentation Hardware to shift the region up in terms of linear space so maybe it's just that the application sort of sees this range and you could actually place at different offsets in your virtual address space could be um yeah so maybe you could actually play tricks with the segmentation Hardware to run multiple modules in same address space it's also because they want to catch no point exceptions yeah so they want to catch null pointer exceptions but you could sort of do that because the null pointer I guess we'll talk about segmentation in a second but the null pointer actually go is relative to the segment in which you're accessing so if you shift the segment around then you could map a unused zero page at the beginning of everyone's segment uh so yeah it might be that uh you could do multiple modules um I think for well I guess one one reason they probably want to do this is that when they Port their stuff to 64 bits they have a slightly different design this paper doesn't really talk about it and I think in that 64-bit design the hardware itself got rid of some of the segmentation Hardware they were relying on for efficiency reasons uh and they have to do a much more software oriented approach in which case having the low bits does help them uh but in this 32-bit version I think yeah maybe that's true they there's not a deep reason why it has to start at zero fair enough other questions all right so so I guess we sort of roughly understand what uh the plan is or what we want to enforce in terms of safety so how do we do this so let's look at some at least naive approach and see what's how could we screw it up I guess and then we'll try to fix it afterwards so I guess the naive plan that uh you know sort of looks like what they do is is to just look for disallowed instructions by just scanning the executable from the start going forward so how do you detect bad instructions well you could just take the sort of program code and you sort of lay it out in a giant string that goes from zero up to well maybe all the way to 256 megabytes depending on how big your code is and you start looking okay well maybe there's a op instruction here maybe there are some sort of an ad instruction there maybe there's a knot I don't know some sort of a jump Etc and you just keep looking and if you find a bad instruction you say ah it's a bad module and then you discard it and if you don't see any system call or in instructions then you're going to allow this module to run module or whatever it is will do for the inbounds checks so is this going to work so why not what are they worried about why is this so complicated overlapping instruction yeah so the weird thing is that x86 which is the sort of platform they're targeting has variable length instructions so this means that the particular size of an instruction depends on the first few bytes of that instruction so you have to actually look at the first bite and say okay well the instruction is going to be this much more and then maybe you sort of have to look at a couple more bites and then decide okay well that's exactly how long it's going to be but you don't know ahead of time so some architectures like Spark arm uh myips um have more fixed length instructions well on arm for ex arm is slightly weird but it has two instruction lengths either everything is two bytes or everything is four bytes but in x86 the instructions could be like one b or 10 bytes or five bytes anything in between as well I forget actually how big you can get a pretty long instruction I think you can get like a 15 B instruction on x86 if you try hard um it's a complicated instruction set um but uh as a result the problem that could show up is that maybe you're scanning linearly and everything seems to be fine but maybe at runtime you'll actually jump into the middle of some instruction maybe it was a multi-te instruction if you interpret it starting from the second bite it looks like a completely different thing so just one example sort of playing around with an assembler and if you have an instruction like this 25 uh CD 80 00 and if you interpret it as this five byte instruction meaning that you you look at this B and says oh yeah this is going to be a five byte instruction so you have to look five bytes forward and then this turns out to be a fairly benign instruction that's an end of the eax register with some particular constant that happens to be um I think 0000 um 80 CD so this is one of these safe instructions that native client should just allow under the first rule in checking these binary instructions but if it turns out at runtime the CPU decides this is where it has to start executing the code then this instruction is actually a 4 byte instruction and it's actually an in instruction that makes an ox ad0 interrupt which is a way one way to make system calls in Linux so if you miss this fact then potentially you're going to allow this unrusted module to jump into the kernel and make system calls which is what you wanted to prevent make sense so how could we prevent it like one possibility is maybe we should like try looking at every bite offet because you know at least x86 can only start interpreting instruction on a bite boundary instead of a bit boundary um so you like look at every single possible bite offset and you see what instruction starts there is this a reasonable plan could it work yeah I mean what if someone actually is doing in and that they're never going to jump into that and now you're disallowing their program right so it's basically prone to potentially false positives um now you know if you really wanted to you could probably contort yourself and change the uh code a little and somehow avoid this if you knew exactly what the Checker was looking for you could potentially change these instructions you know maybe like end it with one thing first and then end it for with another mask later but to just avoid these suspicious bite patterns but that just seems pretty awkward to do um now it is actually possible Right their architecture does involve changing the compiler so in principle they do have some component that actually has to compile the code correctly it's not like you can you can't just take offthe shelf GCC and compile code for Native client so in principle it's doable but I think they're just thinking it's too much hassle and it's not going to be reliable or high performance Etc makes sense and plus there's a couple of x86 instructions that are prohibited or should be unsafe and prohibited but they're like one bite long so that is gonna be pretty damaging to to look for or to filter out okay so if they can't just disassemble sort of straight up and hope for the best then they need some other plan for doing this disassembly in a reliable fashion so so what is their plan a native client to ensure they don't get tripped up by this variable length encoding so I guess one way to think of it is um well okay so how are we're going to solve this reliable disassembly so in some sense right if we if we really do scan forward from the left to the right and we look for all the possible up codes if that's the way the code executes then we're in good shape so even if there is some weird instruction at a some offset then the CPU isn't actually going to jump there if it actually executes the same way that we're scanning the instructions stream from left to right so the problem with reli with getting the disassembly to be reliable really comes from the fact that there's jumps because if if we execute linearly from left to right then the CPU will follow the same rules that our Checker is following and see the same instruction Stream So the problem really comes down to what happens if there's a jump somewhere in the application could have jump to some position in the code that we didn't observe in our left to right scan so this is what they're sort of going after in the reliable disassembly and the basic plan is to just check where all the jumps go so it's actually fairly straightforward at some level um they have I guess a bunch of rules we'll look at in a second but roughly the plan is if you see a jump instruction then it actually has to be uh well you have to check that the target was seen before so you basically do the left to right scan that we sort of described in our naive approach here but then if you see any kind of a jump instruction then you see what the what is the address to which the jump instruction is pointing to and you make sure that it's an address that you saw in your left to right disassembly so if there's a jump instruction for example that goes to that CD bite then we're going to flag that jump as invalid because we never saw an instruction starting at the CD bite we saw a different instruction but if all the instruction if all the jumps go to the start of instructions we saw then we're in good shape that make sense so the one of course problem is that you can't check the targets of every jump in the program because there might be indirect jumps so for example in xd6 you could actually have something like jump to the value of the eax register so this is great for implementing function pointers you store the function pointer somewhere in memory then you load the function pointer into some register at run time and then you jump to whatever address was in the that memory location and register so how do these guys deal with these indirect jumps so they have no idea if it's actually going to jump to the CD b or the 25 byte what do they do yeah yeah so this is their basically main trick here is instrumentation so whenever they see a jump like this what they actually what the compiler is going to generate is a proof that this jump is actually going to do the the right thing and the way they actually do this is well they don't actually know it's actually kind of hard to put in a proof here that it's one of the addresses that you saw during the left to right disassembly so instead what they do is they want to make sure that all the jumps go to multiples of 32 bytes and the way they do this is they actually change all the jump instructions into something that they call a pseudo instruction so there's still that sort of jump to you know gu the eax register at the end but they prefix it with an end instruction that uh is going to clear the low five bits F uh 1 2 3 4 5 6 e0 with the eax register so this and instruction clears the low five bits which means that it forces this value to be a multiple of 32 2 to the 5 and then you jump to it so if you look at it during verification time then you can sort of convince yourself that this instruction this in this instruction pair will only jump to a multiple of 32 bytes and then in order to make sure that this is not going to jump to some weird instruction you're going to enforce an extra rule which is that during your disassembly when you're scanning the instructions from left to right you're going to ensure that every multiple of 32 bytes is a start of a valid instruction so in addition to this instrumentation you also are going to check that every multiple of 32 is a valid instruction and well what what I mean by valid instruction here is it's an instruction that you see when you disassemble from left to right yeah2 well yeah so why do they choose 32 that seems like some magic number they pulled out of a hat like should you choose a thousand or five any comment okay like should we choose five is okay so why is five bad power of two yes okay so we want a power of two okay so good point because otherwise like ensuring something is a multiple of five is going to require a couple of instruction here which is going to lead to overhead okay how about eight is eight good enough yeah you can have instruction long yeah okay so it has to be at least as long as the longest x86 instruction you want to allow so if there's a 10 BTE instruction everything has to be a multiple of eight well you're kind of screwed with that 10 B instruction there's nowhere to put it um so it has to be at least as long so 32 is pretty good like the biggest I could find was 15 bytes so it's probably good enough yeah be longer than that I think it's called the trampoline code they have to like you know have a he instruction at the beginning second so you can't just jump directly into there like code and then they also need REM jump instruction that like jumps to you know another like that's right yeah so you have to be able to fit for their entry and exit from The Trusted service run time you need to be able to fit some non-trivial amount of code in a single 32 byte slot we'll see how this works in a second or even 31 byte slot as you're pointing out for one hold instruction um should it be much larger should we make it I don't know a th by well 1024 problem your too SP because you have jum yeah so if if you if you have a lot of function pointers or lots of sort of indirect jumps then every time you want to create some place where you're going to jump two you have to Pat it out to the next boundary of whatever this value is so with 32 maybe that's okay you're like well worst case you waste 31 bytes because you just need to fast forward to the next boundary but if it's a multiple of a 1024 then all of a sudden you're wasting a kilobyte of memory for an indirect jump and if you have probably you know short functions or you know lots of function pointers then this might actually be a memory overhead all of a sudden as well do that make sense roughly so yeah I think the 32 is not like set in stone like native client has to have 32 byte blocks but you know something on that order probably works 16 is probably a little short on the other hand like 64 could work 128 maybe it's getting a little longish but it's I don't think you can derive 32 from first principles make sense all right so that's sort a plan for Reliable disassembly and as a result sort of the the compiler has to be a has to be a little bit careful when it's compiling your C or C++ code into a native client binary it has to basically follow these rules so whenever it has a jump it has to instru add this extra instruction in front and whenever it's creating a function that it's going to jump to or an instruction is going to jump to as we were talking about it has to Pat out and it can just Pat it out with zeros because all those have to be valid up code so it actually has to Pat it out with knobs just to make the validator happy just to make sure that every possible instruction is a valid one and luckily on x86 KN is a single bite so or at least there is a no that's a single bite there's many NOS on x86 uh so you can always pan things out to a multiple of whatever this constant is make sense all right so so what does this guarantee to us I guess what what what what this makes sure is that we always see what happens in terms of the instructions that will be executed so this finally gets us this rule so we can be sure there's never a system call being issued what about so this is for jumps what about returns how do they deal with returns can you return from a function in Native client gu what would happen if you ran a red op code that' be good yeah po the stack you don't want theode to be modifying your overall St well so it's true that it pops the stack but the stack that native client modules use is actually just some data inside of their section so you don't actually care well the native client runtime doesn't care whether this guy screws up their stack overflows their stack or what but but you can put anything on the stack and when you return you jump for that that's true yeah so return is almost like an indirect jump from a particular weird memory location that's the top of the stack so I guess one thing they could do for returns is maybe prefix it with a similar check where maybe you like pop the top thing of the stack you check whether it's valid and then you WR maybe you somehow do an and to a memory oper and that's the top of the stack um that seems a little fragile um partly because of race conditions because for example if you look at the top location on a stack and you check that it's okay and then you do a r later another thread in the same module could modify the thing at the top of the stack uh and then you would be returning to a bad address could this not be the case with the jumps as well yeah so what happens with a jump could a race condition somehow invalidate this check yeah notable well so the code is not writable that's true so you can't modify the and but could another thread modify this jump Target in between these two instructions yeah yeah so that's the cool thing there there's basically well if if it modifies it in memory or wherever it loaded into E from sure it could do it before you load in which case this e will be bad but then will clear the bad bits or it could modify it after at which point it's already in eex so it doesn't matter that it's modifying the memory location from which the E register was loaded and threads don't actually share the register sets so if another thread modifies the eax register it will not affect this thread's eax register so this instruction actually is sort of race proof in some sense other threads can't invalidate this instruction sequence make sense all right so here's another interesting question um could we bypass this and like I can jump around on this address space all I want and when I'm disassembling this instruction this seems like a perfectly fine pair of instructions an and and a jump and the check for static jumps is that well it just has to point to some Target that was seen so yeah we saw an end that's one instruction that's valid we saw a jump that's another valid instruction so when I see a direct jump up here maybe I you know jump to some address I don't know 1 2 3 7 that's actually okay even though it's not a multiple of 32 native client doesn't General allow direct jumps to arbitrary addresses as long as that address is a instruction that we saw during this assembly as we were just talking about so could I put in this very nicely checked indirect jump and then later on in the code jump to the second instruction in that sequence so then I'll load something into e jump here and then directly jump into this unsandboxed address so that would violate the security right did everyone see that so how how is this avoided well the any should and jump as a single instruction yeah so this is why they call this thing a single pseudo instruction and even though at the x86 level they're actually distinct instruction as far as the knal validator thinks of it it's actually a single Atomic unit so as far as this check for whether it's an instruction that you saw before they think oh this is the only instruction that I saw before jumping in the middle it's like jumping in the middle of this guy it's the same thing so they basically enfor slightly different semantics than what x86 enforces in terms of what an instruction is and this might actually mean that you can't represent certain instruction sequences in knle so you know if you actually had legitimate code that looked like this before this is going to be turned into a single op code and knle but hopefully that's not a problem yeah presumably they could put that in the the trusted codebase in the start of the the text segment because that way all you would always replace those two instead of putting those in the binary that you produce anyway you just jump straight to that jump Target that's in the privileged section in this trusted run time you mean or something no so in the the first 64k the yeah they have these trampolines and yeah right so they could make that one of the trampolines except that it doesn't jump out it jumps back in so that way the it wouldn't need to be a superu it just be a single x86 jump into there which would then jump out that's true yeah so so that's another sort of clever solution that you could do is instead so does everyone sort of see what what the plan is so suppose that you didn't want to do this pseudo instruction trick and you just wanted to have a single instruction replacing the jump eax well what you could basically come up with is a library of all possible indirect jumps you could ever do so like well there's jump eax there's jump ebx and so on and you would construct a library of these guys for each of them you would construct the safe check that you want to replace them with so for this one you would put an and in front of it and so on for this one You' also put an end in front of it and so on and then in the compiled binary every time you want to jump to eax what you actually do is you actually jump to a fixed address that corresponds to this helpful piece of code stored somewhere in that low 64k of the program and then this guy will do the end and jump again the reason that they probably don't do this is performance for this sort of interesting reason so the Intel processor and actually most processors these days um have have to predict where the Branch goes to keep the pipeline of the processor full at all times so there's a really complicated Branch predictor that not only decides whether you're going to Branch or not like if statements but it also actually guesses where you're going to jump so if you see an indirect jump it actually is going to guess what the address is going to be and this is actually a guess that's stored per cach line storing the jump instruction so if you have a single place where all the jump EXs come from then the CPU is always going to be very confused because these jump EXs seem to be going all over the place everywhere whereas if you really tie to a particular jump instruction it'll be a better prediction but that's just a sort of performance uh trick that they play make sense all right so so I guess we roughly understand how it's going to disassemble all the instructions how it's going to prevent these instructions so now let's look at the set of rules they are going to enforce so there's this table in the paper table one that has all these different rules for the validator to follow or that the binaries have to follow and the validator checks so let's go through these rules and just double check that we understand why all of these rules are there so so they have these things C1 all the way to C7 so C1 basically says that once you load the binary in memory then the binary is actually not writable at the page table level so they map they set the permission bits for the binary to be non-writable so why should be fairly obvious hopefully so I guess the the the reason is that their whole security plan relies on them checking that your binary is correct so once they've checked it they want to make sure you can't modify the binary and have it do something illegal uh that they prohibited so this is I think reasonably clear then so C2 is their plan that basically has to you know linked at zero and start at 64k so this requirement I think doesn't actually have to do with security so much I think it's just for convenience because they just want to have a standard layout for the program in some sense it's for simp licity so that they don't have to deal with complicated relocation records which means that there's fewer things that the validator or loader would might screw up but basically this is a sort of a standardization plan for them in terms of how to load their um executable make sense all right so the third requirement um is that I guess their indirect jumps use the Pudo instruction so the the Pudo instruction is this thing above so that we just talked about why it needs to ensure this so that you don't jump to the middle of some instruction or sort of somehow invoke you know CIS Call Etc make sense all right so what's going on with C4 so basically you have to pad out to page boundary with a halt instruction why do they want to pad their binary out with halts any ideas yeah maybe like like instead yeah I think um so I I I don't actually I have to admit I don't have a crisp answer for why C4 exists but I think it's roughly what you're saying which is that if you well this code module stops at some point there's some end to it and the question is what happens when when you just keep executing and you get to the end then the processor might just keep executing past the end and execute some extra instructions or maybe it wraps around in some weird way so they just want to make sure that there's no ambiguity about what happens if you keep running and you don't jump and you just run off the end of the instruction Stream So this just make sure that the only answer there is if you keep executing you'll Halt and that'll trap into the runtime and you'll terminate the module so think it's just sort of a Simplicity safety guarantee there uh I don't think there's a concrete attack that I could at least construct in my head yeah but uh here's the thing you can't jump past the end so the last thing that you could jump to is the last instruction and by this rule that instruction must be halt so you could jump to the halt but then you'll immediately Halt and you won't ran past past the end either so I think it's just sort of a cleanliness uh as far as I could tell again guante for them to make sure that there's no weirdness in terms of wrap around at the edges all right so then the the C5 is basically no instructions that span the 32bit bound the 32 byte boundary so this is sort of a variant of this rule that we talked about before where every uh multiple of 32 must be a valid instruction that we saw during disassembly so otherwise we'll jump to the middle of an instruction and have the problem with that csol that might be hidden there okay and then we have C6 which is basically all instructions reachable by disassembly from the start so this ensures that we see every instruction we can check every instruction that can possibly be run at runtime and I guess C7 is this basically all direct jumps are okay so this is an example of that jump up there where you code in the Target right away so it doesn't have a multiple of 32 but it still has to be a valid instruction that we saw during the left to right disassembly question what's the difference between C5 and C3 yeah so I think C5 says that if I have a instruction that's multiple bytes it cannot uh span that 32 byte boundary so suppose that I have uh you know my instruction stream and here's you know an address 32 here's address 64 and maybe I have some sort of an end instruction that sort of spans this boundary so this is what C5 prohibits no instructions can span this boundary because otherwise we saw this s instruction but because you can always jump to 32 by multiples you could jump into the middle of the end and who knows what will happen there so this is what C5 prohibits for them and C3 is the sort of counterpart to it on the jump side so C3 says that whenever you jump you're going to jump to a multiple of 32 bytes and C5 says that everything you can find at a multiple of 32 bytes is a safe instruction I'm wondering if there's any redundancy between some yeah I don't know I I'm not sure I I had a little bit of that feeling when reading this list it seems like well they're all good rules but I'm not sure it's the minimal non-orthogonal set of or set of orthogonal rules that that uh um you need to enforce this um but any other questions about the set of restrictions they have all right so I guess let's uh think about uh this homework question that we sort of assigned it turns out I think there was actually a bug that native client had in its inner sandbox at some point which is that for some complicated instruction I believe they didn't have the length encoding correctly if I remember properly uh in which case something bad might happen I don't remember exactly what the bug was but uh suppose that the inter sandbox validator gets the length of some instruction wrong what what bad things could happen how would you exploit this we can hide this call or R and eventually yeah so suppose that you know there's some you know fancy variant of some and instruction that you could write down and maybe the validator gets that wrong and thinks that ah this guy is actually six bytes when in reality it's five bytes long so you could plop this down plop this and down and then the validator thinks oh it's six bytes so I'll look six bytes further and I'll check whether this is a valid instruction and we just have to make sure that whatever is six bytes later looks like a valid instruction but the CPU itself when it runs this code is going to maybe only look five bytes since from the end because that's the real length of the instruction so if we can use this extra bite to stick a sisol instruction then we could be in business so if we stick that you know remember from that example above on x86 Ox CD is how you make an in instruction so if we stick a CD bite at the end of that and then maybe we can put something here that looks like an instruction but actually is going to be part of the in instruction here and then we could all of a sudden make a CIS call and escape the inner sandbox make sense any questions so the validator in Native client has to be really in sync with what the CPU is doing because it's sort of guessing well here's exactly how the CPU is going to interpret every instruction it has to be right at every level here um so that's going to be a little bit tricky to do right um there's been actually sort of other interesting bugs in Native client that people have found one I think has to do with not properly sanitizing the environment of the CPU when you're jumping into the trusted service runtime so I guess we'll talk about this in a second but the trusted service runtime is going to basically run with the same sort of set of CPU registers initially that the untrusted module was running with so if there's something that it forgets to clear or reset or whatnot then the trusted service runtime might be tricked into doing something uh that wasn't meant to do or the developers didn't want to do it initially okay so let's see where are we now so what we understand now is roughly how we can disassemble all the instructions and how to prevent disallowed instructions so now let's look at how do we keep the memory references for both code and data within the bounds of the module so for performance reasons the native client guys actually start using some Hardware support at this point to make sure this actually doesn't impose much overhead but before we look at the hardware support that they use does anyone have any suggestions for how we could do it actually without any hardware support could we just enforce all the memory accesses going in bounds with the Machinery we have so far yeah we can instrument the instruction is to clear all the higher that's right yeah so actually we see we have this instruction over here which at every time we s for example jump somewhere right now we clear the low bits but if you want to keep all the possible code that you're executing within the low 256 Megs then we could just replace this with a zero so we end with zero FFF Fe zero so this makes clear as the low bits and also caps the address to at most 256 Megs so this does exactly what you're sort of suggesting and would make sure that whenever you're jumping you're staying within the low 256 Megs and the fact that uh I guess you're we're doing disassembly you can also check for all the direct jumps that they're all in bounds and that's actually not so bad right the reason I think they don't do this for code is because in x86 you can very efficiently encode an and where all the top bits are one so this turns out to be I think uh basically a three bite instruction for the end and a two bite instruction for the jump so the overhead is like three more bytes but if you want to have non one Pi bits then this is actually now a five byte instruction all of a sudden so I think they're worried a little bit about the overhead here question there also the problem that you might as some instructions which like increment the like location that you're trying to get so you might say like your instruction might have like a constant offset or something like that well so I think yeah you would probably prohibit instructions that jump to some complicated formula of an address so you only support an instruction that jumps directly to this value and this value always gets anded and more for memory accesses rather yeah okay so yeah so that's a good point because this is just code and for memory access there's lots of weird ways on x86 to refer to a particular memory location and in that case you'd basically have to first compute the memory location then insert an extra end and then do the access and I think that's the real reason why they're actually very worried about the performance overheads of this instrumentation uh so on x86 at least on 30 two bit which is what this paper talks about they actually use some Hardware support instead to limit the code and data addresses that the untrusted module can refer to so this actually leverages some somewhat esoteric Hardware in x86 so let's see what it looks like first before we start figuring out how we're going to use it to sandbox the Naco module so this Hardware is called segmentation and sort of left over from from the days before x86 actually even had paging so the way it works is actually um there's a segmentation on x86 the way it works is that whenever a process is running um there's actually a table maintained by the hardware uh that's called a segment descriptor table and what this table has is just a bunch of segments numbered from zero up to whatever the size of the table is kind of like file descriptors in Unix except every entry has two values in it some sort of a base and a length and same for every entry base and length Okay so what this table does is tells us uh that we have you know a couple of segments and whenever we refer to a particular segment uh what this in some sense means is that we're talking about a chunk of memory that starts at address base and goes for this length from that base upwards and the way that this actually helps us to enforce memory bounds is that on x86 every single instruction in one way or another um whenever it's talking about memory actually talks talks about memory with respect to a particular particular segment in this table so for example when you actually do something like move a a memory value from the value from a pointer stored in the eex register into maybe another pointer stored in the ebx register what you would sort of think it does is it figures out well what's this address loads memory at this address and then figures out okay what's this address and stores the value there but in fact on x86 there's whenever you're talking about memory there's an implicit what's called a segment descriptor which is kind of like a file descriptor in Unix it's just an index into this descriptor table and unless sort of specified otherwise every op code has a default segment in it so in when you're doing a move this is actually relative to the DS or the data segment register so it's like a special register in your CPU that's a 16bit integer if I remember correctly and that 16bit integer points into this descriptor table and the same here here this is actually relative to that DS segment selector actually a bunch of these guys there are six segment selectors on x86 there's the code selector CS DS es FS GS and SS and the code selector is sort of implicitly used to fetch the instruction so if your instruction pointer points somewhere it's actually relative to what the Cs segment selector says and most data references implicitly use either DS or ES and then fs and Gs are like some special things and SS is always used for the stack operations if you push and pop they implicitly come off of this segment selector it's like a fairly Baroque Machinery but it turns out to be hugely useful for this particular use case because what happens is if you access some address at you know maybe some selector DS you know callon some offset or some address here what the hardware will actually do is translate it into well it'll put this address and it'll add the table of DS base to this guy and it'll actually take the address modul the length from the same table so whenever you're doing a memory access it's actually going to add the base of your segment selectors sort of descriptor table entry and take your address that you're actually specifying and mod it with the l of the corresponding segment does this make sense it's a little Baro but that's what the hardware gives us so why isn't this use for example for like buffer protection yeah a good question so could you use this for protecting against buffer overflows so how what's the plan like you could basically set up uh a for every buffer that you have you could put the Buffer's base here you could put the size of the buffer there you don't need to there before you want to write to it you wouldn't need it there conly yeah so I think the reason that this isn't used so much for protecting against buffer overflows is that this table has at most two to the 16 entries because these descriptors are actually 16 bits long and in fact a couple of the bits are used for other stuff so in fact I think you can only stick like two to the 13 entries here so if you have more than two to the 13 variable size array things in your code then that's probably going to overflow this stable it actually a little bit weird for the compiler to manipulate this table because the way you actually manipulate it is through system calls so you can't actually directly write to this table you have to issue a system call to the operating system and the operating system will put this table into the hardware for you so I think most compilers just don't bother having this complicated story for managing buffers multic actually did this though so like on multic you actually had two to the 18 distinct segments and two to the 18 possible offsets within a segment and every sort of possible like shared Library chunk or chunk of memory was a distinct segment and then they would all be range checked not maybe at the variable level but still yeah presumably it's also a bit slower if you have to chop into the kernel all the time to that's right yeah so there's also the overhead uh I guess to set this up there would be some overhead or if you create a new buffer on the stack all of a sudden you have to call in to this guy and uh uh add an extra entry so yeah it's U it is nice machinery and but it's mostly used for coarser grain things because of the overheads of changing it a bit make sense all right so how do these guys actually use now the segmentation Machinery well you can sort of guess what how it works I guess by default all these segments in x86 have a base of zero and a length of 2 to the 32 so you can access the entire range of memory you could possibly want so for Native client what they do is code in a base of zero and a length of 256 Megs and then they Point all these six segment selector registers to this entry for a 256 Meg region So then whenever the hardware does a memory access it's going to modit the the offset with 256 Megs and it'll be restricted to that 256 Meg range of memory that's allowed for the module to read and modify make sense all right so I guess we sort of roughly understand now this Hardware support and how this works uh and how you could potenti do it without segment selectors um so if we just implement this plan um is there anything that could go wrong like could we escape out of this segment selector in the untrusted module I guess there's one one thing you have to watch out for is that these registers are just like regular registers and you can actually move values in and out of them so you have to make sure that the untrusted module now doesn't tamper with these registers the segment selectors because somewhere in your descriptor table is also the original segment descriptor for your entire process which has a base of zero and a length of two to the 32 so if the untrusted module could somehow change the Cs or DS or ES or any of these guys to point to this original operating system segment that covers your entire address space then you could then do memory references with respect to this segment and get out of this sandbox so as a result native client has to add some more instructions to this prohibited list so I think they basically prohibit all instructions that you know move into a segment selector DS es Etc so that once you're in the sandbox you cannot change the segment that you're referencing things with respect to make sense yeah the segmentation AR privil yeah so it turns out that on x86 the instructions to change the segment descriptor table are privileged but changing these sort of es into the table are completely unprivileged yeah sense other questions yeah can you initialize the table to put all zero lens in the unused slots well yes so the the unused slots uh you could yeah you can set the length of the table to something so there are no unused slots it turns out that you actually need this extra slot containing zero and 2 to the 32 because the trusted runtime is going to need to run in this segment and access the entire memory range so you need this entry in there for the trusted runtime to work question in order to set this in order to set the table out no actually it's pretty cool there's like something that you don't have to be root for on Linux there's actually a system called I think it's called modify unor ldt for local descriptor table and it lets any process modify its own table these tables are actually per process well as everything in X is actually more complicated there's a global table and there's a local table table but the local table is per process you can modify that one uh at least yeah Mak sense all right okay so I guess one thing we could uh now try to figure out is how do we jump in and out of the Native client runtime or or out of the sandbox right so what does it mean for us to jump out so we need to run that trusted code and the trusted code lives somewhere up above the 256 Meg limit and in order to jump there we basically have to undo all these protections that native client sets in place and they basically boil down to changing these uh selectors so we already I guess our validator isn't going to enforce the same rules for the stuff above 256 Meg so that's easy enough but then we need to somehow jump into the trusted runtime and reset these segment selectors to the right values to this sort of giant segment that covers the entire process address space so the way that works on uh Native client is through this mechanism they call trampolines and spring springboards so all these guys are things that uh live in the low 64k of the process of the module um and the cool thing about that is that these are going to be sort of chunks of code living that low 64k of the process space so that means that this untrusted module can actually jump there because it's a valid code address it's going to be on a 32 by boundary potentially and it's going to be within the 256 Meg limit so you can jump to these trampolines but the native client runtime is going to actually copy these trampolines from somewhere outside so the native client module isn't allowed to supply its own trampoline code the trampoline code comes from The Trusted runtime so as a result it can actually contain some of these sensitive instructions like moving DS and CS Etc that the untrusted code itself is not allowed to have so the way you actually jump out of the sandbox and into the trusted run time to do something like malok or create a thread is you jump to a trampoline which uh lives at a 32 by offset so maybe it's an address well who knows maybe it's 4096 plus 32 and it's going to have some instructions to basically undo these segment selector so what it's probably going to do is it's going to you know move some value I don't know maybe seven into the DS register and maybe 7 point to this entry here that's allowed for the entire 2 to 32 address space and then you're going to effectively move Cs and then jump somewhere up into the service runtime and this is basically going to be past 256 Megs so there's going to be this jump in here that's not regularly allowed but we're going to be okay with it because it's going to be into a point in The Trusted service runtime that is expecting these jumps and it's going to perform proper checks afterwards uh on the jump on the arguments and whatever else that is being passed around and we can actually do this move DS here because we know that it's actually safe the code we're going to jump to isn't going to do anything arbitrary or funny with our untrusted module make sense roughly what's going on so why do these guys bother jumping out of this segments like why not just put the whole thing in in the trampoline seems like more work in some level yeah only have4 yeah you don't actually have a lot of space and well I guess you have 64k so that's potentially maybe enough for like maybe you can Implement maloc in there but the problem is not so much the 64k I think but this 32 by restriction and it's actually not a restriction on The Trusted code because the trusted code can do whatever it wants here it's not going to be checked the problem is that the untrusted code can jump to every 32 byte offset so every 32 white offset has to be prepared to be very suspicious of its arguments so you probably are going to be are going to have a hard time writing this code here with every 32 by rechecking the arguments and values and so on so basically you have to jump out of the trampoline and into the runtime up here within 32 bytes of code so then if you jump to the next thing then well something else is going to happen here but it's not part of this same trusted routine here sense okay so this is how you sort of jump out of the uh sandbox to jump back in you also need to you basically need to invert the these Transformations you need to sort of set the DS back and CS back and so on and the tricky thing is that if you're running outside of this um 256 Meg limit if you're running inside of the trusted runtime then you can't really reset these registers yet because otherwise you won't then be able to access any of the memory in your sort of space outside so what they actually do is they have this thing called a springboard which is how the trusted runtime from outside the 256 Meg limit is actually going to jump back into the native client module so here it's going to reload the DS register with maybe whatever that limited segment descriptor is maybe let's say it's one and then it's going to reset other things and then it'll jump to you know whatever address the trust runtime wants to return to in the untrusted module Mak sense so this is how you sort of jump back in and the only sort of tricky piece here is that you don't want the untrusted code to jump into the springboard itself maybe something weird will happen who knows so what they do is they actually put a halt instruction as the first bite of this 32 by multiple sequence so if you jump into the middle of to the beginning of this guy you'll immediately halt the trusted runtime is going to jump to one past this bite and be able to execute the springboard but this is something that only The Trusted runtime can do because regularly check this is not going to be allowed question is a springboard in the untrusted module so the springboard is within the 0 to 56 Meg part of the untrusted module but it actually lives in that 64-bit chunk at the very beginning that doesn't actually come from the binary you download from some website but it's actually patched into it by the native client runtime when it first loads this modu memory why not just have it in the runtime yeah so why not have it in the runtime so what happens if the runtime was allowed to supply the springboard is this bad yeah how how would it know how would it know where to jump back to well so actually I think what this actually is is it actually jumps to something like eax so the trusted runtime says oh I want to return to this address it puts it in the eax register jumps here the springboard does this this this and then jumps to eax wherever the trusted run time set to jump so what if the module came with its own springboard um well in theory you could do it as like a natural jump type thing but it shouldn't know anything about the descriptor table like that's like a hardware yeah so actually this is like a really important instruction for sandboxing the fact that we reload that descriptor to point at one of those limited descriptors up there it's really important and if the module was allowed to supply its own springboard maybe it'll just skip this part and then not restrict itself back to 26 Megs so then once you jump through the springboard you'll be able to still access the entire process address space so it's actually the springboard is part of the enforcement mechanism it sort of sets up the boundaries so this is the reason why I think they don't want the springboard to come from the developer now yeah question put the spring board p the 256 me so I think uh they don't want to put the springboard past the 256 Megs because then you might have trouble jumping down so you want to jump to a particular address but you also want to set some extra registers here so if you're I think this basically has to sorry this has to do with setting that CS code descriptor segment because you want to set the code descriptor segment to this bounded segment and you want to jump to some particular address at the same time so I think it's easier for these guys to do it through a springboard because you first sort of jump to here then you can set your CS value but you can still execute the same code you're still running uh because you're within the 256 bound make sense hold bit I think it basically has to do with what Atomic Primitives the hardware provides to you so you basically you want to set a whole bunch of these DS segment selector registers and the Cs register and jump to somewhere at the same time so it's sort of this is one way to for them to do it sense I think that's maybe not as fundamental I you probably if you if you tried hard you could probably come up with some x86 instruction sequence that could do it from outside the uh bounded address space of the module make sense all right so I guess that's it for uh Native client uh and I see you guys next week we'll talk about web security starting then
okay so now we're going to start with a simple bar plot of the MIT international student data so first let's load the ggplot library ggplot2 and load the data frame so int L is read CSV int L dot CSV okay now the structure of its data frame is very simple there are two columns two variables the first one the region and the second one is the percentage of international students who came from that region so making a bad plot from this data isn't too hard well you start off of a GG plot command of course the first argument being the data frame the aesthetic in this case is to have region on the x axis and on the y axis to have the percentage of international students now the geometry we want to use is as you might guess by GM bar we have to profess one argument to this young bear and it's called stat identity I'm going to come back and explain what that means and I also want to label my bars with their value so it's easy to read in closer detail so I only use geum texts to do that and the acidic of our text is simply to have the value of a label the mr text available to be the value of percentages so let's look at that okay so yes we have a bar for each region the values are between 0 & 1 which looks kind of strange the labels are actually lying over the top of the columns which isn't very nice and the regions aren't ordered in any way that's useful they actually uttered in alphabetical order but I think would be much more interesting to have them in descending order so we're gonna work on this first of all though what is this stat identity it's pretty simple geometry bow has multiple modes of operation stet identity says use the value of the Y variable as is which is what we want the height of the bar is the value of the Y variable now there are other modes including one that counts for number of rows for each value of x and plot set instead so you can look at the documentation of a g-tube plot to see the different options now they work but stet identity is what we want right now now x axis is out of order and the reason for this is that ggplot defaults to alphabetical order for the x axis what we need to do is make a region and ordered factor instead of an unordered vector we can do this with the reorder command and the transform command so let's write this out so we're going to transform the international data frame and what we're going to do is say region it's going to be a reordering of itself based on decreasing order of percentage of int L so if we look at the structure of a data frame now we see if is something something going on in the region column that wasn't going before and that's that ordering so you might have also noticed that I put a negative sign in front of percentage of int L so that negative sign means decreasing order if we had left that out I would have actually ordered them an increasing order so unknown or stateless would have been first and Oceania would have been second and so on okay so that's one thing fixed another thing we didn't like was that the numbers are between 0 & 1 which kind of won't looks looks a little bit messy so let's just simply multiply all the values by a hundred so percent of int l equals int L percent of int L multiplied by 100 all right and now the other things we had to fix like the text overlying and XX is being all bunched up like that we're gonna do that energy cheap new ggplot command so I'm gonna blow right break it across multiple lines so we start off with the ggplot command as we did before actually identical to here before so the acidic is x-axis on there is a region and the y-axis is a percentage of international students we break her into multiple lines so put the plus at the end there and press shift enter we're gonna do a bad plot the acidic sorry the stat is identity as before and this time that we're going to manually specify a fill I'm gonna say dark blue quite like how that looks now we need the text and the acidic of that is to have the label equal the value of the column I'm gonna add one more thing to this I'm gonna say V just equals negative 0.4 and what this does is it moves the labels up a little bit and off the top of the bars now you can play with that so a positive value will move it down and a negative value will move it up next I'm gonna set the wire label excess label to be something a bit more sensible so percentage % of international students and finally I'd like to fix up that x-axis so I want to get rid of the word region because it's pretty obvious that these are regions and I also want to rotate the text on a bit of an angle so you can read it all pot like this so that's none of the theme command sort of a theme theming we're gonna do is we're going to say the axis title is the x-axis should be blank okay and that the access text on the X x axis should be rotated so it's a text element it's angle is 45 I don't move it side way it's just a little bit it's just one and there we go so we've got our labels V justed above the columns the bars themselves the dark blue the numbers now between 0 and 100 instead of 0 & 1 we can read all the text labels and it's generally a lot more readable than the pie graph pie pie plots or our original ggplot at that so let's go back to the slides now and talk about what we've just done
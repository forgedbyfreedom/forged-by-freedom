first let's make sure we have ggplot2 loaded so library ggplot2 okay now let's load our data frame which is in households dot CSV so read dot CSV households does CSV okay if we look at the structure of households we see that there is a year column and then six other columns for each of the different household types so this is actually a problem for us given this structure of a data frame what would we put in the acidic for our ggplot command it's not obvious to me at least and in fact I don't think it's really possible the reason is that ggplot needs it in the form of yeah group and fraction the solution is to use the melt function from the reshape package melt will take a 2-dimensional data frame like oz and convert it into exactly the right form we need the ggplot2 so fast let's low to reshape to the library appreciate to now let's look at the first two columns of a household's data frame so households all rows first two columns okay so there's a year and a married of child for each year now let's look at the first few rows of a melted households data frame so head of melts households we will use the year as the first column of a melted data frame and there you have it so basically what's happened is that each value of married or child has turned into his own road in the new data frame to make it more clear perhaps let's look at the first three columns of households now we have married without child now let's look at instead of just the first few rows of ML two data frame let's look at the first ten rows so rows 1 to 10 all columns whoops there we go so there you can see the eight values of married child and the first two values are married without child so there's that 30.3 up there gone down to three point three here twenty nine point nine gun two down here so every value in a data frame now are corresponds to a new row in our melted data frame which is exactly what we need for ggplot so let's try plotting this melted data frame ggplot melt households using the column as the kind of ID column key column parasitic is going to be now to use here on the x axis y axis will be the value column of a melted data frame and the color of a line would append on the group which is called variable and the melted data frame variable okay so that's our acidic aisle geometry is going to be lines and i want to make the lines a little bit thicker so let's say line size is two and i also want to have points for each hdand of a data frame so i'm gonna have lions endpoints so g um point I'll make the size of these a little bit bigger vanilla two sizes five and we should put a y-axis label so y lab let's call it percentage of households and there you go now this is actually quite interesting when we compare it back to the chart we had in the slides now you can see just how quickly married a child is decreasing as a relative share you can also more clearly say that Marik's even married to that child is pretty much flat and that the difference is being made up by the other four types of household steadily increasing over the years so there you have it same data plotted in two different ways now I'm not saying one of these is better than the other one for example if I want to compare inside a given year say 1970 it's not the most easy thing at a glance to see just how much of each that's just so much of a total 100 percent it's taken up by each but if I want to see across years it's far superior and I can clearly see if in the last data point is pretty much right next to the singham's last data point which is something that was hard to tell the ever other visualization so I hope this is a major you thinking a little bit more about different ways you can plot the same data and hopefully improved your ggplot2 skills a little bit more thanks for watching
the most straightforward way to improve performance is to reduce the propagation delay of a circuit let's look at a perennial performance bottleneck the ripple carry adder to fix it we first have to figure out the path from inputs to outputs that has the largest propagation delay in other words the path that's determining the overall TPD in this case that path is the long carry chain following the carry in to carry out path through each full adder module to trigger the path add minus 1 and 1 by setting the a inputs to all ones and the B input to all zeros except for the low order bit which is 1 the final answer is 0 but notice that each full adder has to wait for the carryin from the previous stage before it produces 0 on at some output and generates a carry out for the next full adder the carry really does ripple through the circuit as each full adder in turn does its thing the total propagation delay along this path is n minus 1 times the carry end to carry out delay of each full adder plus the delay to produce the final bit of the sum how would the overall latency change if we say doubled the size of the operands in other words made n twice as large it's useful to summarize the dependency of the latency on n using the order of notation to give us the big picture clearly as n gets larger the delay of the xor gate at the end becomes less significant so the order of notation ignores terms that are relatively less important as n grows in this example the latency is order n which tells us that the latency would be expected to essentially double if we made n twice as large the order of notation which theoreticians call asymptotic analysis tells us the term that would dominate the result as n grows the yellow box contains the official definition but an example might make it easier to understand what's happening suppose we want to characterize the growth and the value of the equation n squared plus 2n plus 3 as n gets larger the dominant term is clearly N squared the value of our equation is abounded above and below by simple multiples of N squared except for finitely many values of M the lower bound is always true for N greater than or equal to zero and in this case the upper bound doesn't hold only for n equal to zero one two or three for all other positive values of n the upper inequality is true so we'd say this equation was order N squared there are actually two variants for the order of notation we use the theta notation to indicate the G of n is abounded above and below by multiples of F of M the Big O notation is used when G of n is only abounded above by a multiple of F of M here's a first attempt at improving the latency of our Edition circuit the trouble with the ripple-carry adder is that the high order bits have to wait for the carryin from the low order bits is there a way in which we can get the high half of the adder working in parallel with the low half suppose we wanted to build a 32 bit adder let's make two copies of the high 16 bits of the adder one assuming the carry and from the low bits is 0 and the other assuming the carry n is 1 so now we have three 16-bit adders all of which can operate in parallel on newly arriving a and B inputs once the 16-bit additions are complete we can use the actual carryout from the low half to select the answer from the particular high half adder that use the matching carrying value this type of adder is appropriately named the carry select adder the latency of this carry select editor is just a little more than the latency of a 16-bit ripple-carry Edition this is approximately half the latency of the original 32-bit ripple-carry adder so at a cost of about 50% more circuitry with half the latency as a next step we could apply the same strategy to have the latency of the 16-bit adders and then again to have the latency of the 8 bit adders used in the previous step at each step we have the adder latency in a deluxe delay after log base 2 of n steps and will be 1 and we're done at this point the latency would be some constant cost to do a 1-bit addition plus log base 2 of M times the MUX latency to select the right answers so the overall latency of the carry select adder is order log n note that log base 2 of N and log N only differ by a constant factor so we ignore the base of the log in order of notation the carry select adder shows a clear performance size trade-off available to the designer since adders play a big role in many digital systems here's a more carefully engineered version of a 32 bit curie selector you could try this in your ALU design the size of the adder blocks has been chosen so that the trial sums and the carry in from the previous stage arrived at the carry select MUX and approximately at the same time note that since the Select signal for the MUX is is heavily loaded we've included a buffer to make the Select signal transitions faster this carry select data is about two and a half times faster than a 32-bit ripple-carry adder at the cost of about twice as much circuitry a great design to remember when you're looking to double the speed of your ALU
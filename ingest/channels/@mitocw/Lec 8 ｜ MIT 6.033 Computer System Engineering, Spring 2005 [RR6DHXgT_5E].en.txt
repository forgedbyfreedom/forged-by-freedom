let's go ahead and get started okay so uh today we have one topic to finish up very briefly from last time so if you remember when we finish off last time we were talking about the example of a uh multi-threaded web server um so the example that we were talking about and this is an example that we're going to use throughout the I'm going to use throughout the lecture today consisted of a web server um with a is going to this this example consists of a web server with three uh main modules or main components so it consists of a this the three modules are a networking module uh a web server module which is in charge of generating uh for example HTML Pages um and then a disk module which is in charge of reading data off of disk okay so this thing is going to be communicating with the dis which I've drawn as a cylinder here um so what happens is that client requests come in to this web server they're uh come into the network module the network module uh forwards those requests on the web server the web server is in charge of generating say the HTML page that corresponds to the request and in order to do that it may need to read some data off of the disk so it forwards this request onto the disk module which goes and actually gets the page from the disk and then at some point later the disk Returns the page to the web server the web server Returns the page to the network module and then the network module uh sends the sends the answer back over the network to the user so uh this is a very simple example of a web server it should be sort of familiar to you since you've uh just spent a while studying the flash web server so you can see that this is sort of a simplified description of what a web server does um so if you think about what's what how you would actually go about designing a web server like this of course it's not the case that there's only one request that is moving between these modeles at any one point in time so in fact uh there may be multiple client requests that come into the network module and the network module may want to have multiple outstanding pages that it's asking the web server to generate and the web server it's s might be requesting multiple items from the disk okay and so in turn that means that at any point in time there could be uh sort of multiple results there could be you know results coming streaming back in from the disk which are going into the web server uh which is sort of chewing on results and producing pages to the network module um and so it's possible for there to be sort of cues that are building up between these modules both on the send and receive so I'm going to draw a q I'll draw a q just sort of as a box with these vertical arrows through it so there's some buffering that's happening between the sort of incoming requests and the outgoing requests on these modules okay and this buffering is a good thing and we'll we're going to talk more about this throughout the lecture today because what it allows us to do is it allows us to decouple the operations of these different modules so for example the disk module can be reading a page from disk while the HTML page while the HTML server is for example simultaneously generating an HTML page that wants to return to the client um but in this architecture you can see that uh for example when the HTML when the web server wants to produce a result it can only produce a result when the disk pages that it needs are actually available so the web server is dependent on some result from the dis from the disk module uh being available so if we were to look at just this web server the H I'm going to I call this the HTML thread here and the dis thread so these are these uh two threads that are on the uh right the the right side of the of this diagram that I've drawn here um if you were to look at the code that was running in these things and we saw this last time the code would might might look like something like this so the HTML thread is just going to sit in a loop continually trying to DQ information from this queue that is shared between it and the dis thread and then the disc thread is going to be in a loop where it continually reads blocks off the dis and then uh enues them onto this que so this design at first seems like it might be fine but then if you start thinking about what's going really really going on here there could be a problem so suppose for for example that the Q is of a finite length it only has a certain number of elements in it now when we keep calling in Q over and over and over again um it's possible that if the HTML thread isn't consuming these pages off the queue fast enough that the queue could fill up and it could overflow right so that might be a problem that we would want to sort of make a condition that we would explicitly check for in the code and so we could do that by adding a set of conditions like this so um what we what you see here is that uh I i' I've just augmented the code with these two additional variables used and free where used indicates the number of blocks that are in the queue that are currently in use and free indicates the number of blocks that are in the code that are uh the number of blocks that are in the queue that are currently free so uh what this Loop does is the the dis thread says it only wants to inue something onto the queue um when there is are some free blocks so it has a while loop that just Loops forever and ever and ever while they're uh waiting when there are no free blocks okay and similarly the HTML thread is just going to wait forever uh when there are no used blocks okay and then when some when the when the disc thread in cues a block onto the que it's going to decrement the free count because it's reduced the number of things that are in the in the que and it's going to increment the used count because now there's one additional thing that's available in the que okay so this is a simple way in which now we've made it so these things wait for each they're waiting for each other they're coordinating with each other by use of these uh two shared variables used in free okay so these two threads share this the these variables um so that's fine but if you think about this from a scheduling point of view uh there still is a little bit of a problem with this approach so uh in particular what's going on here is that oops um what's going on here is that the free uh that when one of these threads enters into one of these while Loops it's just going to sit there checking this condition over and over and over and over again right so the the thread scheder scheduled that thread it's going to repeatedly check this condition and that's uh maybe not so desirable so suppose for example that the HTML thread enters into this Loop and starts looping because there's no data available um now what really we would like to have happen is for the disc thread to be allowed to get a chance to run so maybe it's can produce some data so that the HTML thread can then go ahead and operate but um with this while loop there uh we we we can't quite do that we just sort of waste the CPU during the time we're in this while loop so instead what we're going to do is introduce a set of uh what we call sequence coordination operators so in order just in in order to introduce this we're going to add something a new kind of data type that we call an event count and an event count is just you can just think of it as an integer that indicates the number of times that something has occurred it's just some some sort of running counter variable um and we're going to introduce two new routines so we call these these two routines are called weight and notify okay so weight takes two arguments uh it takes one of these event count variables uh and it takes a value okay so what weight says is uh check this value uh check the VAR the value of this event count thing and see um whether when we check it it's uh the value of this event count is uh less than or equal to Value so um if event count is less than or equal to Value then it waits okay and what it means for it to wait is that it the it tells the thread scheduler that it no longer wants to be scheduled um until somebody later calls this notify routine on this same event count variable okay so wait says wait if this condition is true and then notify says wake up everybody who's waiting on this variable so we can use these routines in the following way in this code and it's really very straightforward we simply change uh our iteration through uh our our our while Loops into these weight statements so what we're going to do is we're going to have the HTML thread weight until uh the value of used becomes uh greater than zero and we're going to have our dis thread weight until the value of free becomes greater than zero and then the only thing other thing that we have to add to this is simply a call to notify so uh what notify does is it indicates to the HT it indicates to any uh other thread that is waiting on a particular variable that that thread can run so uh the HTML thread will notify free which will indic which will tell the dis thread that it can now begin running if it had been waiting on the variable free Okay so this emulates the behavior of the while loop that we had before except that what it the the thread scheduler rather than sitting in an infinite while loop simply doesn't schedule the HTML thread or the disc thread while it's waiting in one of these weight statements okay so uh what we're going to talk about for the rest of the lecture today um is is related to this and I think you'll see why as we get as we get through the talk um the topic for today is performance so uh performance is really what we've talked what I what we've looked at so far in this class are these various ways of structuring complex programs how to break them up into several modules uh the client server Paradigm how threads threads work how a thread scheduler Works um all these sort of big topics about how you design a system but we haven't said anything about how you take a system design and sort of in an ordered regular systematic way think about making that system run efficiently so that's uh what we're going to try and get at today we're going to look at a set of techniques that we can use to make a computer system more efficient um and so these techniques there there are really three techniques that we're going to look at today the first one is a technique called concurrency and concurrency is really about um allowing the system to perform multiple op operations simultaneously so for example in our uh our sample web server it may be the case that we have uh the this disk that we can sort of read pages from at the same time that for example the CPU generates uh some web pages that it's going to Output to the client okay so that's what concurrency is about we're also going to look at a technique called caching which you guys should have all seen before uh caching is really just about saving off some previous work some previous computation that we've already done or a previous dis page that we've already read in we want to save it off so that we can reuse it again in a later time and then finally we're going to look at uh something called scheduling so scheduling is about um when we have multiple requests to process we might be able to order those requests in a certain way or group the the requests together in a certain way so that we can make the system more efficient so um it's really about sort of choosing the order in which we do things in order to make the system run more efficiently and these we're going to throughout the course of this I'm going to use this example of this web server that we've been talking about to sort of motivate each of the uh applications or each each of these uh performance techniques that we're going to talk about so in order to talk about sort of what in order to get to the point where we can understand how these performance techniques work we need to talk a little bit about what we mean by performance how do we measure the performance of the system and how do we understand where the bottlenecks and performance in a system might be so one thing we might want to the first thing we need to do is to define a set of performance metrics um these are just a set of sort of uh terms and definitions that we can use so we can talk about what the performance of a system is so the first metric we might be interested in is uh the capacity of a system um and capacity is simply some uh measure of the amount of resource in a system okay so this sounds kind of abstract but what we mean by a resource is some sort of thing that we can compute with it's a dis or a CPU or a network so we might for example talk about the um you know number of the the size the capacity uh of a dis might be the size in megabyte in gigabytes you know or the capacity of a processor might be the number of instructions it can execute per second okay so once we have capacity now we can start talking about uh how much of the system we're actually using so we can talk about the utilization so utilization is simply the percentage of capacity we're using so we might have used up 80% of the dis blocks on our computer um so now there are two sort of properties or two metrics that we sort of are very commonly used in computer systems in order to classify uh the or sort of talk about what the performance of the system is so the first metric is late laty okay so latency um is simply uh the time for a request to complete so req is request okay um and we can also talk about uh sort of the the inverse of this what seems like what it first will seem like the inverse of this which is throughput that's simply the number of requests per second that we can process so when you think about latency and through throughput when you first see this definition it's tempting to say that think that simply uh throughput is the inverse of latency right if it takes 10 milliseconds for a request to complete well then I must be able to complete 100 requests per second right um and that's true in the simple case where I have a um in in the very simplest in the very simple example where I have a single module for example that can process one request at a time um so a single computational resource for example that's only running you know that that can only do one thing at a time um if this thing has some set of some infinite set of inputs in it and it takes 10 milliseconds to process each input um we'll see say 100 uh results per second coming out okay so uh if you can do something takes 10 milliseconds to do you can do 100 of them per second um so we get say the throughput of this system is 100 per second and the latency is 10 milliseconds what we're going to see throughout this talk is that um in fact this this strict relationship between latency and throughput doesn't hold I mean you guys probably have already seen the notion of pipelining before in six4 and you'll understand that pipelining is a way in which we can improve the throughput of the system without necessarily changing the latency and we'll talk about that more carefully as the as this talk goes on okay so um given these metric now what we need to do is think a little bit about you know okay so suppose I have some system and suppose I have some sort of set of goals for that system like I want the system to be able to process a certain number of requests per second or I want the latency of this system to be under some amount um so then the question is so you know you're given this computer system and you sit down you want to measure it um and you sort of so you're going to measure the system and what are you going to expect what what do you expect to find so um in the design of computer systems it turns out that uh there are some sort of well-known performance pitfalls or so-called performance uh bottlenecks and the goal of sort of doing performance analysis of a system is to look at the system and figure out where the bottlenecks are so um this typically in the design of a big computer system what we're worried about is which of the you know little individual modules within the system is most responsible for slowing down my computer and what should I do in order to sort of and then once you've identified that module think about how to make that particular module that's slow run faster so that's really what finding performance bottlenecks is about um and there's a classic bottleneck uh that occurs in computer systems um that you guys all need to know about this is the so-called IO bottleneck okay so what the io bottleneck says um is really uh fairly straightforward if you think about your a computer system um it has a hierarchy of memory devices in it okay and these memory devices start uh or storage devices so these storage devices first start with the CPU right so the CPU has some set of registers on it and a small number of them say for example 32 um and you can access those registers very very fast say once per uh instruction on the once per cycle on the computer so for example uh we so if your CPU is one gigahertz you may be able to access uh one of these registers in 30 in in one nond okay um and so what we're talking so typically at the at the tallest level of this pyramid we have a small storage uh that is fast okay as we go down this pyramid adding new layers looking at this uh the storage hierarchy we're going to see that things get bigger and slower so just below the CPU we may have some processor cache okay and this might be for example 512 12 kilobytes uh and it might take 20 NS to access a single uh say uh block of this memory okay and then we're going to have the ram the main memory of the device which on a modern machine might be 1 Gigabyte and it might take 100 nond to access okay and then below that you take a big step down or big step up in size and big step down in performance you typically have a disc so a disc might be as big as 100 gigabytes right but the performance is very slow so it's a mechanical thing that has to spin and it only spins so fast um so a typical access time for a block of the disc might be as high as 10 milliseconds or even higher and then sometimes people will talk about in this hierarchy uh the network is actually a level below that so if something isn't available on the local dis for example on our web server we might actually have to go out into the network and fetch it and if this network is the internet right the internet has a huge amount of data I mean who knows how much it is it's certainly orders terabytes um and it could take a long time to get a page off the internet so it might take you might have it take 100 milliseconds to reach some remote site on the internet okay so uh the point about this IO bottleneck is that this is going to be a very common the the sort of the disparity in the performance of these different levels of the system is going to be a very common source of performance problems in our computers so in particular uh you know if you look at the sort of the access time here is 1 NC the access time down here is 100 milliseconds this is a 10 to eth difference right which is equal to 100 million times difference in the performance of the fastest to the slowest thing here so if the CPU has to wait for something to come over the network you're going to be waiting you're waiting for a very long time in terms of the amount of time the CPU takes to say read a single word of memory so um what this so when we look at the performance of a computer system we're going to see that often these sort of this IO bottleneck is the problem with that system so if we look for example at our web server um with its three stages um where this stage is the one that goes to disk this is the HTML stage which maybe can just be computed in memory uh and this is the uh Network stage know we might be talking about 10 milliseconds latency for the disk stage we might be talking about just one millisecond for the HTML page because all it has to do is you know do some computation in memory and we might be talking about 100 milliseconds for the network stage to run because it has to send some data out to some remote site so if you in order to process a single request have to go through each of these steps in sequence then the total performance of the system is going to be uh the time to process a single request is going to be say for example 111 milliseconds the sum of these three things okay um and so if you look at this system and you say okay so where should what's the performance bottleneck in this system so the prond r is clearly this network stage because it takes the longest to run um and so if we want to if we want to answer the question about where we should be optimizing the system one place we might think to optimize uh is within this network stage and we'll see later uh an example of a simple kind of optimization that we can apply based on this notion of concurrency to improve the performance of the networking stage so uh this so as I just said the the notion of concurrency is going to be the way that we're going to be get get at um sort of eliminating these IO bottlenecks so um and the idea is going to be that we want to overlap uh the use of some other resource during the time that we are uh waiting for one of these slow iio devices to complete um and we're going to look at two types of concurrency we're going to look at concurrency between modules and within a module okay so we may have modules that are composed for example our networking module uh may be composed of multiple threads Each of which can be accessing the network so that's an example of concurrency within a module um and we may we're going to look at the case of between module concurrency where for example the HTML module can be processing can be generating an HTML page while the dis module is reading another reading in request for for another client at the same time okay um and so the idea behind concurrency is really going to be uh that by using concurrency we can uh hide the latency of one of these slow I/O stages okay so um the first kind of concurrency we're going to talk about is concurrency between modules um and the primary Technique we use for doing this is pipelining so the idea with pipelining is as follows suppose we have our web server again and this time let's draw it as I drew it at first with the Q with cues between each of the modules okay um so we have these we have our web server which has our three stages um and suppose that what we're doing is we have some set of uh requests sort of an infinite cue of requests that is uh sort of queed up at the disc thread and the disc thread is producing these things and we're sending them through we want to look at how many requests sort of how many uh Pages come out here per second and what the latency of each page is so um if we have some list of requests suppose these requests are numbered uh R1 through RN okay so the uh what's going to happen is that the first request is going to be ready is going to be start being processed by the dis server right so it's going to produce our it's going to start processing R1 now in a pipelining system what we're going to want to do is to have each one of these threads uh sort of working on a different request each one of these modules working on a different request uh at each point in time and because the disk is this independent resource from the CPU is an independent resource from the network um this is going to be okay these three modules aren't actually going to contend with each other too much so what's going to happen is this guy's going to start processing R1 right um and then after 10 milliseconds he's going to uh pass R1 up to here and start working on R2 okay and now uh 1 millisecond after that this guy is going to finish R1 and send it to here um and then 9 milliseconds after that R2 is going to come up here and this guy can start processing R3 okay so does everybody sort of see where those numbers are coming from okay uh good so um now what we're going to do is uh what we if we look at the sort of at time starting with this equal to Time Zero um in terms of the requests that come in and out of this last network uh thread um we can sort of get a sense of how fast this thing is processing so the first R1 is uh going to enter enters into the system at at uh after 11 milliseconds right so it takes one 10 milliseconds to get through here and 1 millisecond to get through here uh and it starts processing R1 at this time so I'm going to write plus R1 to suggest that we start processing it here um the next time that this module can do anything is 100 milliseconds after uh it first started processing um the the next time this module does anything is 100 milliseconds after it started processing R1 so at time 111 milliseconds it can output R1 or it's done processing R1 and then um of course by that time R2 and R3 some set of requests have already queed up um in this Q waiting for it so it can immediately begin processing um R2 at this time okay so then clearly what's going to happen is after uh 211 milliseconds it's going to Output R2 uh R2 and it's going to begin processing R3 okay um so there should be a plus there and a plus there so um and similarly at 311 we're going to move move on to the next one so if you look now at this system we've done something pretty interesting which is that it still took us 100 is still uh the sort of time for this request to travel through this whole thing was 110 milliseconds but if you look at the intro arrival time between uh each of these successive outputs of R1 they're only 100 milliseconds right so we're only waiting as long as it takes R1 to Pro to take R1 to process a result in order to produce these results in order to produce answers so by pipelining the system in this way and having uh the the web server thread and the disk thread do their processing on later requests while R1 is processing its request um we can increase the throughput of the system so in this case we get a arrival every 100 milliseconds so the throughput is now uh equal to uh one one uh result every 100 milliseconds or 10 results per second okay so even though the latency is still is still 111 milliseconds uh the throughput is no longer one over the latency because we've separated this them in this way by pipelining them okay so that was good that's nice we improved the performance of the system a little bit but we didn't really improve it very much right it's just uh you know we've increased the throughput of this thing a little bit but um we haven't really addressed what we identified earlier as being the bottleneck which is the fact that this R1 stage is taking 100 milliseconds to process um and in general when we have a pipeline system like this um we can say that the the throughput of the system is uh bottlenecked by the slowest stage of the system so anytime you have a pipeline um the throughput of the system is going to be the throughput of the of the slowest stage so in this case the throughput is uh you know is 10 results per second and that's the the throughput of the whole system so if we want to improve the throughput uh any more than this what we're going to have to do is to somehow improve the performance of this module here um and the way that we're going to do that is also by exploting concurrency but this is going to be uh this within a module concurrency so if you think about how a web server Works um or how how a network works typically when we're sending these requests to a to a client uh each of the requests it's not that the it's it's not that we're using up all of the available bandwidth of the network when we're sending these requests to the client right you may be able to send 100 megabytes per second um out over your network or you know if you're connected to a machine here you may be able to send you know 10 me megabytes a second you know across the country to some other university um the issue is that it takes a relatively long time for that request to propagate especially when that request is propagating out over the Internet so uh the latency can be quite high but you may not be using all of the bandwidth when you're say for example sending an H HML page so in particular um it is the case that uh multiple applications can be sort multiple threads can be simultaneously sending data out over the network um and if that doesn't make sense to you right now we're going to spend the whole next four lectures talking about Network performance and it should make sense for you so to you so just take my word for it that uh one of the properties of the network is so that the latency of the network um may be relatively High uh but in this case we're not actually going to be using all the the bandwidth that's available to us so that suggests that there's an idle resource it means that we're we sort of have some Network bandwidth that we could be using that we're not using so we'd like to take advantage of that in the design of our system so we can do this in a relatively simple way um which is simply to say uh let's within our networking module rather than only having one thread sending out requests at one time let's have multiple threads let's for example have say we have uh 10 threads so we have uh thread one thread two thread 10 okay um we're going to allow these all to be using the network at once uh and they're all going to be talking to the same que that's connected to the same HTML module that's connected to the same dis module there's a q between these as well okay um so now when we think about uh the performance of this now let's see what happens when we start running requests through this Pipeline and let's see sort of uh how how frequently we get requests coming out at the other end so um we draw our timeline again you can see that you know R1 is going to come in here at after and then after 10 milliseconds it's going to move to here and then after 11 milliseconds it'll arrive here we'll start processing request one now the second request R2 is going to be here and we're going to have 9 milliseconds of processing left to do on it uh be after R1 it get gets sent onto the next thread so um R2 is going to be in here for 9 milliseconds it'll be in here for 1 millisecond so 10 milliseconds after R1 arrives here R2 is going to arrive here so what we have is we have at 11 milliseconds we have R1 now 10 milliseconds later we have R2 okay so now you can see that suddenly this module this system is able to process multiple requests it's sort of has multiple requests that it's processing at the same time and so you know 10 milliseconds after that uh our three is going to start being processed and then so what that means is at time you know after some passage of time we're going to have r10 in here and that's going to go in after 101 milliseconds right so we're going to get r10 okay and now we're ready to start processing now we've sort of pushed all these through and now suppose we start processing R11 okay so R11 is going to flow through this Pipeline and then it it's at at time uh 111 R11 is going to be ready to be processed um but notice that at time 111 we are finished processing R1 right so uh at this time we can add R11 to the system and we can output R1 okay so now every 10 milliseconds after this another result is going to arrive and we're going to be able to output the next one okay and this is just going to continue so now you see what we' managed to do is we've reduced we made this system so that every 10 milliseconds after this sort of startup time of 111 milliseconds after every 10 milliseconds we're producing a result right so we're going to get actually 100 uh per second is this is going to be the throughput of this system now okay so that seems that was that was kind of neat how did we do that what have we what have we done here well effectively what we've done is we've made it so that uh this module here can process sort of 10 times as many requests as it could before and so um it's just simply going this module itself now has 10 times the throughput that it had before um and we said before that the bottleneck the bottleneck and the system uh is you know the the slowest through the the throughput of a system is the throughput of the SL throw slowest stage so uh what we managed to do is decrease the throughput of this slowest stage and so now the system is running 10 times as fast notice now that uh the dis thread and the network thread are both take 10 milliseconds to sort of the throughput of each of them is is uh 100 per second um and so that's uh now we have sort of two stages that have been equalized in their late in in their throughput and so if we wanted to further increase the performance of the system we would have to increase the performance of both of these stages not just one of them okay so um that was that was a nice result right it seems uh like we' done something sort of uh We've shown that we can use this notion of concurrency to increase the performance of a system um but we've introduced a little bit of a problem so in particular the problem we've introduced uh is as follows so remember we said we had this set of threads uh uh one through say for example 10 that are processing uh that all sharing this Q data structure uh that is uh connected up to our uh HTML thread right um so the problem with this is that uh what we've done is to introduce what's called a race condition uh on this q and I'll show you what I mean by that so if we look at our code snippet up here for example for what's happening uh in our HTML thread we see that uh what it does is it calls DQ right so the problem that we can have is that we may have multiple of these modules that are simultaneously executing at the same time and they may simultaneously both call DQ right so depending on how DQ is implemented we can get some weird results so let me give you a sort of very simple possible implementation of DQ suppose that what DQ does is it uh reads so let's uh given given this Q here let's say uh the Q is managed by there's two variables that keep track of the current state of this Q there's a variable called first which points to the head of the Q um and there's a variable called last which points to the it first points the first used element in this q and last points to the last uh used element so the members the elements that are in use in the que at any one time are between first and last okay um and what's going to happen is when we DQ we're going to decrement we're going to sort of move first over one right um so we're going to free we're going to when we DQ something we'll free up this cell um and when we inq uh we'll move last down one okay so this is a and then when last reaches the end we're going to wrap it around so this is a fairly standard implementation of a q it's called a circular buffer um and if first is equal to last then uh the we know that the Q is full so uh that's that's the condition that we can check so we're not going to go into too many details about how this thing is actually implemented but let's look got a very simple example of how uh in of how DQ might work so um remember we have these two shared variables first and last that are shared between these say all these threads that are accessing this thing uh and what DQ might do is to say it's going to read a block from uh rate a page from this Q so read the next HTML page to Output um and it's going to read that into a local variable called page um let's call this Q buff buuf and let's just we'll use array notation for accessing it so it's going to read buff sub first okay and then uh it's going to increment first uh first gets first plus one and then it's going to return page okay that seems like a standard a straightforward implementation of of DQ uh and so we have one thread that's doing this now suppose we have another thread that's doing exactly the same time same thing at the same time so it runs exactly the same code and remember that there these two threads are sharing the variables uh buff and first okay so if you think about this and you think about how these two things running in two two threads at the same time uh there's sort of an interesting uh condition an interesting problem that can arise so um one thing that might happen when we're running these two things at the same time is that the thread scheduler might first start running thread one and it might run the first instruction of thread one and then it might run this second instruction then it might run this return thing and then it might come over here and it might run uh start running T2 so it then might you know uh it it might then stop running T1 and start running T2 and execute his three instructions so if the thread scheduler does this um there's nothing wrong it's not a problem right the threed scheduler has uh you know each of these things read read its value from the Q incremented it T1 read one thing from the from the Q and then T2 read the next thing from the Q so clearly some of the time this is going to work fine so let's make a list of possible outcomes sometimes we'll be okay first possible outcome was okay but let's look at a different situation suppose that what happens is that um the first thing T1 the first thing that thread scheder does is schedule T1 and T1 executes this first instruction and then just after that the thread scheduler decides to preempt T1 and allow T2 to start running so it in particular allows T T2 to execute this DQ instruction uh to its end and then it comes over here and it runs T1 okay so uh what What's the problem now yeah right okay so um they've both read in the same page variable so now both of these threads have dced the same page um and so they so the value of first was it was initially pointing for T1 it was pointing here um and then we switched and it was still pointing here right and now so both these guys have read the same page and now they're both at some point going to increment first so you're going to increment it once then you're increment it again so this second element here in the queue has been skipped okay so this is a problem we don't want this to happen because uh the system is not outputting all the pages that it was supposed to Output so uh what can we do to fix this so the way that we fix this is by introducing something we call isolation Primitives and the basic idea is that we want to uh introduce an operation that will make it so that anytime that the page variable gets uh gets read the anytime a page gets read out of the queue that we also at the same time increment first without any other sort of threads accesses to this queue being interleaved with our accesses to this que or our DQ's from the que um so uh in sort of technical terms what we say is we want uh these two things P the the the uh reading of page and the incrementing of first to be socalled Atomic okay and the way that we're going to make these things Atomic is uh by uh isolating them from each other uh by isolating these two threads from each other when they're executing the inq and DQ things so uh these two terms we're going to come back to uh in in a few weeks in a few months in in the class towards the end of the class um but all you need to understand here is that there is this race condition and we want some way to prevent it and the way that we're going to prevent it is by using these isolation routines um also sometimes called locks so the idea is as uh and in this case the isolation schemes are going to be called locks so the idea is that a lock is simply a variable um which can be in one of two states can either be set or unset um and we have two operations that we can apply on a lock we can acquire it and we can release it okay and acquire and release have the following Behavior what acquire says is um set check the state of the lock and if the lock is unset then change the state to set but if the lock is set then wait until the lock becomes unset okay what it release says is it simply says change the state of the lock from unset to set or from set to unset excuse me um so uh let's see how we can use these two routines in our code so let's go back to our example of inq and DQ let's introduce a lock variable we'll call it TL For Thread lock um and what we're going to do is simply around these two operations to uh access the Q we're going to uh to to modify this page in first ver to read the page and modify first we're going to put acquire and a release okay um okay so we have acq on this threadlock and we have release on this thread loock um okay so let's look so this this seems fine it looks like we've done this but it's sort of positing the existence of this uh acquire thre this acquire procedure that just sort of does the right thing and if you think about this for a minute it seems like we could have the same race condition problem in the acquire module as well right or the acquire function as well what if two two but guys both try and acquire the lock at the same time how are we going to avoid this problem um and there's a couple of ways that are sort of well understood for avoiding this problem uh in practice and they talked about in the book I'm just going to introduce the simplest of them now which is that we're going to add a special instruction to the microprocessor that allows us to uh do this acquire efficiently and it turns out that most micro most modern microprocessors have an equivalent instruction so we're going to call this instruction uh RSL um for read and set lock okay so the idea with RSL is as follows um we uh can basically the implementation of the acquire uh module is going to be like this we're going to set we're going to acquire is going to um what it's going to do remember we want to wait until we want to Loop while we we if if we don't have the lock we want to Loop until we've had the lock so the implementation of choir may look as follows we'll have a local variable called held we'll initially set it to false and then we'll in a while loop while we don't hold the lock um we're going to use this RSL instruction okay um so what this says is held equals RSL of TL okay so what the RSL instruction does is it looks at the state of the lock and if the lock is unset then it sets it um and if the lock is set then it returns it sets it and it returns true um and if the lock is set then it returns false okay so it's this has this property that it can both read and set the lock within a single instruction right and that is a way that we're now able so we're going to use this uh read and set lock sort of primitive this basic thing as a way to build up this sort of uh more complicated acquire function which we can then use to build up these locks okay so this is this so so anytime you're designing a multi-threaded system in this way or a system with lots of concurrency you should be worrying about whether you have race conditions um and if you have raise conditions you need to think about uh how to use locks in order to prevent those raise conditions all right um so there are a couple of other topics uh related to Performance that appear in the text um and one of those topics is caching and I just want to spend one very brief minute on caching so you guys have already seen caching uh presumably in the context of 64 with processor caches um and what we would want to do so so you might want to sit down and think through as an example of how you would use a cache in order to per improve the performance um of our web server so one thing that you might do uh in order to improve the ovance of the web server is to put a cache between the disk uh and the uh is is to put a cache between in the disk thread uh that reduces the uh that you use instead of going to disk in order to sort of reduce the latency of a disk access um and all at the beginning of class next time take you through a very simple example of how we can actually use the dis thread in order to do that um but you guys should think about this a little bit on your own so um barring that little digression that we'll have next time this takes us to the end of our discussion of uh sort of modularity abstraction and performance um and what we're going to start talking about next time is networking and how networks work but I want you guys to make sure that you keep in mind all these topics that we've talked about because these are going to be the sort of fundamental tools that we're going to use throughout the class in the design of computer system so because we finished this module doesn't mean that it's sort of okay to stop thinking about this stuff you need to keep all of this in mind at the same time so uh we'll see you on Wednesday
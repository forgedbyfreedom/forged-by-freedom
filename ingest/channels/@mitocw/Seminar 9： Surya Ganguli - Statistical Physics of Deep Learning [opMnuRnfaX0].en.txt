the following content is provided under a Creative Commons license your support will help MIT OpenCourseWare continue to offer high quality educational resources for free to make a donation or view additional materials from hundreds of MIT courses visit MIT opencourseware at ocw.mit.edu to talk about the statistical physics of deep learning essentially so this is some ongoing work in my lab that was really motivated by trying to understand how neural networks and infants learn categories and then it sort of led to a bunch of results in in deep learning that involves two Cisco physics so I wanted to just introduce my my lab a little bit I'm an interloper from the methods and computational neuroscience summer school where I tend to spend a month and so there you know the the the flavor of research that we do there in the flavor research we do in our lab is sort of drilling down into neural mechanisms underlying well-defined computations and we've been working on that with you know I spend a lot of time talking to neurophysiologists especially at Stanford where I am so we have a whole bunch of collaborations going on now involving understanding neural computation so for example the retina itself is actually a deep neural circuit already because there's interview there's an intervening layer of neurons the bipolar cells and a broken cells that intervene between the photoreceptors and the ganglion cells and oftentimes what we do is we we shine light on the photoreceptors and we measure the ganglion cells but we have no clue what's going on in the interior so we've developed computational methods that can successfully computationally reconstruct what's going on in the interior of this putative deep neural network even though we don't have access to these things and we can actually infer the existence and properties of intermediate neurons here and those properties are sort of similar to what's been previously recorded when people do directly record from those say the bipolar cells in the cleansing in lab we've sort of been unraveling the the motions underlying underlying the computations the competitions underlying of motion vision so you know when you swat a fly it's really hard because they can really quickly detect motion coming towards it and they fly away so we've been you know there's so there's been lots of work on what kinds of algorithms might underlie motion estimation for example the Rijkaard correlator the Barlow Levesque model and so forth we've been applying systems identification techniques to whole grain calcium imaging data we'll whole brain meaning from the fly visual circuit and you know we find we just literally identify the computation and we find that it's sort of none of the above it's a mixture of all previous approaches so grid cells we have some results in grid cells so these are these famous cells that resulted in a Nobel Prize recently we can actually show that these grid cells maintain their spatial coherence because the rat and the mouse are always interacting with the boundaries and the boundaries actually correct the rats internal estimated position and were it not for these interactions two boundaries the grid cells would rapidly Tico hair decohere on the timescale of minutes you know like less than a minute and so it's actually quite interesting we can show that whenever the rat encounters they encounters a boundary it corrects its internal estimate of position perpendicular to the boundary but it doesn't parallel and it doesn't because it can because when it hits a boundary it receives no information about where it is parallel to the boundary but it receives information in this direction and then with the Shenoy lab we've been looking at I think a really major conceptual puzzle in neuroscience is why can we record from a hundred neurons in a circuit containing millions of neurons and do dimensionality direction and infer these dynamical portraits of how the you know try to infer the state space dynamics in the circuit and claim that we've achieved success right we're doing dramatic undersampling recording a hundred neurons out of millions how would the how would the state space dynamics that we infer change if we recorded more neurons and we can show that essentially it will not change because we've come up with a novel connection between the active neural measurement and the act of random projections so we can show that the act of recording from a hundred neurons in the brain is like the act of measuring a hundred random linear combinations of all neurons in the relevant brain circuit and then we can apply random projection 3 to give us a predictive theory of experimental design that tells us given the complexity of the task how many neurons would you need to record to correctly recover the state space dynamics of the circuit and then in the Raymond lab at Stanford we've been looking at houseing synaptic plasticity can either enhance or impair learning depending on experience so for example we know that we think that synaptic plasticity underlies the very basis of our ability to learn and remember so you might think that enhancing synaptic plasticity through various pharmacological or genetic genetic modifications might enhance our ability to learn learn and remember but previous results have been mixed when you perturb synaptic plasticity sometimes you enhance learning sometimes you impair learning so I believe in the Raymond lab they're the first to show that in the same subject enhancing synaptic plasticity for example the cerebellum can either enhance or impair learning depending in the history of prior experience and we can show that in order to explain the behavioral learning curves you need much more complex than optic dynamics than people naturally assume we have to really promote our notion of what a synapse is from a single scalar like a W IJ in a neural network to an entire dynamical system in its own right so this relates to view our learning so this is sort of the low-level stuff that we've been doing I know that in this course you guys study a higher level stuff so I'm not going to talk about any of these that each of them could be like a one-hour talk but I wanted to discuss some more high level stuff that we've been doing oh sorry and the other sorry the other sort of direction in our lab that we're looking at is actually the statistical mechanics of high dimensional data analysis so this is of course very relevant and the age of the brain initiative and so on we're developing very large scale data sets and we'd like to extract theories from this so-called big data so the entire edifice of classical statistics is predicated on the assumption that you have many many data points and you have a small number of features right so then it's very easy to see patterns in your data but what's actually happening nowadays is that we have a large number of data points and a large number of features so for example where you can record from a hundred neurons using electrophysiology maybe for only a hundred trials of any given trial type in a monkey doing some tasks so the ratio of the amount of data to the the number of the number of features is something that's order one so you know the data sets are like three points in a three-dimensional space that's the best we can visualize so it's a significant challenge to do data analysis in this scenario so it turns out there's beautiful connections between machine learning and data analysis and the statistical physics of systems of the quench disorder so so what I mean by that is in data analysis you want to learn statistical parameters by maximizing the log likelihood of the data given the parameters and Cisco physics you often want to minimize an you know this can be viewed as energy minimization and so there's beautiful connections between these and so we work on that so we've applied this to compress sensing we've applied this to the problem of what's the optimal inference procedure in a written in a regime in the high-dimensional regime we know that maximum likelihood is optimal in this regime but something else is better in this regime and we found what's better it turns out to be a smooth maximum likelihood and of course we've applied this to a theory of neural dimensionality and measurement so you know there's lots of beautiful interactions between physics machine learning and neuroscience and and you know this school is a lot about that if you're interested actually we wrote like a 70 page review on statistical mechanics of complex neural systems and high dimensional data we cover a whole bunch of things like spin glasses the statistical mechanics of learning random matrix theory random dimensional reduction compressed sensing and so on and so forth it was our attempt to sort of put some systematic order on the diversity of topics viewed through the lens of statistical physics but what do I want to talk about today and why did I decide to branch out in the direction I'm going to tell you about well I think there's lots of motivations for an alliance between theoretical neuroscience theoretical machine learning that lead to opportunities for physics and math and so on so and this is the question that should haunt all of us right the question is what does it even mean to understand how the brain works or how a neural circuit works okay you know it that's an open question that we really have to come to terms with a proximal sort of a specification or a more concrete version of this question might be or a specification this question might be we will understand this when we understand how the connectivity and dynamics of a neural circuit give rise to behavior and also how neural activity and synaptic learning rules conspire to self-organize useful connectivity that's observes behavior okay so so you know the you know various brain interests are promising to give us recording some large numbers of neurons and even give us the connectivity between those neurons now what what if theorists own often what theorists in competition neuroscience do is they they often develop theories of random networks that have no function but what we would like to do is we'd like to understand engineered networks that have function so the field of machine learning has generated a plethora of learn neural networks that accomplish interesting functions yet we still don't have a meaningful understanding of how their connectivity dynamics the learning the developmental experience okay so basically we can measure anything we want in in these artificial neural networks right we can measure the connectivity between all the neurons we know the dynamics of all the neurons we know the learning rule we know the entire developmental experience the network because we know the training data that was exposed to yet we still do not have a meaningful understanding of how they learn and how they work and if we can't solve that problem how are we ever going to understand the brain right in the form of this question okay so that was sort of what was motivating me to look into this so so this is the outline of the talk the original entry point was trying to understand category learning in neural networks and then at the end of the day we made actually several theoretical advances that led to advances in machine learning and applications to engineering so for example we find we found random weight initializations that make a network dynamically critical and allow very very rapid training of deep neural networks we were able to understand and exploit the geometry of high dimensional error surfaces to again speed up learning like training deep neural networks and we're also able to exploit sort of recent work in non-equilibrium thermodynamics to learn complex probabilistic generative models so it's a diversity of topics but I'll walk you through them and you can relax because almost everything I'm going to talk about is is is published okay so let's start with the motivation a mathematical theory of semantic development I think this speaks to some of the high-level stuff that you guys think about this part could equally be talked it could could be called the misadventures and implied physicists who found himself lost in the psychology department so I sort of showed up at Stanford Jay's a great guy was talking to him and I learned about his work and I realized I didn't understand it and and this is my attempt to understand that work with Andrew and NJ so ok so what is semantic cognition so human semantic cognition a rough definition of this field is that we have an ability to learn recognize comprehend and produce inferences about properties of objects and events in the world especially properties that are not present in your current perceptual stimulus so for example I can ask you does a cat have fur and do birds fly and you can answer these questions correctly despite the fact that there's currently no cat or bird in the room right so you know our ability to do this likely relies on our ability to form internal representations of categories in the external world and associate properties with those categories because we never see the same stimulus twice so whenever we see a new stimulus or we trying to recall information from our brain we rapidly identify the relevant category that contains the information and we use that categorical representation to guide future actions or give answers right so a category formation is central to this right so what are the kinds of psychophysical tasks that people use to probe semantic cognition so this is a very rich field psychologists have been working on this all the time so one example is looking time studies to ascertain whether or not an infant can distinguish between two categories of objects at what age so for example they'll show a sequence of objects from category one say horses and the first time the infant sees a horse the looking time will go up and then it goes down over time it gets bored then you show a cow and if the infant is old enough the looking time will go up and then go back down and that's from that we infer that the infant can distinguish between horses and cows but if it's not old enough the looking time will not go up so it as the infant gets older and older it can make more and more fine scale discriminations between categories it turns out so property verification tasks you can ask can a canary move can it sing and certain questions are answered quickly certain questions are answered late which speaks to certain properties being central and peripheral to certain categories a category membership queries is a sparrow a bird or as an ostrich bird again there's different relate latencies and that's that suggests that there are typical and atypical category members and also very very important to us in is inductive generalization we can both generalize familiar properties to novel objects for example a blick has feathers does it fly does it sing and we can generalize novel properties to familiar objects a bird has gene X does a crocodile have gene X does a dog have gene X you know so the so people have measured these patterns of inductive generalizations and there's various theories that try to explain all of this stuff so jay has been working on this stuff from a neural network perspective and he wrote a beautiful book called semantic cognition where he uses neural networks to explain a whole variety of phenomena especially for example the progressive differential of progressive differentiation of concepts so let me just walk you through that and so this was you know my you know an encounter with a you know at first encounter with a deep neural network so they were they were doing deep neural networks before they became popular and so what they were doing was they asked can we model infants the development of say concepts and infants and so what they did was they had a toy data set where they had a bunch of objects and each object had a whole bunch of properties so for example a canary can grow move fly and swing sing right and and so what they did was they exposed this deep neural network to training data of this form you know they had a whole bunch of features and questions and objects and they just exposed the network to training data trained it using back propagation and they looked at the internal representations in the network especially their evolution over developmental time or training time right and this is what they found so initially the network started with random weights so there was no structure so these are okay so what what did they do here they looked at the the distances between the internal representations in this space and they did har cocoa clustering or multi-dimensional scaling and they found these plots or these plots right so so what you see is early in developmental time the network first makes a coarse grained discrimination between animals and and plants right and then and then later it makes finer scale discriminations and then eventually when it's fully learned it has it learns the hierarchical structure that's implicit in the training data okay and this is the multi-dimensional scaling plot where initially the animals move away from the plants and then you know fish move away from birds and and trees move away from flowers and then finally individual discriminations are learned so when I learned about this I was at once excited and mystify and also mystified because this is sort of qualitatively behaving like the way that an infant behaves yet it's a really stupid neural network with like five layers yet I don't understand how it's doing this right it so so I wanted a theory of what's going on here right so and by the way you know there's lots of reasons to believe that semantic relations are in cloak are encoded in the brain using relatively simple metrics like Euclidean distance between neural representations for different objects so for example this is a famous study which I'm sure you've seen what they showed was a whole bunch of objects to both monkeys and humans and they clustered the objects or looked at similarity matrix of the objects measured using a Euclidean distance in neural NIR electrophysiology space of firing rates of neurons here and voxel activity patterns in the human and they showed the same set of objects the monkey and human and and the matrices aligned essentially so basically the similarity structure of internal representations as both monkey and human is the same so we tend to encode similar we tend to encode semantic information using these similarity representations so this is the hierarchical clustering view so this sort of you know this sort of seems to actually happen in in real live animals and humans okay there's actually something else that happens is that different properties are learned on different time scales so for example the network can learn that Canaries can move much more quickly than it learns that a canary is yellow so it learns these sort of so some properties are much easier to learn than others and the properties that are easier to learn it for the network are also easier to learn for the infant ok so these are the theoretical questions we'd like to answer what are the mathematical principles underlying the hierarchical self-organization of internal representations in the network you know this is a complex system so what are the relative roles of the various ingredients it's and there's a nonlinear input-output response there's a learning rule which is back propagation there's the input statistics is the network somehow reaching into complex input statistics in the in the training set or or can it really rely on just second-order statistics you know there what is a mathematical definition of something called category currents and how does it relate to the speed of category learning so what determines the speed at which we learn categories why are some properties learn more quickly than others and how can we explain changing patterns of inductive generalization over these developmental timescales so ok so how do we get a theory well it turns out if you look at the activations of this network as its training over time so these are sigmoid units and that the activations don't really hit the saturating regime that much during training because you start from small weights so we started with an audacious proposal that maybe even a linear neural network might exhibit this kind of learning dynamics okay now it's not at all obvious that it should because it's a simple linear neural network and this learning dynamics is highly nonlinear all right but it turns out that even in a linear neural network the dynamics of learning on synaptic weight space is nonlinear and so there might be a hope that it might work and we might be able to get a coherent theory okay so what we did was we analyzed just a simple linear neural network that looks like this that goes from a input layer to hidden layer to output layer so the composite map is linear okay so so we can write down dynamical equations on weight space for the learning dynamics so this is the training data and we can adjust the weights using back propagation and these are the back propagation equations and if we work in a limit where the learning is slow relative to the time it takes to cycle through the data set you can take a continuous time limit and you essentially get a nonlinear set of equations on weight space right and the equations are cubic in the weights right and that's because the error is quartic in the weights right the error is the output minus W w times the input squared so the errors quartic in the weights and so if you differentiate we're supposed to get the weights on the right hand side the gradient descent equations will be cubic in the weights but there is one simplification that happens because the network is linear it's learning dynamics is sensitive only to the second-order statistics of the data right so in particular the input input covariance matrix and the input-output covariance matrix okay so essentially this network knows only about second-order statistics in our work here that the input statistics is white so it's really only the input-output statistics the drives learning okay so this is a set of coupled nonlinear differential equations they're in general hard to solve but we found solutions to them so what we we can express the solutions in terms of the singular value decomposition of the input-output covariance matrix so so any you know any rectangular matrix has a unique singular value decomposition in this context we can think about the input-output covariance matrix as a matrix that map's input objects to feature attributes and the singular vectors have an interpretation where the the these singular vector is essentially map objects into interim representations the singular values amplify them and then the columns of you are sort of feature synthesizers the columns are sort of modes in the output feature space okay so this is the SVD but the question is how does the learning dynamics how does this drive the learning dynamics so what we did was was we found exact solutions to the learning dynamics of this form where the product of the in the layer 1 to layer 2 weights in layer 2 to layer 3 weights are of this form where essentially the system what it's doing the composite system is building up the singular value decomposition of the input-output covariance matrix mode by mode and each mode alpha associated with singular value alpha in the training data is being learned in the sigmoid illan ok in a way that so at x 0 a is sort of small and random you know some initial condition a 0 but over time as as time goes to infinity training time goes to infinity the the a approaches the actual singular value in the input-output covariance matrix so basically this is the learning dynamic so nothing happens for a while and then suddenly the strongest singular mode defined by the largest single value gets learned and then later on smaller singular mode gets learned and later on a even smaller singular mode gets learned and the time it takes to learn each mode is governed by 1 over the singular value so just intuitively stronger statistical structure as quantified by singular value is learned first okay that's the intuition often time when we train neural networks we see sort of these plateaus in performance where the network does nothing and then suddenly drops plateaus and drops and this actually shows that you can see very very sharp transitions in learning and you can actually show that the ratio of the transition period to the the ignorance period can be arbitrarily small infants also seem to show these developmental transitions but okay yes so that's so yeah you you can have arbitrarily sharp transitions in the system okay so the take-home messages so so far that is the network learns different modes of covariation between input and output and timescale inversely proportional to the statistical strength of that covariation and you can get these sudden transitions and learning now the question is what does this have to do with the hierarchical differentiation of concepts right that's what we'd like to understand first so now we've come up with a general theory of learning the nonlinear dynamics of learning in these in these deep circuits now we want to connect this back to hierarchical structure so one of the the things with J's work is they were just working with toy datasets and we didn't have any theoretical control over those toy datasets but we sort of understood implicitly these toy datasets of hierarchical structure so we need a generative model of data controlled mathematically well-defined generative model of data that encodes the notion of hierarchy okay so so can we move beyond specific data sets to general principles of what happens when a neural network is exposed to hierarchical structure that's what we like to answer so we consider a hierarchical generative model and a classic hierarchical generative model is is a yeah so essentially what we want to do is we want to connect the world of generative models to the world of neural networks and that you know that will connect the methods in computational neuroscience to this course eventually right so so so yeah so we have data generated by some generative model we take that data and we expose it to a neural network and we'd like to understand how the dynamics of learning depends on the parameters of the generative model okay so a natural generative model for defining hierarchical structure is a branching diffusion process that essentially makes the process of evolution where properties diffuse down a tree and instantiate themselves across a set of items so what do I mean by that okay okay so so so basically imagine for example that your items are at the leaves of a tree right and you can imagine that this is the process of evolution where there's some ancestral state maybe for one property so we do one property at a time and the properties are independent of each other this might be an ancestral state like can move right and then each time it that this this property diffuses down the tree there's a probability of flipping okay so maybe in this lineage which might correspond to animals this doesn't flip right and in these lineages corresponding to plants it does flip so these things cannot move and these things can move and then maybe it doesn't flip so all of these things inherit the property of moving so these are the animals and these things cannot move so these are the plants and then we do that for every single property independently and we generate a set of feature vectors so that's our generative model so what are the statistical properties of generative model so essentially because we know that we're analyzing these deep linear networks and we know that the learning dynamics of such networks is driven only by the input upper covariance matrix to understand the learning dynamics we just have to compute the singular values and singular vectors of Harken through structured data generated in this fashion and it's actually quite easy and we did it so here's what happens so imagine a nice symmetric tree like this so these are objects if we look at the similarity structure of objects measured by dot product in the feature space generated by the features under this branching diffusion process we get this nice blocks within block similarity structure where all the items on this on this branch are you know this item this item are slightly similar this item this item are even more similar and of course each item is most similar to itself so you have this hierarchical hierarchy of clusters that naturally arise because of this branching diffusion process so what are the singular values and singular vectors of the of the associated input-output covariance matrix well they turn out these are the one set of singular vectors the so called object analyzers which are functions across objects there's another set of singular vectors that are functions across features which I'm not showing you but we can under but but there's of course the duality right so that you get pairs of singular vectors for each singular value okay so what's the singular vector associate or the largest singular value well it's a uniform mode that's constant across all the objects but the next the most interesting one the next largest one is the most lowest frequency function essentially it's constant along all the ancestors of this branch and a different constant along all the ancestors of this branch so this singular vector essentially makes the most coarse-grained discrimination in this heart cookie structure data set the next set of seeing the vectors as a pair of them discriminate between this set of objects in this set of objects and don't know about these ones and the next one discriminates between this set of objects in this set of objects okay and then as you go down to the smaller singular values you get fine scale you get individual object discriminations right so this is how the Hart constructor is reflected in the second order statistics of the data and and this these are the singular values so the this is the theory for the singular values in a tree that has five levels of hierarchy and you can see that the singular values decay with the hierarchy level of the singular vectors okay so there's a general theory for this in which singular vectors are associated with levels of the tree okay so now you can see the end of the end of the story if you put the two together you automatically get the results that we that we were trying to explain right so essentially the general theory of learning says that the network learns input-output modes in a time scale given by 1 over C 1 over the singular value when the data is hierarchically structured singular values of broader hierarchical distinctions are larger than singular values of finer distinctions and the input-output modes correspond exactly to heart conditions in the tree so that essentially says the network must learn broad scale discriminations before it can learn fine scale discriminations so then actually what we did was we just analytically worked out that the dynamics of learning for hard Kuki structured data and we computed the multi-dimensional scaling and this was theory we never did a single simulation to get this plot we generated we generated a branching diffusion process with it was essentially this one and we just labeled these nodes arbitrarily with these labels and this is what we get right so we see the multi-dimensional scaling plot that we sort of see here and essentially just to compare this is what was done with a toy dataset over which had no theoretical control in a nonlinear neural network over which we had no theoretical control and this is a well-defined mathematical generative model under linear neural network and we see that we qualitatively explain the results so this is the difference between simulation and theory right now we have a conceptual understanding of effectively what was going on in the circuit and we really a sort of now it's no longer a mystery so now I think I understand what Jay was doing Jay and collaborators were doing it would be lovely if for all of the stuff that's going on in this course we could obtain such a deep rigorous understanding it's much more challenging but it's a goal worthy of pursuit I think ok so conclusions progressive differentiation of hierarchical structure is a general feature of learning and deep neural networks it cannot be any other way ok interestingly enough deep but not shallow networks exhibit such stage like transitions during learning so if you just do no hidden layers you don't get this actually you need it in and layer to do this and somewhat surprisingly it turns out that even only the second order statistics of semantic properties provide powerful statistical signals that are sufficient to drive this nonlinear learning dynamics right you don't need to look at the higher order statistics of the data to get this dynamic second order sisters sick suffice which was not obvious before we started okay so in ongoing work we can explain a whole bunch of things like illusory correlations early in learning so for example infants will initially say that they don't even know that for example pine trees don't don't have leaves then at an intermediate point they think that pine trees have leaves and then at a later point they correctly know that pine trees don't have leaves right so we can explain these non monotonic learning curves we can explain these familiar in typicality effects we can explain an inductive property judgments analytically we're looking at basic level effects we have a theory of category coherence and so on but in the interest of moving forward I wanted to give short shrift to this stuff so we essentially we can answer why are some properties learned faster basically properties that look that are low frequency properties on the leaves of the tree get learned faster properties whose inner product with sync with the singular vectors of largest singular value get learned faster that's the story why are some items more typical we have a theory for that how is inductive generalization achieved by neural networks we have a theory for that and so on and you know what is a useful mathematical definition of category hair and so for example you know there some things that are just intuitively called incoherent categories the set of all things are blue is a very incoherent category in fact it's so incoherent we don't have a name for such a category the set of all things that are dogs seems to be a very coherent category and so coherent that we have a you know well-known name for it names quite short actually to actually know I wonder if there's a theory where shorter words correspond to more coherent categories and that's like a an informative or efficient representation of category structure of anyways so we have a natural definition of coherent category that's precise enough to prove a theorem that coherent categories are learned faster and actually this this also relates the size of the categories so frequency effects show up anyway so there's a lot of stuff there but that was sort of the entry point so now what about a theory of learning in much deeper networks that have many many layers okay so again I'm gonna make a long story short because it's all published you can read all the details but I wanted to give you the the spirit or the essence of the intuition behind the work okay so the questions we like to answer are how does training time scale with depth how should learning rate scale with depth how do different weight initialization impact learning speed and what we'll do is once we understand these theoretically we'll find certain weighed initializations that correspond to critical dynamics which I'll define can aid deep learning and generalization so the basic idea is in a very very deep neural network right you have a you you have a vanishing exploding or gradient problem and that's one of the issues that makes deep neural network learning hard so if you're going to back propagate the error through multiple layers the back propagation operation is a product of jacobians from layer to layer and that product of jacobians is fundamentally of linear mapping right so if the singular value is associated with that linear mapping so essentially if the singular values of the Jacobian and each layer are large bigger than 1 the product of such matrices will lead to a product matrix that has singular values that grow exponentially with depth similarly if the singular values are less than 1 they'll decay with depth right so that's that's a vanishing gradient in the latter case and an exploding gradient in the former case that seems to be one of the major impediments understanding deep learning so what people often did was they tried to scale the matrices to avoid this question right so what they often do is they they picked they initialize the weights randomly so that W is a random matrix where the elements W IJ are iid and Gaussian with a scale factor scaled precisely so that the largest eigen value of the Jacobian or the backpropagation operator is one okay so it's like scaling the system so that if you place a random error vector here right the desired output minus the actual output and back propagated through a random network a random vector will preserve a rare vector will preserve its norm as its back propagated across and so such and this is the famous sort of glory and Benjy Oh initialization and it works pretty well for depth four or five or whatever right okay so we would like a theory of that for the learning dynamics of that and as I said there's no hope for a complete theory at the moment with arbitrary nonlinearities okay so what we're going to do is we're to analyze the learning dynamics just we'll get rid of the nonlinearities right so again it might seem like we're throwing the baby out with the bathwater but we're actually going to learn something that helps us to Train on than your networks okay so the basic idea then is that we have a network which is linear so why the output is a product of waves right so then the the back propagation okay so so then the back propagation is again just a product of matrices right the gradient descent dynamics is long linear and coupled and non convex and actually even in this linear network you see plateaus and sudden transitions right and actually interestingly enough even in this very deep linear network you see faster convergence from pre-trained initial conditions right so basically if you start from random Gaussian initial conditions you get slow learning for a while and then sudden sudden learning here relatively sudden learning year whereas if you pre train the network using greedy unsupervised learning right so this is the time it takes to pre train you get sudden learning in a drop here so remember if you go back to the original Hinton paper this was the phenomenon that started deep learning greedy unsupervised pre-training allows you to rapidly train very very deep neural networks so the very empirical phenomenon that led to the genesis of deep learning was present already in deep linear neural networks right so deeply linear neural networks in terms of their expressive power are crappy because the composition of linear operations is linear they're not good they're not a good model for deep nonlinear networks in terms of input-output mappings but they're a surprisingly good model theoretical toy model for modeling the dynamics of learning in nonlinear networks ok because because very important phenomena also arise in the deep linear networks and we're focusing on learning dynamics here ok so we can build intuitions for the nonlinear case by analyzing the linear case ok so we went through the three layer dynamics already what about the multiple layer dynamics so again the Jacobian can back propagator explode right so what we okay so again I'm going to make a long story short but what we find is that if you take what we tell you the final result what we find is that we find a class of weight initializations that allow learning time to remain constant as the depth of the network goes to infinity now I'm measuring learning time in units of learning epochs right so obviously to train a deep neural network very very deep neural network it just takes longer to compute each gradient right so in cutting in terms of real time of course the time will scale with the depth of the network but you might imagine in terms of number of gradient evaluations as the network gets deeper and deeper it might take longer and longer to train it and we show a class of initial conditions which that's not true as the as the network gets deeper and deeper the number of gradient evaluations you need to train the network can remain constant even as the depth goes to infinity okay even in a nonlinear network ok so let me give you the intuition for why okay so for example we okay and the classical initialization this the scleral and Benjy initialization doesn't have that but our initialization does so basically what we did was we'll start off with the linear networks we trained deep linear networks on M NIST and we scaled the depth like this right and we started with random Gaussian initial conditions and then ran back propagation but scaled random - the notion was random Gaussian initializations and we found that the training time as you might expect grew with depth this is training time measured a number of learning a box or a number of gradient evaluations but here what we did was we initialize the weights using random orthogonal weights right and then we found that the learning time didn't grow with depth and also if you pre train it it doesn't grow with depth okay so there's a dramatically different scaling and learning time between random Gaussian initialization and random orthogonal initialization why okay and the answer is the following right let's think about the back propagation operator let's say you want to back propagate errors from the output to the input right so the the back propagation operated in a linear network right it's just the product of weights throughout the entire network okay so so your your so the back if you do a random weight random Gaussian weight initialization here then the this is a product of random Gaussian matrices so to understand the statistical properties of back propagation you need to understand the statistical properties of the singular value spectrum of random Gaussian matrices there isn't really a general theory for that but we can look at it numerically and get intuition for it so the basic idea is if you have one random Gaussian matrix right the singular values of W are the are the eigen values of W transpose W that's a famous distribution called the Marcinko posterior distribution and you know they're they vary in a range that's order one okay so if you're back propagate through one layer you're fine right you don't get vanishing x1 ingredients okay but if you if you look at the singular values of a product of five random Gaussian matrices the singular value per spectrum gets very distorted you get a large number of single values that are that are close to zero and a long tail that you know extends up to four okay but if you if you do a hundred layers you get a very very large number of singular values that are close to zero and a very much longer tail okay now these this is a product of random Gaussian matrices so if you feed a random vector into this on average its norm will be preserved the vectors length will not change but we know that preserving the length of a vector is not the same as preserving angles between all pairs of vectors okay so actually the way that this network that the way that this product of random Gaussian matrices preserves the norm of the gradient is it does it in a very anisotropic way it takes an error vector at the output and it projects it into a low dimensional space corresponding to the singular values that are large and then it amplifies it in that space so the length is preserved but all error vectors get projected onto a low dimensional space and amplified so a lot of error information is lost in a product of random Gaussian matrices okay so that's why the Gloria and Benjy initial conditions work well up to depth five or six or seven but they don't work well up to depths say a hundred or in recurrent neural networks as well okay so so what can we do well a simple thing we can do is we can replace the the matrices these random matrices with a fog '''l matrices okay so we know that all the singular values of an orthogonal matrix are one every single one and the product of orthogonal matrices is orthogonal so therefore the back propagation operator has all of its singular values equal to 1 and there's generalizations of orthogonal matrices to two rectangular versions when when the layers don't have the same number of neurons in each layer okay so this is fantastic so this works really well for linear networks okay but how does this generalize to non linear networks because then you have a product of jacobians right so what happens here okay so what is that what is the the product of Jacobian so what you have is you have a so if we imagine how if we imagine how either errors back propagate to the front or how input perturbations back propagate to the to the end right it's the same thing so it's easier to think about forward propagation imagine that you have an input and you perturb it slightly how does the perturbation grow or decay well what happens is there's a linear expansion or contraction due to W and then this this nominator is usually compressive so there's a nonlinear compression due to the Jacobian passes at the diagonal Jacobian passing through the point wise non-linearity and then again linear modification and nonlinear compression linear modification nonlinear compression okay so what we could do is just simply choose these again to be random orthogonal matrices and then what happens is the growth or decay of perturbations is and we scale the we scale the random Gauss image the random orthogonal matrices by a scale factor to combat the nominee or compression and then the dynamics of perturbations is like this you rotate linearly scale non linearly compressed rotate linearly scale nonlinear compress and so on that's essentially the type of dynamics that occurs in in dynamically critical systems that are close to the edge of chaos you get this alternating phase space expansion and compression that's in different dimensions at different times okay so now you can just compute numerically under that initialization how does the product of Jib how does a singular value spectrum the product of Jacobian scale and it scales beautifully so this is the scale factor for the type of non-linearity that we used the hyperbolic tangent non-linearity the optimal scale factor in front of the random orthogonal matrix is 1 and you see when you choose that even for this was a hundred layers I believe even for a hundred layers the end-to-end Jacobian from the input to output has a singular value spectrum that remains within a range of order one if G is even slightly less than one the singular values exponentially vanished with depth if G is larger than one the singular values grow but actually not not as as quickly as you'd think so this is the the critically dynamical regime that at least preserves not only the norm of back propagated gradients but all angles between pairs of gradients right so it's an isotropic isotropic preservation of error information from the end of the network all the way to the beginning ok so does it work and it works better than other initializations even in nonlinear Network so we trained 30 layer nonlinear networks and the initialization works better and and so also interestingly enough at this critical factor you also achieve better generalization error and we don't have a good theory for that actually the test error and the training error of course goes goes down so okay so that's an interesting situation where a theory of linear networks led to a practical training advantage for nonlinear networks ok so here's another question that we had ok there's a whole like world of convex optimization we want our machine learning algorithms to correspond to convex optimization so we can find the global minimum and there are no local minima to impede us from finding the global minimum all right that's a conventional wisdom yet the deep neural network people ignore this conventional wisdom and train very very deep neural networks and don't worry about the potential impediments to local minima they seem to find pretty good solutions why ok is the intuition that local minima are really an impediment to nonlinear caught non convex optimization in high dimensional spaces really true ok and you might think that it's not true for the following intuitive reason right so again it's often thought the local minima at some high level of error in training error stand is a major impediment to non convex optimization ok and you know this is an example a two-dimensional caricature of a protein folding energy landscape and it's very rough so there's many many local minima and and the global minimum might be hard to find and that's true if you sort of draw a random generic surfaces over low dimensions those random surfaces will have many local minima but of course our intuition about geometry derived from our experience with a low dimensional world is woefully inadequate for thinking about geometry in high dimensional spaces ok so it turns out that random non convex error functions over high dimensional spaces local minima are sort of exponentially rare in the dimensionality relative to saddle points just intuitively imagine you have an error function over a thousand variables say a thousand synaptic weight it's a deep network that's a small deep network but anyways let's say there's a point at which the gradient and weight space vanishes so now there's a thousand directions in weight space you could move away from that from that extrema what are the chances that every single direction you move has positive curvature right if it's a fairly generic landscape the answer is exponentially small in the dimensionality some directions will have negative curvature some directions will have positive curvature unless your critical point is already at the bottom in that case most direction will have positive curvature or unless your critical point is at the top and higher than most directions no negative curvature right so statistical physicists have made this intuition very precise for random landscapes and they've developed a theory for it so this is a paper in Physical Review Letters by Bray and Dean so what they did was they imagined just a random Gaussian error landscape so what they did was they looked at an error landscape ok that's a continuous function over n dimensions but there is correlations it's correlated over some length scale so it's like a rat it's a single draw from a random Gaussian process where the kernel of the random cows of the Gaussian process is falling off with some length scale so the error at point 1 is correlated with the error at point 2 over some length scale and that that correlation falls off smoothly so it's a random smooth landscape ok but the correlations are local essentially and then what they did was they asked the following question Let X be a critical point a point where the gradient vanishes ok we can plot every single critical point in a two dimensional feature space what is that feature space well the horizontal axis is the error level of the critical point at how high on the error axis does this critical point sit and then this F is the fraction of negative eigenvalues of the Hessian at that critical point so it's the fraction of directions that curve downwards and the fraction ok so now a priority critical points could potentially sit anywhere in this 2 dimensional feature space right it turns out they don't they concentrate on a monotonically increasing curve that looks like this so the higher the error level of critical point the more the negative curvature directions you have and to be an order one distance away from this curve the probability of that happening is exponentially small in the dimensionality of the problem okay now what does that mean it automatically implies that there are no local minima at high error or at least they're exponentially rel rare rather to saddle points of a given index okay so basically you typically never encounter local minimum at high error right that would be stuff that sits here and there's nothing here okay second if you are a local minimum which means on this axis you're at the bottom then your error level must be very very close to the global minimum okay so if you're if you get stuck in a local minimum you're already closed an error to the global minimum yeah so if you're a local minimum your error level will be close to the error level of the global minima why because what does it mean to be a local minimum it means that F is zero the fraction of negative curvature eigenvalues lasting is zero and this is the distribution of error levels of such critical points they're strongly peaked at this value which is the value of the global minimum essentially there's nothing out here okay all right now in physics there's this well-known principle called universality there are certain questions whose answers don't depend on the details for example certain critical exponents in the liquid gas phase transition are the exactly the same as critical exponents in the ferromagnetic phase transition because the symmetry and dimensionality the order parameter density in the case of liquid and magnetization in the in the case of ferromagnets are the same so there's certain questions whose answers don't depend on the detail they only depend on the symmetry and dimensionality of the problem so one might think that this qualitative prediction is true in just generic high dimensional error landscapes now the computer scientist would say no no no no no no no your random landscapes are a horrible model for our error landscapes of deep neural networks trained on Emma stand in CFR 10 and and so on and so forth you're completely irrelevant to us because we're doing something special we're not doing something random we have a lot of structure okay the physicist might counter well you know you just have a high dimensional problem and just the basic intuition that in my dimensions it's very hard to have all directions curved up at high at a critical point high error should also hold true in your problem okay but of course we'll never get anywhere if we stop there right we have to move over into your lands which is also my land and and and just simulate the system so oftentimes you know biologists and computer scientists don't believe a theory until they see the simulation okay so what we'll do is we'll search for critical points in the error landscape of deep neural networks and that's what we did so what we did was we use Newton's method okay to find critical points so it turns out that Newton's method is attracted to saddles right so Newton's method will descend in the positive curvature direction that it will ascend in the negative curvature direction because Newton's method is gradient descent multiplied by the inverse of the Hessian so if the Hessian has a negative eigen value you take the negative gradient multiply it by the negative eigen value to turn back around and you go uphill so Newton's method uncorrect it is attracted to saddle points okay so what we did was we looked at the error landscape of deep neural networks trained on em missed and c510 and we just plotted the prediction of random landscape theory right and what we found was exactly qualitative either prediction we took each critical point and plotted in this two dimensional feature space and we found that the critical points concentrated on a monotonically increasing curve which again shows that there are no local minima at high error and if you're a local minimum your error is close to at least the the lowest error minimum that we found we can't guarantee that the lowest error minimum we found is a global minimum but qualitatively the structure holds okay now the issue is what can we do about it so what this is telling us that even in these problems of practical interest saddle points might stand as the major impediment to optimization right because saddle points can trap you you know you might get you might go down here and then there might be a very slowly curving negative curvature direction that might take you a while to escape in fact in the learning dynamics that I showed in these transitions in learning hierarchical structure the thing controlling the transitions with the existence of saddle points and weight space of the linear neural and so the the part of no learning correspond to to sort of falling down this direction slowly and then the rapid learning on to eventually coming out this way okay so how do we how do we do that well what we can do is we can do a simple modification to Newton's method which instead of dividing by the Hessian we divide by the absolute value of the Hessian and again I should say that this was done in collaboration yoshua bengio slab and a set of fantastic graduate students in yoshua bengio slab did all of this work on the training and testing of these predictions okay so what we we suggested was you know the offending thing is dividing by negative eigenvalues so just take the absolute value of the Hessian which by definition I mean take the Hessian compute its eigenvalues and replace each negative one with its absolute value okay so that will obviously get repelled by saddles all right and and that actually works really really well and there's a way to derive this algorithm in a way that makes sense even far from saddles using by minimizing a linear approximation to F within a trust region in which the linear and quadratic approximations agree okay so let me just show you first that it works so that this is the most dramatic plot so basically what we did was we did stochastic gradient descent right for awhile and then it seemed like the error as a function of training time plateaued both for a deep autoencoder and a recurrent neural network problem so when the error is a function of training time plateaus that sort of interpreted as the fact that you're stuck in a local minimum right but actually when we switch to this what we call the saddle free Newton method the error suddenly drops again so this was an illusory signature of a local minimum it was actually a saddle point with probably a very shallow negative curvature direction it was hard to escape and then we switched to our algorithm we could escape it okay and and and you know that what these curves show is that we do do better in the final training error as well so now how do we train deep neural networks with thousands of layers and actually how do we how do we model complex probability distributions so we want to we want to sample from very very complex probability distributions and do complex distributional learning right so this was done by a fantastic postdoc of mine Joshua Sol Dickstein so we were going to Berkeley for this nanak Cobra missile mechanics meetings and things like that and there's been lots of advances in our computer system mechanics where you can show that roughly speaking the second law of thermodynamics which says that things get more and more disordered with time can be transiently violated in small systems or short periods of time so you can spontaneously generate order okay so let's so I'll just go through this again very quickly so so here's the basic idea let's say you have a complicated probability distribution let's just destroy it let's feed that probability distribution through diffusion to turn it into a simple distribution maybe an isotropic Gaussian and we keep a record of that destruction of structure and then we try to reverse time in that process by using deep neural networks to reverse time and then essentially create structure from noise and then you have a very very simple way to sample from complex distributions if you can train the neural network which is you just sample noise and you feed it through a deterministic neural network and that constants a sample from a complex distribution and so this was inspired by recent results in non equilibrium static so the basic idea again is you know let's imagine that you have a very complex distribution corresponding to this density of dye you diffuse for a while it becomes a simpler distribution eventually become uniform you keep a destruction now you if you if your reverse process of diffusion you'll never go from this back to this but if your reverse process is a neural network trained to do it you might be able to do it okay so that's the basic idea so that's what we did and I'll just show you some nice movies to show that it works this is the classical toy model will go to more complex models this is some distribution in two-dimensional space and so what we do is we just systematically have the points to fuse under Gaussian to fusion with the restoring force to the origin so the stationary distribution of that of that destructive process is an isotropic Gaussian okay and that and that's that's that's what happens so that's our training data ire movie is the training data okay then what we do is we train a neural network to reverse time in that movie so it's an it's a neural network with many many layers hundreds and hundreds of layers right so classically training a network of hundreds of layers you have the credit assignment problem because you don't know what they the intermediate neurons are supposed to do convened the credit assignment problem because each layer going up to the next layer just has to go from time T to time t minus one in the in the training data so you have targets for all the intermediate layers therefore you've circumvented the credit assignment problem okay so it's it's relatively easy to train such networks and so once you have such a network what should be able to do you should be able to feed that neural network an isotropic Gaussian and then have that Gaussian be turned into the data distribution so that's what happens this on the right is a different Gaussian and we just feed it through the trained deterministic neural network and out pops the it's not perfect there's some data points that are over here but this is roughly the distribution that had learned which is similar to what it was trained on okay so now we can look at slightly more complicated distribution so okay so that's that so now we can train it on a toy model of natural images right so a classic toy model of natural images is the dead leaves model where the sampling process is you just throw down circles of different radii so you get a complex model of natural images that in count that has long range edges occlusion coherence over long length scales and so on and so forth so we can train the neural network on such distributions we train it in a convolutional fashion by working on local image patches and we convolve so information and propagate over long ranges and so again we take these natural images and turn them into noise keep a record of that movie and then reverse the flow of time using a deep neural network and it's okay so once we train that right we should be able to turn noise into the network's best guess as to what a dead leaves model would look like so this is what happens it's taking noise and it turns it into a gas okay so it's not a perfect model but it turns out log probability of dead leaves under this generative model that consists of a thousand layer deep neural network that's higher than any other model so far so this is currently state of the art okay and as you can see it gets long-range coherence and sharp edges and moreover it gets long-range coherence and the orientation and hard to do in in generative models of natural images okay now we can actually do something somewhat practical with this is we sample from the conditional so then we also trained it on textures okay so so so for example textures of bark right and we can also sample from the conditional distribution so what we can do is we can clamp the the pixels outside of a certain range replace the interior wouldn't make it blank and because the network operates in a convolutional fashion information from the boundary should propagate into the interior and fill it in right so if we look at that so that's white noise and the network is filling it in and it fills in the it fills in the best guess image okay and so again it's not identical to the original image but it does get long-range edge structure coherence and the orientation of the edge and smooth structure as well and again this is like a thousand layer neural network now there's some lessons here okay oftentimes when we model complex data distributions what we try to do is we try to create a stochastic process whose stationary distribution is the complex distribution right now if your distribution has multiple modes you're going to run into a mixing problem because it can take a stochastic process a long time to jump over energy barriers that separate the multiple modes so you always have a mixing problem that often times a new train system train publishing models you have to sample and then use the train the model so that makes training along the long that makes trailing training take a long time so what we're also doing in addition to circumventing the credit assignment problem in training very deep neural networks we're circumventing the mixing problem in training the generative model because we're not trying to model the data distribution as a stationary distribution of us to catch there would have to run for a very long time to get to the station distribution we're demanding that during training the process get to the the data distribution in a finite amount of time right so because during training we demand that we get to the data distribution of fine item at a time during training and that's the idea that that's idea there's lots of results now that show that you can attain information about stationary equilibrium distribution through non equilibrium trajectories okay so that now I'm done so let's see well okay so there's that ok so again you can read about all of this stuff in this set of papers again I'd like to thank my funding and just the the key players so Andrew sacks you know did the work with me on nonlinear learning dynamics and learning how academy structure joshua's addiction did the work on deep learning using noncom thermodynamics and the the the work and saddle points was a nice collaboration Yoshi Avengers lab and again fantastic graduate students in yoshua bengio Salaam okay so I think there's a lot more to do in terms of unifying neuroscience machine learning physics math statistics all of that stuff it'll keep us busy for the next century you
for the nonhomogeneous system of equations which means that for your right hand side at least one of B1 B2 BN is non zero let's see how to solve this so the general Matrix representation is axal B so premultiply it by a inverse so a inverse * a * x = a inverse * B you have to do the operation on both sides of the equation to keep the relation the same and provided a inverse exists then you will recognize this part A inverse * a as the identity Matrix of order n equals a inverse * B and any Matrix multiplied by identity is just itself so that gives us the solution a inverse * B provided a inverse exist so that is adjoint of a over determinant of a * B so this is the method to find the solution of a set of linear equations in N unknowns now there are some special cases uh so this thing works fine if the determinant is non zero now what happens if the determinant is zero so the special case if determinant of a is zero look at the numerator of this expression if ad joint of a times B if this product is not zero the system of equations this whole system of equations is inconsistent and it has no solution but if uh the numerator is also zero in addition to the denominator being zero then the system is consistent and it has an infinite number of solutions okay so this is one way using Matrix operations to solve a system of simultaneous linear equations a second way is using determinants or what is known as Kramer's rule for the same system of equations axal B the solution for each unknown variable is given as x i equal 1 / determinant of a times this determinant so a11 a21 a N1 a12 a22 a N2 and then observe this carefully find the I column of the determinant so A1 I replace it by B1 A2 I replace it B2 yeah so this is a substitution operation you don't keep the original term A1 I A2 I and so on keep replacing this by the elements of the right hand side BN and then the rest of the determinant stays the same all the way up to A1 n A2 n a n n so this this whole operation uh we can write it in short hand as AI over a where the determinant AI basically means that you're substituting the I column of that determinant by the right hand side or the elements of Matrix B and this is known as Kramer's rule in reality these two are identical um if you care to write this whole thing down symbolically and you can show that this works out to be this exactly so each element of this uh X will work out to be um adjoint of a over determinant of a Time the Matrix B which will work out to be this now again what happens if the denominator is zero so special case if a equals 0 well again look at the numerator if any of the numerators is also zero uh inconsistent set of equations there is no solution I'm sorry if uh any of the determinants in the numerator is non zero it is inconsistent if all AIS are zero for all I the system is consistent and it has infinite solutions so we have looked at uh two methods to how to solve a system of simultaneous linear equations in N unknowns for the nonhomogeneous case which means that at least one of the elements of your right hand side the B Matrix is nonzero now the only thing to look at that is remaining is the homogeneous system which means that every single one of the right hand side is zero again we can look at it uh using this method for example if the determinant of a is non zero there's only a trivial solution which means every every single variable X1 X2 all the way to xn is zero but if the determinant is zero then there's an infinite number of solutions so these are the two general methods to find out the um values of the unknown variables X1 X2 all the way through xn and so let's take a solved example to better understand how to work these methods so let's say solve this system of equations so you have x + 7 y - 3 Z = 11 25 y + z = -3 and 3x - 6 y + 2 Z = 0 now as soon as soon as you have a set of equations given to you there are a few things to check so first of all uh how many unknowns so x y z three unknowns and you have three equations and three unknowns so the number of equations is equal to the number of unknowns now the right hand part of the set of equations so 11 - 3 0 so clearly it is not a homogeneous system because you have these nonzero elements on the right hand side the next thing to check would be this determinant so this part axal B so let us first write it in that form so a would be 1 7 - 3 or here it's 0 * X so 0 25 1 and 3 - 6 2 this is your a matrix times the variable Matrix in this case x y z = b which is 11 - 3 0 so after writing it in this Form axal B the next thing to check is whether this determinant a is zero or not so check this calculation so 1 7 - 3 0 25 1 3 - 6 2 works out to be 1 * 56 I'm expanding by this column note that if any column or row of the determinant has one or more zero elements it's always easier to expand by that row or column so that's why I'm choosing to expand by this column instead of by any other row or column so 1 * 50 + 6 so 56 - 0 * something the co-actor of this element which we don't care Plus 3 * so 7 minus - 75 so 82 so the value of the determinant of a works out to be 302 which is not zero so so which means we can find out uh these unknown values so because we are taking this as a worked out example I want to demonstrate this by both methods so let's first try to find out the values of X Y and Z by The Matrix method so using the Matrix method well X Y and Z you need to find out as a inverse * B using this definition and to do that you first need to find out a inverse so that is AD joint of a times B over determinant of a um and for calculating the ad joint um you need to find out the co-actors of each element of a and so let's take an example U recall C11 is -1 to ^ I + J so 1 + 1 and then um you block this off block this off so so uh 50 minus - 6 so 56 and so on I leave it as an exercise for the reader uh to calculate each co-actor and what you can show is um ad joint of a is basically the transpose of all the co-actors of the elements of a works out to be this so 56 4 82 3 - 7 -1 - 75 27 and 25 so a inverse is simply ad joint of a divided by the determinant of a so 1 / 302 times this whole big Matrix I'm not going to write it again and then once you have a inverse you get your unknown variables x y z is a inverse times B well I'll write it here 1 over 302 * 56 4 82 3 - 7 times your right hand side Matrix which is 11 - 3 0 and you can do this calculation and it works out to be let's still keep this outside 1 over 302 and first do the multiplications here and you can show that this works out to be 64 0 - 96 which finally gives you X is 2 Y is 0er and Z is minus 3 so this is one way to find out the values of x y and z